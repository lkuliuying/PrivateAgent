# 仓库整理与历史引用保留记录

核验日期：2026-08-31（Asia/Shanghai）。本记录针对仓库结构、Git 引用和发布入口；不代表 1.0.3 故障已部署修复，也不代表安装包或产品功能已重新验收。

## 整理范围与现场保护

整理开始时，当前检出为 `dev/1.0.0`，HEAD 为 `0c1705570ed0b75940189d52708c59b0021c2034`。共有 1,075 个受控文件；原工作区另有 13 个已有变更文件，包括运行时修复、测试及会话交接文档。这些文件保留在原工作区，不纳入本次整理提交。

本次在独立分支 `codex/repository-maintenance-20260831` 和独立 worktree 中实施。原工作区 `.run/repository-maintenance-*/` 保存整理前 Git bundle、引用清单、工作区与暂存区补丁、已有文件副本及 SHA256 清单，以及 GitHub 发布元数据。该目录被忽略，不应上传到发布页或复制为运行环境。

不重写历史，不改普通版或远程版版本号，不删除标签、Release、正式 E2E 快照、数据库迁移或签名素材，不移动存在相对路径依赖的脚本，不清空 `.run/`、`.tmp/` 或虚拟环境。

## 分支与历史阶段映射

以下是整理前通过 `git ls-remote` 与本地提交图交叉核对的快照，后续操作前仍须重新核对远端。

| 分支 | 整理前提交 | 处理方式 |
| --- | --- | --- |
| `main` | `962a4f0bc6d7e054a0762687eca87305131d15d3` | 保留默认分支；本次不合并产品改动 |
| `dev/1.0.0` | `180603b6690d2fb18f08bf10c4197520b97840cc` | 保留开发主线；整理通过独立分支审查 |
| `dev/v0.8.0` | `253d0755b68e96bf2541fa8a9a1c7ead02df800e` | W0 遗留引用，已完全包含于 main；保存映射后按准确旧 SHA 清理 |
| `agent/desktop-architecture-motion-refactor` | `87cf83b59eaa624c7b3067e5504a990811ba0044` | 保留，等待逐项能力取舍，不整体合并 |

远端 `main...dev/1.0.0` 的左右独有提交数为 2 / 7。main 的两项是 PR 合并提交；本地开发分支已包含双方历史，与远端开发分支的已提交文件树相同。本地显示 ahead 4 来自合并历史，不应因此 reset、rebase 或强制推送。

`dev/v0.8.0` 当前指向 W0，而不是完整的 v0.8.0 开发终态。历史文档中的分支名保留当时语境，准确阶段按下列提交追溯，不按旧分支 tip 新建正式版本标签：

| 阶段 | 永久提交引用 |
| --- | --- |
| W0 UI 审计与状态冻结 | [253d075](https://github.com/lkuliuying/PrivateAgent/commit/253d0755b68e96bf2541fa8a9a1c7ead02df800e) |
| rc.1 冻结与开发完成检查点 | [1227b15](https://github.com/lkuliuying/PrivateAgent/commit/1227b15fcc1ae34e88d5e02165e340ed324bd3ac) |
| W6-R/R2/R3 修订终态与 v0.9.0 交接 | [d5add0b](https://github.com/lkuliuying/PrivateAgent/commit/d5add0bea78bcc23036d8cd24076e482d85be6ff) |

旧分支删除须同时满足：远端仍指向上述 W0 SHA、该 SHA 仍为当前 main 的祖先、无未合入提交、无当前脚本或工作流依赖。删除使用期望旧 SHA 的 lease，远端发生变化则停止；不能无条件删除。仓库内未发现针对该分支删除的 Actions 触发器，外部 webhook 不在本次审计范围。

如后续确需恢复旧引用，先确认远端不存在同名分支，再由维护者执行：

```powershell
git push origin 253d0755b68e96bf2541fa8a9a1c7ead02df800e:refs/heads/dev/v0.8.0
```

### 旧架构分支的后续取舍

该分支有 8 个无补丁等价记录的独有提交；这不证明全部功能均未被后续开发吸收。相对分叉点涉及 186 个文件，不能仅为收敛分支而整支合并。

| 能力 | 已知情况与处理 |
| --- | --- |
| 动画、无障碍、视觉回归、包体预算、代码签名 | 当前主线已有同类能力，逐项比对行为后决定是否迁移，不重复引入整套旧实现 |
| Windows 安装、升级、卸载和隔离环境检查 | 保留旧脚本供评估；适配当前应用标识、双更新渠道和运行时后重新验证 |
| 真实服务压力测试、进程 watchdog 和清理机制 | 保留，先核对测试数据库隔离、进程归属与故障退出边界 |
| 旧 Windows assurance 工作流 | 触发器绑定旧架构分支名，不能只复制 YAML；需随依赖脚本和新触发条件一起评审 |

## 标签、Releases 与版本口径

五个实际标签全部保留。表中提交为解析后的 commit，而非 annotated tag 对象 SHA。

| 标签 | 提交 | 用途 |
| --- | --- | --- |
| `v0.1.1` | `89849e5996042b4baef03b01a8ad6cbb8abed42c` | 历史版本 |
| `v0.2.1` | `a3ac17c27eeac3a5636cd15f28f1d7fa2f22860e` | 普通版当前 GitHub Latest |
| `0.5.0-rc.4` | `9250da68d4c018c676ec5ab38d5eb08103e8ed2d` | 历史候选版 |
| `v0.7.0-freeze` | `ae0f312c3b8e365f1b5c9b9aede28297faec7789` | 内部开发冻结点 |
| `remote-v1.0.2` | `debcd81d9b6084f8ab13f4c0423b9f14696b9496` | 已发布远程客户端 |

GitHub 共五条 Release 记录：

| Release | 核验状态 | 本次处理 |
| --- | --- | --- |
| `v0.2.1` | 已发布、普通版 Latest、8 个附件 | 保留 |
| `remote-v1.0.2` | 已发布、3 个附件 | 保留，不设置为普通版 Latest |
| `0.5.0-rc.4` | 当前 draft=true、prerelease=true、3 个附件 | 保留；历史 published_at 非空不改变其当前草稿状态 |
| `v1.0.0` | 草稿、2 个附件，内容是远程便携客户端 | 保留，不直接发布为普通版正式安装包 |
| `remote-v1.0.3-test.1` | 测试草稿、2 个附件 | 保留 dirty 构建说明，不改指向后追认为新源码产物 |

`v1.0.0` 与 `remote-v1.0.3-test.1` 目前仅存在于草稿的 tag_name 字段，没有对应实际 Git tag。新正式交付须从准确的干净提交重新构建并绑定哈希、签名和验收证据，不能覆盖旧测试附件来消除来源差异。

源码清单的 `1.0.0`、普通版已发布的 `0.2.1`、远程版已发布的 `1.0.2` 和测试安装包 `1.0.3` 是不同口径。远程构建会生成独立配置，不为统一数字而修改普通版版本源。

本次只读请求两条在线更新清单均返回 HTTP 200：普通版为 `0.2.1`、平台键 `windows-x86_64`；远程版为 `1.0.2`、平台键 `remote-windows-x86_64`。两者包含签名字段，但读取清单不等于安装包验签或在线升级验收。本次不改清单和任何在线服务。

发布前必须先核对检出 tag/SHA 与产物来源，拒绝已有同名附件；发布触发、旧标签工作流和双渠道限制见[发布检查清单](./release-checklist.md)。

## 文件整理约定

- 文档入口区分当前说明与历史阶段；历史设计验收改用准确提交中的永久链接，不恢复本机临时文件。
- 已忽略的参考资料目录和 ZIP 归档是可选本地内容，索引不再提供不存在的相对链接，不改变忽略策略。
- `scripts/` 保持位置，分类与调用方式见[脚本索引](../scripts/README.md)。
- 删除无受控源码引用的 `tauri.svg`、`vue.svg` 脚手架素材；`vite.svg` 仍被页面 favicon 引用，保留到品牌图标替换任务。
- 保留正式 E2E 快照、版本验收证据、完整 Alembic 迁移链及依赖锁文件；同内容的不同尺寸测试快照不能仅按哈希去重。

原工作区的运行时修复与共享项目状态文件保持原样。整理分支合入开发主线时，如与这些后续文档变更重叠，应保留双方有效内容，不能整体覆盖 README 或文档索引。

## 验证与未完成边界

本次验证针对 Git 引用、文件引用和发布保护，不运行生产数据库测试、付费模型调用、签名服务或实际发布。

| 检查 | 结果与边界 |
| --- | --- |
| 发布保护回归 | 默认 PowerShell 执行策略下 15 项离线测试通过；使用临时 Git 仓库与本地 gh 替身，不调用真实上传 |
| Python 静态检查 | 新增测试文件 Ruff 通过 |
| 工作流结构与脚本语法 | YAML 解析通过；辅助脚本及 11 个 PowerShell 运行块解析通过；既有触发事件保持不变 |
| 文档引用 | 156 个 Markdown 文件的简单本地链接目标检查无缺失；不涵盖所有扩展语法、外链和锚点 |
| 历史文件与素材 | 两条固定提交中的 design-qa.md 路径存在；被删除的两项 SVG 无源码引用 |
| Git 差异 | `git diff --check` 通过；没有混入原工作区的运行时修复和会话记忆文件 |

这些检查不等于产品构建、GitHub 托管 runner、SignPath、真实安装或在线升级已验收。原目录中两个会话文档在整理期间由其他会话更新，已保留其最新内容；没有回写初始备份。

`main` 的 PR 检查与保护规则仍应在可持续运行的 CI 建立后单独配置；本次不把尚不存在的检查设为必需，以免阻断正常协作，也不声称已经启用保护规则。
