# 0.4.0-alpha.2 开发进展（2026-08-08 · 第三轮）

> 版本：`0.4.0-alpha.2`（开发中）
> 基线与入口：alpha.1 检查点（`995877a`，release-check 14/14 绑定 `2c6f497`）
> 计划：D4 业务工作区迁移 → D5 动效/无障碍/性能 → D6 视觉 QA/实机/检查点

## 第一轮（已提交 ea80395）

### 验收遗留（alpha.1 后用户反馈）
1. **Python "协程未等待" 运行时警告**：`agents/runtime.py` `_await_with_cancellation`
   先 `ensure_future` 调度再检查取消，消除取消窗口内协程未等待警告；
   `-W always::RuntimeWarning` 全量 637 passed、零警告。
2. **NavRailV2 4 处 RGBA 字面量**：收敛为 `--pa-rail-*` 组件令牌（legacy NavRail 同步）。

### alpha.2 契约切换：默认新 UI
- `DEFAULT_UI_V2 = true`；E2E 适配（legacy 动画测试固定 `?ui=v1`、共享行为测试壳无关化）；
  Playwright 25/25（默认壳 = v2）。

## 第二轮（已提交 8802a61）

- D4：`features/knowledge/` DocListItem 文档行拆分 + PaEmptyState/PaErrorState；
- D5：包体基线核对（合计 gzip +6.2%，≤15% 门槛内）；
- D6：`e2e/visual-regression.spec.ts` 5 个 toHaveScreenshot 断言基线。

## 第三轮（本记录）

### D4 · 今日视图组件化（`features/today/`）
- 新增 `OverviewCards`（概览卡片，纯展示 + navigate）与 `PriorityList`
  （优先事项/空态引导，收件箱/提醒/捕获/文档动作），展示字段在父层预计算后传入；
- TodayView 2057 → ~1980 行；单测 +4（TodayComponents.spec.ts）；
- 视觉回归基线无变化（提取样式原样迁移），5/5 通过。

## 门禁快照（第三轮）

| 项 | 结果 |
|---|---|
| Vitest | 91 passed（+4） |
| vue-tsc + vite build | 通过 |
| Playwright | 30/30（含视觉回归 5 项） |
| pytest | 637 passed（第一轮已验证零 RuntimeWarning） |
| release-check-full | **14/14，绑定 `e13e2f6`，`worktree_dirty=False`**（`dist/release-check-0.4.0-alpha.1.md`） |

## 下一轮计划

- D4：系统页（Settings/Diagnostics/Backup）v2 层级收尾；Memory/Learning 迁移启动；
- D6：安装版验收（全新安装/覆盖升级/回滚）与 `0.4.0-alpha.2` 检查点文档、
  `0.4.0-alpha.2` 版本提升（五处 + lock 同步）后干净绑定 release-check。
