# 0.4.0-alpha.2 开发进展（2026-08-08 · 第二轮）

> 版本：`0.4.0-alpha.2`（开发中）
> 基线与入口：alpha.1 检查点（`995877a`，release-check 14/14 绑定 `2c6f497`）
> 计划：D4 业务工作区迁移 → D5 动效/无障碍/性能 → D6 视觉 QA/实机/检查点

## 第一轮（已提交 ea80395）

### 验收遗留（alpha.1 后用户反馈）
1. **Python "协程未等待" 运行时警告**：`agents/runtime.py` `_await_with_cancellation`
   先 `ensure_future` 调度再检查取消，消除取消窗口内协程未等待警告；
   `-W always::RuntimeWarning` 全量 637 passed、零警告。
2. **NavRailV2 4 处 RGBA 字面量**：收敛为 `--pa-rail-*` 组件令牌（legacy NavRail 同步）。

### alpha.2 契约切换：默认新 UI
- `DEFAULT_UI_V2 = true`；E2E 适配（legacy 动画测试固定 `?ui=v1`、共享行为测试壳无关化）；
  Playwright 25/25（默认壳 = v2）。

## 第二轮（本记录）

### D4 · 知识库页面组件化（首个领域迁移 `features/knowledge/`）
- 新增 `features/knowledge/DocListItem.vue`：文档行（选择/名称/状态徽标/元数据/操作区）
  从 KnowledgeView 整体拆分，状态映射与文案内聚，focus-visible 补齐；
- 空态/加载态/错误态改用 `PaEmptyState` / `PaErrorState`（加载骨架统一设计语言）；
- KnowledgeView 行数 767 → ~540；单测 +4（DocListItem.spec.ts）；
- E2E「知识库窄窗口状态与操作区无横向溢出」通过。

### D5 · 包体基线核对（相对 0.3.0-alpha.2 ≤15% 门槛）
| 资源 | 基线（0.3.0-alpha.2） | 当前 | 增长 |
|---|---:|---:|---:|
| index.js（raw） | 394.1 kB | 433.9 kB | +10.1% |
| index.js（gzip） | 118.6 kB | 131.1 kB | +10.6% |
| vendor.js（raw） | 405.2 kB | 414.0 kB | +2.2% |
| 合计 gzip | 224.8 kB | 238.7 kB | +6.2% |

结论：**在 15% 门槛内**，无需理由豁免。

### D6 · 视觉回归矩阵（toHaveScreenshot 断言基线）
- 新增 `e2e/visual-regression.spec.ts`：5 个断言基线
  （v2 Agent 1280/1440/1920、v2 今日 1440、v1 回退 1440）；
- 确定性措施与证据采集一致（冻结时钟 + reduced-motion + fonts.ready），
  `maxDiffPixelRatio: 0.02` 吸收字体栅格化噪声；基线生成后复验通过；
- 更新基线命令：`npx playwright test e2e/visual-regression.spec.ts --update-snapshots`。

## 门禁快照（第二轮）

| 项 | 结果 |
|---|---|
| Vitest | 87 passed（+4 DocListItem） |
| vue-tsc + vite build | 通过 |
| Playwright | 30/30（含 5 视觉回归断言） |
| pytest | 637 passed（第一轮已验证零 RuntimeWarning） |
| release-check | 待提交后干净重跑 |

## 下一轮计划

- D4：Today 视图组件化（概览/收件箱/提醒分区子组件）；
- D4：系统页（Settings/Diagnostics/Backup）v2 层级收尾；
- D6：安装版验收（全新安装/覆盖升级/回滚）与 `0.4.0-alpha.2` 检查点文档、
  干净绑定 release-check。
