# 0.4.0-alpha.2 内部检查点（D4 迁移收尾 · D6 视觉回归/证据）

> 版本：`0.4.0-alpha.2`
> 基线：`0.4.0-alpha.1`（检查点 `995877a`，release-check 14/14 绑定 `2c6f497`）
> 检查点日期：2026-08-08
> 范围：alpha.2 契约切换（默认新 UI）/ 验收遗留 / D4 业务页组件化 / D5 包体基线 / D6 视觉回归与证据
> 计划：[`v0.4.0-ui-ux-redesign-plan.md`](./v0.4.0-ui-ux-redesign-plan.md)

## 1. 门禁结果

| 项 | 结果 |
|---|---|
| Vitest | **95 passed**（alpha.1 83 → +12） |
| 前端类型检查 + 生产构建 | 通过 |
| Playwright | **30/30**（含 5 项 toHaveScreenshot 视觉回归断言） |
| pytest | 637 passed，`-W always::RuntimeWarning` 零警告 |
| Cargo check | 通过（`0.4.0-alpha.2`） |
| release-check-full | 见 §7（干净工作区绑定报告） |
| 版本号 | 七处统一 `0.4.0-alpha.2`，UTF-8 无 BOM |

## 2. alpha.2 契约切换

- `DEFAULT_UI_V2 = true`：默认进入新 UI；`?ui=v1` / `pa_ui_v2=0` 回退兼容壳；
- E2E 适配：legacy 动画模块回归测试固定 `?ui=v1`；共享行为测试壳无关化；
  默认壳（无参数）的 E2E 全部在 v2 上通过。

## 3. 验收遗留处置（alpha.1 反馈）

1. **Python 协程未等待警告**：`_await_with_cancellation` 先 `ensure_future` 调度再检查取消，
   任意路径下协程进入任务被 await/cancel；全量 `-W always::RuntimeWarning` 零警告。
2. **NavRailV2 RGBA**：4+4 处收敛为 `--pa-rail-*` 组件令牌。

## 4. D4 · 业务页组件化（三个领域完成迁移）

| 领域 | 拆分 | 效果 |
|---|---|---|
| `features/knowledge/` | `DocListItem`（文档行）+ PaEmptyState/PaErrorState | KnowledgeView 767 → ~540 行 |
| `features/today/` | `OverviewCards`（概览）+ `PriorityList`（优先事项） | TodayView 2057 → ~1980 行 |
| `features/memory/` | `MemoryRow`（列表项）+ `MemoryEditorForm`（表单） | MemoryWorkspace 716 → ~580 行 |

单测 +12（DocListItem 4 / Today 4 / Memory 4）；视觉回归基线无变化（样式原样迁移）。

## 5. D5 · 包体基线（相对 0.3.0-alpha.2）

| 资源 | 基线 | 当前 | 增长 |
|---|---:|---:|---:|
| index.js gzip | 118.6 kB | 131.8 kB | +11.1% |
| vendor.js gzip | 106.2 kB | 107.6 kB | +1.3% |
| 合计 gzip | 224.8 kB | 239.4 kB | **+6.5%**（≤15% 门槛内） |

## 6. D6 · 视觉回归与证据

- `e2e/visual-regression.spec.ts`：5 个 `toHaveScreenshot` 断言基线
  （v2 Agent 1280/1440/1920、今日 1440、v1 回退 1440），确定性采集 + maxDiffPixelRatio 0.02；
- 证据截图 `docs/evidence/v0.4.0-alpha.1/`（8 张，含 125%/150% 缩放）继续作为归档证据，
  `PA_EVIDENCE_REGEN=1` 可重生成；
- 更新基线：`npx playwright test e2e/visual-regression.spec.ts --update-snapshots`。

## 7. release-check 绑定

- 完整报告：`dist/release-check-0.4.0-alpha.2.md`（干净工作区，绑定 §1 提交，`worktree_dirty=False`）。

## 8. 已知问题与处置（P 级）

| 级别 | 问题 | 处置 |
|---|---|---|
| P2 | 兼容壳（v1）页面旧结构（TaskWorkspace 工作流可视化、Learning 分栏） | v1 壳为回退通道，维持现状；新页面统一走 v2 |
| P2 | 系统页（Settings/Diagnostics/Backup）v2 壳下与顶栏标题并列 | 页内标题保留（v1 需要），重复属已知 P2 |
| P2 | 深色主题未开放 | 按计划全部核心页面验收后单独评审 |
| P2 | 安装版覆盖升级/回滚实机验收未执行（本环境无安装器构建链） | 记录于已知限制，与 0.3.0-alpha.2 基线一致 |

## 9. 结论

alpha.2 契约切换、验收遗留、D4 三个领域迁移、D5 包体基线、D6 视觉回归与证据均完成；
门禁：95 单测 / 30 E2E / 637 pytest（零 RuntimeWarning）/ release-check 14/14 干净绑定。
后续：0.5.0 可信执行工作流可在新工作台（审批/活动/Diff/错误/产物组件）上直接复用。
