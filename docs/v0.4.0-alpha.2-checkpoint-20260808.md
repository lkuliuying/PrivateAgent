# 0.4.0-alpha.2 内部检查点（D4 全页面验收 · D5 性能证据 · D6 安装/视觉/证据包）

> 版本：`0.4.0-alpha.2`
> 基线：`0.4.0-alpha.1`（检查点 `995877a`）
> 检查点提交：`9127ef2`（绑定报告 `dist/release-check-0.4.0-alpha.2.md`，`worktree_dirty=False`，`installer_built=True`）
> 检查点日期：2026-08-08
> 范围：alpha.2 契约切换 / 验收遗留 / D4 业务页组件化与全页面验收 / D5 性能与资源清理 / D6 安装升级回滚、视觉回归、证据包
> 计划：[`v0.4.0-ui-ux-redesign-plan.md`](./v0.4.0-ui-ux-redesign-plan.md)

## 1. 门禁结果（最终）

| 项 | 结果 |
|---|---|
| Vitest | **95 passed** |
| 前端类型检查 + 生产构建 | 通过 |
| Playwright | **46/46**（30 基础 + 13 全页面验收 + 3 性能/资源清理） |
| pytest | 637 passed，`-W always::RuntimeWarning` 零警告 |
| Cargo check / test | 通过（`0.4.0-alpha.2`） |
| **release-check-full** | **14/14，绑定 `9127ef2`，`worktree_dirty=False`，`installer_built=True`，schema `0022`** |
| 版本号 | 八处统一 `0.4.0-alpha.2`（含 package-lock.json 两处），UTF-8 无 BOM |

## 2. alpha.2 契约切换

- `DEFAULT_UI_V2 = true`：默认进入新 UI；`?ui=v1` / `pa_ui_v2=0` 回退兼容壳；
- legacy 动画模块回归测试固定 `?ui=v1`；默认壳（无参数）E2E 全部在 v2 通过。

## 3. 验收遗留处置（alpha.1 反馈）

1. **Python 协程未等待警告**：`_await_with_cancellation` 先 `ensure_future` 调度再检查取消，零 RuntimeWarning。
2. **NavRailV2 RGBA**：8 处收敛为 `--pa-rail-*` 组件令牌。

## 4. D4 · 页面迁移与全页面验收

### 组件化（三个领域）
| 领域 | 拆分 | 效果 |
|---|---|---|
| `features/knowledge/` | `DocListItem` + PaEmptyState/PaErrorState | KnowledgeView 767 → ~540 行 |
| `features/today/` | `OverviewCards` + `PriorityList` | TodayView 2057 → ~1980 行 |
| `features/memory/` | `MemoryRow` + `MemoryEditorForm` | MemoryWorkspace 716 → ~580 行 |

### 全生产页面验收（`e2e/pages-smoke.spec.ts`，14/14）
12 个生产视图逐一验证：导航可达、主体渲染（空数据 → 空态）、1280 无横向溢出、键盘 Tab 可达；
另含**加载态**（延迟 API 响应断言"正在加载文档"→收敛空态）与后端断连**错误态**。
**完整功能对照表：`docs/v0.4.0-alpha.2-function-comparison.md`**（逐页功能清单 + 证据 + 状态覆盖）。

状态覆盖口径（诚实声明）：空态（逐页自动化）、加载态（延迟响应断言）、错误态（503 断连）、
权限态（审批卡/授权路径单测 + shell-v2 E2E）、成功态（组件单测）。真实业务功能逐项操作与
键盘深度焦点路径的逐页人工对照列为 **P2 补强**（对照表 §结论 已注明，不宣称完整人工验收）。

## 5. D5 · 性能与资源清理（`e2e/performance-resource.spec.ts`，3/3）

| 验收项 | 实测 |
|---|---|
| 页面切换 + 会话切换长任务 | **longtasks=0，p95=0ms，max=0ms**（50ms 门槛内） |
| 150 帧 SSE 长流压力 | 长任务=0，流收敛，输入区恢复 |
| 重复切换视图/会话后残留 | 定时器 4→4、timeout 3→3、**存活监听器 41→41（零增长）** |
| 包体基线（vs 0.3.0-alpha.2） | 合计 gzip +6.5%，≤15% 门槛内 |

## 6. D6 · 安装、升级、回滚与证据包

### 构建产物
- **NSIS 安装包**：`apps/desktop/src-tauri/target/release/bundle/nsis/PrivateAgent_0.4.0-alpha.2_x64-setup.exe`（98,688,199 B）
- **updater 签名**：`*.exe.sig` 已生成（`.tauri/personal-assistant.key`）；Authenticode 未签名（与 0.3.0 一致，`codesign-status-0.4.0-alpha.2.json`）
- **manifest**：`dist/alpha-0.4.0-alpha.2-manifest.json`（绑定 `9127ef2`，installer SHA-256 `08211f62…`，sidecar SHA-256 `41a0e2a6…`，与安装目录实测一致）
- **flags 快照**：`dist/alpha-0.4.0-alpha.2-flags.json`（19 项，与 0.3.0 同口径、同值）
- `dist/release-manifest-0.4.0-alpha.2.md`

### 实机演练（`dist/alpha-0.4.0-alpha.2-install/`，成对计数文件可审计）
| 演练 | 结果 |
|---|---|
| **覆盖升级** 0.3.0-alpha.2 → 0.4.0-alpha.2 | 成功：版本/二进制替换（`pre-upgrade-install.json`→`post-upgrade-install.json`）；**`pre-upgrade-dbcounts.json` / `post-upgrade-dbcounts.json` 成对保存且逐项一致**（10 表），appdata 保留 |
| **降版回滚** 0.4.0-alpha.2 → 0.3.0-alpha.2 | **真实执行**：版本回退（`rollback.json`：0.3.0-alpha.2，appsdesktop `9ED50BF0…`，与 alpha.2 `1752A367…` 不同）；`pre/post-downgrade-dbcounts.json` 成对且一致 |
| **关闭 ui_v2 回退** | 0.3.0 无 ui_v2 开关，降版后即为兼容 UI；`ui=v1` 回退由 E2E `shell-v2` 用例在默认壳与回退壳双侧持续验证 |
| **全新安装** | 成功：卸载保留数据（`data_retained_after_uninstall=true`）→ 重装 alpha.2 → 首次启动 smoke（应用存活 15s、sidecar 拉起） |
| 侧载 SHA 链 | 打包侧载 = 安装目录侧载 = manifest（`41a0e2a6…` 三处一致） |
| 可复跑回滚 | 0.3.0 重建安装包归档于 `dist/rollback-archive/`（原包被构建流水线清理，重建自 e6da3ee） |

## 7. 已知问题与处置（P 级）

| 级别 | 问题 | 处置 |
|---|---|---|
| P2 | v1 兼容壳旧结构（TaskWorkspace 可视化、Learning 分栏） | v1 为回退通道，维持现状 |
| P2 | 系统页页内标题与 v2 顶栏并列 | v1 需要页内标题，重复属已知 |
| P2 | 深色主题未开放 | 全部核心页面验收后单独评审 |
| P2 | 真实业务功能逐项操作与键盘深度焦点的人工对照未完整执行 | 对照表注明；加载/权限/错误/成功态已有自动化，人工补强列入 P2 计划 |
| P2 | Authenticode 未签名（SmartScreen 风险） | 与 0.3.0 一致，正式证书交后续阶段 |

## 8. 结论

**P0/P1 = 0**；验收阻断项全部处置：
1. package-lock.json 两处版本同步（八处统一）；
2. 全生产页面功能对照表 + 14 项逐页验收（含加载态/错误态断言）；
3. 50ms 长任务（实测 0）/SSE 长流/定时器与监听器零残留证据；
4. NSIS 安装包 + .sig + manifest + SHA-256 + flags 快照；
5. 全新安装、**覆盖升级（成对 pre/post 计数可审计）**、**真实降版回滚（0.4.0→0.3.0）**、
   关闭 ui_v2 回退与数据保留演练；
6. 干净提交重跑 release-check：**14/14、`installer_built=True`、`worktree_dirty=False`**。

`0.4.0-alpha.2` 满足计划书 D4–D6 退出条件，可进入 `0.5.0`（可信执行工作流可直接复用新工作台组件）。
