# 本机学习：可选原生专项手测

> **范围更新（2026-09-17）：E、F 已按用户要求取消。** 本页三项场景保留为研究流式输出、并发保护和幂等重试的可选练习，不再是“进入 E”的前置任务。当前基本完成标准见 [本机学习计划](s6-follow-up-development-plan.md#1-目标与完成边界)，无需准备虚拟机或发布材料。

记录日期：2026-09-17。按用户“不要控制我的电脑，告诉我怎么测试即可”的要求，全部点击、输入、审批和关闭由用户完成。助手只整理材料、运行非界面检查并读取本轮隔离证据，不再启动或操控窗口。

本页准备时绑定的候选为 `.run/s6-gap-closeout-20260917/build-01/candidate/`，不是旧 build-06。证据及启动器均在 `F:\Program\Agent\.run\s6-gap-closeout-20260917`。选择执行多个场景时须串行，不能同时启动。没有真实模型调用、账号或 API Key 要求。

## 1. 延迟专项：已准备的窗口入口

本轮目录：`F:\Program\Agent\.run\s6-gap-closeout-20260917\native-latency-e128e227`，合成编号 `32435733`。助手此前只启动了此隔离窗口；读取窗口的工具请求超时，没有点击、输入、批准或关闭。`operator-handoff.json` 单独记录此事实，原始准备清单保持不变。

### 配置一次本机替身

1. 在当前隔离窗口点击 **“使用自己的 API Key”**，进入本机模型设置。这里选择的是免登录入口；本次使用 Ollama 替身，不填写真实密钥、不登录真实账号。
2. 在“模型供应商”添加 `S6 本机替身`：供应商选 **Ollama（本地）**，Base URL 填 `http://127.0.0.1:6002`，API 格式选 **Ollama Chat（/api/chat）**。
3. 点击“获取模型”，选择 `s6-fixture`，点击“添加模型”并保存。
4. 回到 Coding，新建项目 `S6 延迟测试`，项目目录选：

   ```text
   F:\Program\Agent\.run\s6-gap-closeout-20260917\native-latency-e128e227\project
   ```

5. 新任务选择 `s6-fixture`，权限保留逐次确认。若上述任何一步失败，保留报错并反馈，先不要发送任务。

### 只提交一次

```text
PRE_E_LATENCY_32435733 请仅运行 python latency.py，报告实际输出与退出结果，不修改脚本，不重试命令。
```

1. 审批卡应仅运行该合成项目内的 `python latency.py`。核对后批准一次。
2. 立即展开底部 **“本机进程”** 中的命令输出；看到 `PIXEL_MEASUREMENT_READY` 后，保持这个窗口前台、无遮挡，并保持输出尾部可见约 45 秒。不要切换窗口或向上滚动。
3. 等待 `PIXEL_MEASUREMENT_FINISHED`，再等 5 秒，通过应用的“退出”正常退出。后台会自动处理本次采样；不要强退或删除目录。
4. 回复是否看到了 READY、连续输出、FINISHED，以及是否正常退出。有报错就描述报错；不需要提供任何凭据。

本轮会话从启动起限时 30 分钟。若窗口已关闭或长时间未测试后替身不可用，正常退出并反馈，由助手准备新目录；不要再次对已经产生 `launch.json` 的目录运行启动命令。

若选择执行这项严格测量，其通过仍需要 100 个有效前台样本，宿主收到输出到实际可见的 p95 ≤ 1000 ms，并报告 max 与超过 1 秒的数量；未做测量不记为通过，也不阻止开始本机学习。窗口显示“完成”不等于测量通过，助手会结合原始像素、时钟校准和宿主记录核对。被动采样仅针对该候选窗口，不向窗口发送输入。

## 2. 延迟场景结束并核对后：独立活动冲突

本轮目录：`F:\Program\Agent\.run\s6-gap-closeout-20260917\native-active-a139f43f`，编号 `84522337`。尚未启动。确认上一窗口已正常退出后，由用户在 PowerShell 执行：

```powershell
& 'F:/Program/Agent/.venv/Scripts/python.exe' -B 'F:/Program/Agent/.run/s6-gap-closeout-20260917/native_probe_manual.py' --launch 'F:/Program/Agent/.run/s6-gap-closeout-20260917/native-active-a139f43f'
```

保持 PowerShell 运行。按照第 1 节配置相同的本机供应商和模型，新项目目录改为本场景的 `project` 子目录。提前准备下面两条输入。

先提交并批准一次：

```text
PRE_E_ACTIVE_84522337 请仅运行 python active.py，报告实际输出与退出结果，不修改脚本，不重试命令。
```

看到 `PRE_E_ACTIVE_READY` 后，命令仍会等待 45 秒。在这段时间内点击“新建任务”，仍选**同一个项目和工作区**，提交另一条请求：

```text
PRE_E_CONFLICT_84522337 请只解释当前项目，不执行命令、不修改文件。
```

记录实际显示的活动冲突提示。预期第二请求被拒绝，没有第二次审批或执行；如果显示排队、启动了新运行或出现其他结果，如实反馈，不能自行当作通过。等第一条命令结束后正常退出。

助手随后核对独立 request ID、422 响应、宿主活动时间区间、唯一运行/审批/执行、`once.txt` 仅一行及租约释放。用户不需要修改数据库或脚本。

## 3. 活动冲突结束并核对后：创建响应丢失与显式重试

本轮目录：`F:\Program\Agent\.run\s6-gap-closeout-20260917\native-loss-c4a5c617`，编号 `48227258`。尚未启动。由用户执行：

```powershell
& 'F:/Program/Agent/.venv/Scripts/python.exe' -B 'F:/Program/Agent/.run/s6-gap-closeout-20260917/native_probe_manual.py' --launch 'F:/Program/Agent/.run/s6-gap-closeout-20260917/native-loss-c4a5c617'
```

保持 PowerShell 运行，按第 1 节配置本机供应商和模型，新项目目录改为本场景的 `project` 子目录。提交一次：

```text
PRE_E_LOSS_48227258 请仅运行 python loss.py，报告实际输出与退出结果，不修改脚本，不重试命令。
```

1. 本场景会故意丢弃第一次成功的创建返回。等待约 20 秒，预期显示 **“执行创建结果未知”**，不要改输入、另建任务或重复发送。
2. 只点击一次该提示中的 **“重试”**，等待重新关联同一个任务。若出现的是“后端拒绝”或没有重试入口，先反馈，不尝试绕过。
3. 出现 `python loss.py` 的审批后批准一次，等待 `PRE_E_COMMAND_FINISHED`，然后正常退出。
4. 回复第一次提示原文、是否只点击一次重试、审批出现次数和结束情况。

助手核对两次传输请求使用相同 `client_request_id` 并关联同一 run，第一次成功响应确实被丢弃，最终只有一次命令副作用。脚本输出、`once.txt` 与数据库分别核对；单看界面恢复不能证明去重正确。

## 4. 当前范围

这三项仅作为本机功能排查和 Agent 学习练习保留。E/F 已取消，[原 E 环境方案](s6-e-environment-preparation-20260917.md) 只作历史存档，用户无需执行。当前完整回归的 900 秒超时和历史异常仍有原始记录；具体功能按实际本机结果判断，不把取消阶段或未做专项检查记为通过。最新范围见 [本机学习使用检查第 36 节](s6-pre-e-readiness.md#36-本机学习范围调整ef-阶段取消)。
