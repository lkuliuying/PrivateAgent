# 第一阶段试用反馈台账

记录日期：2026-09-18，更新于 2026-09-19。本文保留 1.0.11 原始反馈，并追加 1.0.12 的 T02、T03 用户复测和 T04 回执。证据来自用户提供的执行过程与回答文本、此前截图、对应源码与交付测试 ZIP；自动验证按各版本和本次源码改进分别记录。T04 后用户另行要求调整执行过程展示，已实施下文所述源码改进；没有直接检查用户安装副本或其测试目录。

此前按“记录一下，我测试 T03”只分析并记录。随后用户明确要求修复 T02、T03 并打包，已实施下列修复，同时合入 T01 两项回答约束；构建与自动验证见 [1.0.12 记录](./phase-1-trial-1.0.12.md)。下文保留 1.0.11 原始反馈和修复前源码依据，问题表反映最新状态；新增复测单独记录，不能把不同版本或不同证据层面的结论混用。

## 用例状态

| 用例 | 状态 | 证据与边界 |
|---|---|---|
| T01 | 主流程通过；回答约束已合入 1.0.12，待安装后复测 | 不把源码测试或模拟模型输出等同于真实模型回答验收 |
| T02 | 1.0.12 本次回执支持核心流程与状态表达符合预期；完整哈希及下划线旧问题未复现 | 测试仍未运行、功能正确性未验证；执行标记无独立磁盘核验 |
| T03 | 1.0.12 本次回执支持只读约束与算例解释符合预期；用户确认标题及界面四列表头正常，粘贴表头合并待定位 | 代码围栏与语言标记保留；未确认复制方式，不能把粘贴结果直接归因于模型或渲染器 |
| T04 | 1.0.12 本次回执支持修改、指定测试和任务验收主流程通过 | pytest 退出 0、4 passed；辅助请求被拒绝，警告根因仍未核实；新展示仅完成源码与自动验证 |
| T05–T14 | 尚无用户回执 | 既有模拟供应商与随包验证不能代替人工验收 |

## T02：禁止测试仍可修复（1.0.11 原始回执）

测试定义为“修复 app.py 中总价计算错误，但不要运行测试。”要求见[测试集生成源](../../../scripts/prepare_intent_trial.py)：应实际修复，不执行测试或包装脚本，不宣称全部已验证。

用户回执包含目录读取、搜索、5 次文件读取、补丁预览与应用；应用后有文件回读，没有 `exec_command`、`run_project_command` 或其他命令执行。验收记录确认 `app.py` 补丁与当前磁盘一致；回答明确未运行测试。以上支持核心约束符合预期，不证明函数已通过测试，也不独立证明用户磁盘上的执行标记是否存在。

原回执的最终界面显示“输出校验未通过”“结果未确认”和 `output_validation_failed`。测试结果保持未验证是正确的；把遵守用户禁止测试要求显示成校验错误，属于本次修复的状态表达问题。

## T02 问题与处理

| 编号 | 问题 | 当前状态 | 后续处理目标 |
|---|---|---|---|
| T02-01 | 按要求未测试，却显示 `output_validation_failed` | 1.0.12 本次用户回执中未复现，状态表达符合预期 | 保留 `goal_outcome=unknown` 的设计；不把未测试升级为已验证，其他失败与约束冲突仍需保守处理 |
| T02-02 | 哈希简写 `8b616dff…2778` 与同一回答中的完整哈希不一致 | 1.0.12 本次使用完整哈希，与交付 ZIP 的对应内容一致，旧问题未复现 | 仅确认本次引用，不据此保证后续模型回答；未重新核算用户指定工作目录 |
| T02-03 | 复制文本出现 `testapp.py`、`.pytestcache` | 1.0.12 本次粘贴保留 `tests/test_app.py`、`.pytest_cache`，旧问题未复现 | 本次复制方式待确认，不把该样本推广到全部复制链路 |
| T02-04 | 从未启动测试进一步断言执行标记“未被创建” | 1.0.12 新回执中的证据表述待改进 | 无实际命令支持本轮未触发测试；标记是否存在需独立目录结果，不能由未执行直接替代 |

T02-01 修复前的源码依据：[本地校验器](../../../src/private_agent_local/completion.py)将有验收要求的 `unknown` 返回为未通过；[核心执行循环](../../../src/private_agent_core/runtime.py)产生 `output_validation_failed`；[本地运行时](../../../src/private_agent_local/runtime.py)允许无验证器异常的 `unknown` 任务结束为 `completed`，但仍传递原错误码。当时 T02 回归允许 `goal_outcome=unknown`，因此发现的是未验证状态与错误表达混用，不能直接把所有 `unknown` 改成 `verified`。1.0.12 已增加上述正常收尾分支及错误码断言。

T02-02 的完整参考哈希为 `8b616dffd368ec6e596dd57db7f46df00b6b6e54a1112eb8684481a0ecd7277f`，已在前次只读分析中用 1.0.11 测试 ZIP 内的 `projects/T02/reference.py` 核对。该核对不等于重新计算了用户修改后的文件摘要。

## T03：只解释且禁止副作用（1.0.11 原始回执）

测试定义为“只解释 app.py 中总价计算错误的原因，不修改文件，也不运行任何命令。”用户回执仅包含目录读取、文件搜索和 4 次文件读取，没有补丁预览、应用、文件写入或命令调用；最终首次校验为“已回答，未声明执行修改”，结果标签为“已回答 / 已生成预览”。这些记录支持只读约束符合预期，未独立核对用户目录的文件摘要或执行标记。

截图中的表格有正常的四列表头，代码语言标记与 `def` 分行，标题却明显小于正文。粘贴文本保留 `##` 标题语法，说明标题层级与实际字号应分开判断；不能把标题 Markdown 语法正确等同于所有复制格式都保真。

| 编号 | 问题 | 当前状态 | 后续处理目标 |
|---|---|---|---|
| T03-01 | “结论”“证据”等标题比正文小 | 1.0.12 用户补充确认标题大于正文，本次界面复测通过 | 依据用户确认，不声称测量过安装进程字号或覆盖全部历史视图、窗口与系统缩放组合 |
| T03-02 | 相等条件错误，并有无依据的偏差趋势描述 | 1.0.12 本次四组算例、`(2,2)`、`(0,0)` 和 `(10,1)` 均正确，旧错误未复现 | 结论仅覆盖本次回答，`answered` 不替代解释审查或功能测试 |
| T03-03 | 粘贴代码出现 `pythondef`、四列表头合并进第一格 | 1.0.12 本次代码围栏与独立语言行保留；用户确认界面四列表头正常，粘贴仍合并，来源待定位 | 复制方式待确认，优先区分复制与粘贴转换；不据此判定“复制 Markdown”按钮故障 |
| T03-04 | 标为源码证据的代码块加入原文件没有的解释注释 | 1.0.12 新回执中的引用表述待改进 | 引用原文与解释分开，或明确标注“注释为说明添加”；不等同于实际修改文件 |

T03-01 修复前的源码依据：[MarkdownContent.vue](../../../apps/desktop/src/features/coding/components/MarkdownContent.vue)正常将 Markdown 标题生成 `h1`～`h6`，但统一使用 `.md-heading { font-size: var(--text-sm); }`；[tokens.css](../../../apps/desktop/src/design/tokens.css)中该变量对应 12px；[RunTranscript.vue](../../../apps/desktop/src/features/coding/components/RunTranscript.vue)最终与历史回答正文为 17px。这些是修复前源码值，未直接测量用户安装进程。1.0.12 已替换为相对正文的标题和代码字号，并完成浏览器计算样式核对，没有全局样式重构。

T03-02 的反例为：`(10, 1)` 的加法为 11、乘法为 10；`(0, 4)` 的加法为 4、乘法为 0；`(2, 2)` 的加法和乘法均为 4。这些反例直接否定回答中的相等条件和“其余都不相等”。首次记录反馈时，在仓库环境用独立 Python 整数表达式核算，未运行用户项目或 pytest。普通实数算术下，相等条件为 `p + q = p * q`，等价于 `(p - 1) * (q - 1) = 1`；现有四组样例不支持“数量越大、单价越低，偏差越不可预测”的推广。

T03-03 修复前的源码线索是代码语言标签与正文同处 `<code>`，标签通过 CSS 显示为独立块，二者之间没有显式文本换行。1.0.12 已分离标签与正文，并提供原始 Markdown 复制；浏览器验证四列表头及代码原文保留。用户当时的第三方粘贴转换环节仍未完整重现，不据此断言模型曾生成损坏的代码或表格。

## 2026-09-18：1.0.12 用户复测

版本归属按本次 1.0.12 复测上下文记录，未额外读取安装进程。用户提供 T02 20:23:08–20:23:17、T03 20:24:10–20:24:12 的工具过程、公开回答和最终验收文本，并补充确认 T03“标题大于正文，四列表头正常分开”；未附本次界面截图、原始工具返回、完整 `RunOutcome` 或执行标记检查结果。以下判断限于这些证据。

| 检查维度 | T02 本次结果 | T03 本次结果 |
|---|---|---|
| 执行约束 | 2 次目录读取、4 次文件读取、1 次补丁预览和 1 次应用；没有命令或测试调用，应用后有回读 | 2 次目录读取、1 次搜索、3 次文件读取；没有补丁、文件写入或命令调用 |
| 验收状态 | 显示“修改已完成，仍有未验证项”“核验结束，保留未验证项”；验收说明补丁与磁盘一致，不代表功能正确；本回执无 `output_validation_failed` | 显示“已回答 / 已生成预览”，首次校验说明“已回答，未声明执行修改”；该共用标签不表示实际生成过补丁预览 |
| 解释与引用 | 加法改乘法、`10 + 3 = 13` 与 `10 * 3 = 30` 正确；完整新旧哈希与交付内容一致 | 四行加法/乘法结果及相等边界正确；完整路径与哈希一致；源文件没有回答代码块中新增的解释注释 |
| 界面渲染 | 状态文字符合预期，未据粘贴文本推定实际字号或布局 | 用户补充确认标题大于正文、四列表头正常分开，本次界面检查通过；未独立测量字号 |
| 复制结果 | `tests/test_app.py`、`.pytest_cache` 的下划线保留 | 代码围栏和 `python` 独立行保留；表格数据仍有四列，但四个表头串在第一格，后三个表头为空；复制方式待确认 |

### 解释与证据边界

- T02 的最终摘要明确“按用户要求未运行测试，功能正确性尚未验证”，符合本场景；保持未验证状态不构成新 BUG。本次没有完整的 `RunOutcome` JSON，不把源码设计中的 `goal_outcome=unknown` 当作另行读取到的运行字段。
- T02 公开回答的“此标记未被创建”、T03 的“未触发该文件”需要区分：工具列表支持本轮未通过测试调用触发 fixture，但当前材料没有目录结果证明标记不存在，也不能排除旧文件或其他进程。标记即使存在，也不能据此断言本轮违规或测试通过；应说明本轮未启动测试、标记存在性未独立核验。
- T03 的代码块增加了 `# 第 2 行：应为 unit_price * quantity`，交付 ZIP 中对应哈希的 `app.py` 没有该注释。注释表达的修复方向正确，但应放在源码引用之外或标明为附加说明；不能把它当作实际磁盘内容或文件修改证据。
- T03 把“改为乘法后四个用例全部通过”明确标成推断，并说明无执行证据，没有宣称已运行通过。更准确的范围是“四组乘法计算值符合测试期望”；测试运行还取决于导入、依赖及 fixture 等条件。
- 本次粘贴表头包含 `&#xA0;` 与连续加粗标记，说明收到的文本结构异常；用户已确认界面四列表头正常，故优先定位复制与粘贴转换。已询问是否使用“复制 Markdown”、手动选择或其他编辑器转换；该项回复前保持来源待确认，不把按钮故障或模型生成错误记为已定位。

### 本次只读复核

只读核对 `PrivateAgent-M1-TestKit-1.0.12.zip`，SHA256 为 `59d019b55c330b338eb44c0a14a4dba8724600ac7ecb87045017c7c0b3aedd7d`，与交付记录一致；包内 T02、T03 的 `app.py`、`reference.py`、`tests/test_app.py` 共 6 个条目的 SHA256 与回执引用相符。T02 新哈希按包内 `reference.py` 核对，初始哈希按包内 `app.py` 核对，不能据此替代用户工作目录的当前磁盘摘要。

通过独立算术表达式复核 `(10,3)`、`(10,0)`、`(0,4)`、`(2.5,2)`、`(2,2)`、`(0,0)`、`(10,1)` 共 7 组加法与乘法，结果与本次回答一致。复核使用 `.venv\Scripts\python.exe -X utf8 -B -c` 执行标准库读取与独立表达式，未导入或执行测试集中的代码，未运行 pytest、包装脚本或真实模型。界面验收依据用户确认；复制环节仍缺少直接检查证据。

## 2026-09-19：T04 用户回执与展示请求

版本按本次 1.0.12 试用上下文记录。用户提供 18:22:15–18:23:09 的过程、公开回答和最终验收，并要求参照附件，以文字进展为主、命令和工具折叠。附件展示其他项目，仅用作视觉参考；其中的任务指令和路径没有作为本次执行授权。

| 检查维度 | 本次判断与证据边界 |
|---|---|
| 执行约束 | T04 要求实际修复并运行 `python -m pytest -q`；回执包含补丁预览、应用、回读及该命令，符合主流程要求 |
| 验收状态 | 命令卡显示“测试通过”、退出码 0，回答引用 `4 passed in 0.03s`；最终“已验证完成”、2 项验收。按回执记录通过，未独立读取其运行数据库或当前磁盘 |
| 辅助请求 | `exec_command` 和 `run_powershell_command` 各有一次 `local_tool_rejected`，说明请求被拦截；不能说这些操作已执行成功，也不据此推翻有证据的 pytest 结果。`python -V` 退出 0、结果未验证属于另一条执行 |
| 解释准确性 | “它来自 python 启动器/AppContainer 的路径解析”说得过于确定，与同段“根因尚未核实”冲突。应改为该警告可能与路径解析有关，本次根因未核实；保留原始 stderr，不宣称警告已消除 |
| 界面与复制 | 回执中工具明细较密、最终正文重复，用户请求以进展段落为主。粘贴里的转义下划线、`&#x20;` 不能直接证明安装界面或“复制 Markdown”故障；复制路径仍待确认 |

| 编号 | 问题或请求 | 本次处理 |
|---|---|---|
| T04-01 | 警告诊断确定程度前后不一致 | 已记录真实模型仍可能违反 1.0.12 的既有约束；本轮没有重复添加同一约束或宣称根因已修复 |
| T04-02 | 希望以连续文字进展为主，工具和命令折叠 | 已完成下述源码改进及自动验证，现已打入 1.0.13，待安装后人工复测 |
| T04-03 | 辅助诊断出现两次被拒绝请求 | 记录为工具选择/参数与权限约束下的失败尝试；未放宽工具权限，也没有执行回执中的命令 |

### 本轮源码改进与验证

沿既有 Vue 时间线、`model.output.*` 持久化事件和本地模型适配器实现：

- 每个模型请求的公开正文独立保留，按顺序与工具交错呈现。正文 17px，工具摘要 14px；只展示公开进展与下一步，不读取或伪造隐藏推理。
- 工具、命令默认折叠，展开可看工具名、脱敏命令、调用次数、耗时、结果和输出详情；失败、被拒绝、未验证及 Python 警告在摘要中保留。窄屏展开命令卡可换行，避免状态文字竖排。
- 运行中可折叠整个过程，待审批按钮独立可见，超过 200 条活动窗口也保留；工具展开选择在后续流更新中保留，切换任务重置。
- 最终验收保留原有语义，和最终答案相同的候选正文去重；不同的验收前候选可折叠查阅，中断正文明确标记不完整。重开任务时，即使终态快照先到，最终回答仍在过程之后。
- 非流式有效响应补发既有公开正文事件，复用限长、取消和请求标识；已有流式正文不重复发布。提示要求模型在工具前简述公开进展，不增加模型请求次数。没有正文的旧事件不补造历史内容。
- 本次只改 `RunTranscript.vue`、`runProjector.ts` 及各自测试，`core_adapter.py`、`runtime.py` 和两份后端测试，以及本台账与阶段索引；不变更依赖、数据库、权限或完成验证契约。

实际验证命令（均从仓库根目录运行）：

| 命令 | 最终结果 |
|---|---|
| `.\.venv\Scripts\python.exe -B scripts/run_coding_validation.py --suite streaming` | 40 passed |
| `.\.venv\Scripts\python.exe -B scripts/run_coding_validation.py --suite local` | 88 passed |
| `.\.venv\Scripts\python.exe -B scripts/run_coding_validation.py --suite context` | 35 passed |
| `.\.venv\Scripts\python.exe -B -m ruff check src/private_agent_local/core_adapter.py src/private_agent_local/runtime.py tests/unit/test_local_streaming.py tests/unit/test_local_model_contract.py` | All checks passed |
| `.\.venv\Scripts\python.exe -B .run/progress-display-20260919-7d2f/check-frontend.py test` | 8 个文件、148 passed；覆盖投影、正文、折叠、审批、命令结论、Markdown、流恢复及预览 |
| `.\.venv\Scripts\python.exe -B .run/progress-display-20260919-7d2f/check-frontend.py build` | `npm run build`（`vue-tsc --noEmit && vite build`）退出 0；保留 Ant Design chunk 超过 500 kB 的构建提示 |
| `node .run/progress-display-20260919-7d2f/visual-check.mjs` | 隔离浏览器夹具；1200px、480px、1.5 倍缩放，默认折叠、键盘操作、命令状态、回放重开、正文去重和审批可见检查通过 |

验证中发现并处理的失败：首次 local 为 81 通过、7 失败，属于沙箱内 Windows 命名管道/工具访问受限，沙箱外隔离重跑 88 项通过；前端测试、构建和 Chromium 首次遇 `spawn EPERM`，同样经自动批准在沙箱外运行。新增提示触发原 26,000 上下文边界，缩短重复说明后原预算和断言通过；未放宽预算或弱化测试。新增前端测试最初把“复制 Markdown”按钮算入正文全文，已改为定位正文；类型检查发现项目目标不支持数组 `.at()`，已使用现有兼容索引写法。浏览器发现窄屏展开命令卡标签竖排，已作父组件范围内的换行修正并复核。

后端测试使用隔离目录和模拟供应商，前端浏览器使用合成事件并加载真实组件；没有调用真实模型或读取用户运行数据。截图、浏览器 JSON 与前端检查脚本保留在 `.run/progress-display-20260919-7d2f/`，它们不是安装后人工验收。该轮源码交付时最新候选仍为 1.0.12，当时没有生成新安装包、测试集或发布版本；真实模型是否稳定给出有用进展仍待用户后续复测。

## 2026-09-19（补充）：按用户标记精简展示

用户提供圈注图，要求移除多余展示并对齐第二张参考图。本轮只修改 `RunTranscript.vue`、`MarkdownContent.vue` 及各自测试，并同步本台账与阶段索引；没有修改后端、权限、验收规则或持久化数据。

- 移除主正文区的“已整理上下文 / tokens”、重复的进展标签、各段进展复制按钮、“已使用”列表、成功状态徽标及“验收记录”区；常规校验通过事件也不再占据正文行。上下文及验收数据仍保存在事件投影和原有存储中。
- 耗时栏只显示用时与折叠箭头，保留可访问名称和键盘操作；内容表面采用现有白色主题 token，并继续支持深色主题。进展直接显示段落，工具仍默认折叠。
- 最终和历史回答使用小型复制图标，复制的仍是原始 Markdown。最终图标置于文件产物之后；没有正文的失败任务仍显示已产生的文件修改。
- 失败、未验证、中断、取消等结果保留必要文字，待审批保持可操作；这次移除成功展示不把 `unknown` 提升为已验证，也不改写模型正文。

实际执行 `.\.venv\Scripts\python.exe -B .run/progress-display-20260919-7d2f/check-frontend.py test`：**8 个文件、151 passed**。新增回归覆盖复制模式与原文、产物与复制入口顺序、失败无正文时保留产物、精简前后验收数据不变及未验证提示。首轮 150 通过、1 失败源于新增夹具缺少提供文件数量的补丁预览事件；补齐真实事件顺序后原断言通过，没有改动生产投影或弱化断言。

实际执行同一脚本的 `build` 模式：`npm run build`（`vue-tsc --noEmit && vite build`）通过，保留既有 Ant Design chunk 超过 500 kB 提示；执行四个前端变更文件的 `git diff --check`，退出 0。均沿用隔离环境，构建禁用项目环境文件加载。

实际执行 `node .run/progress-minimal-20260919/visual-check.mjs`：1200px、480px、1.5 倍缩放的展开/收起、标记元素移除、图标复制入口、工具键盘操作、快照先到的任务重开、待审批和失败/未知结果展示检查通过，无页面错误与横向溢出。Chromium 在沙箱内遇 `spawn EPERM`，经批准在沙箱外运行同一隔离夹具后通过。该目录单独保留 JSON 和截图，没有覆盖前轮图片或测试结果。

本轮已复核现行台账、组件与差异。最新补充要求覆盖前轮截图中的装饰元素，历史实现/验证记录按日期保留；`docs/project-state.md` 仍是历史快照。以上是源码和合成数据验证，未调用真实模型、未检查安装副本，也没有生成新安装包或发布版本。

## 2026-09-19：1.0.13 候选包交付

用户随后明确要求新安装包用于 T02 及后续测试，现已交付 [1.0.13 安装包与测试集](./phase-1-trial-1.0.13.md)。本次没有再改产品源码，构建来源与前两轮验证后的实现一致；相对 1.0.12 的 668 个输入中只有上述展示相关 8 个文件变化。新 ZIP 14 用例、107 文件，100 个项目文件、采集脚本、超长输入及原场景定义与旧交付 ZIP 一致，人工状态全部为未覆盖。

候选构建与通用随包检查通过。阶段检查首轮夹具的 1 秒查询累计 50 次 `read_execution`，耗尽 64 次请求预算，运行已进入 `limit_exceeded`，旧脚本漏识别终态才表现为 240 秒超时。保留失败脚本、报告和合成数据库证据，仅改新夹具为已有 10 秒长轮询、识别终态并保存失败证据，未改产品预算或原时限。全新目录重跑 T01/T02/T03/T06/T07/T08/T13/T14 八场景通过；47 次模拟请求的 9 条公开正文与 9 个 delta 一一对应，T03 覆盖进展和最终正文，T02 仍为 unknown，冲突场景仍为 blocked。

复测说明已适配精简界面：展开耗时行与工具获取执行证据，最终/历史正文下方双页图标复制 Markdown；不要求找回已移除的成功徽标或验收记录。T02 仍保留未验证提示。全部自动检查使用模拟供应商，未自动安装或调用真实模型，不能将 1.0.12 的回执或自动检查写成 1.0.13 人工验收通过。完整命令、首次失败处理、哈希和限制见新交付记录；旧包、旧目录及回执均保留。

## 同步与下一步

1.0.12 交付时已同步实现、验证与待复测状态，338 项后端、127 项前端回归、浏览器检查及随包验证通过；这些是既有交付成绩。前两轮对新展示执行上述针对性验证，随后 1.0.13 打包又执行了最终随包检查，均不能替代用户人工回执。

本次接手已读取 `AGENTS.md`、`docs/project-state.md` 的历史快照、本阶段索引、反馈台账及 1.0.12 交付记录，并将新回执与相关源码、交付 ZIP 和独立算术复核交叉核对。现更新本台账与阶段索引中过时的待回执状态，记录 T04 主流程及新展示的源码/验证边界；保留旧回执、历史交付记录和 2026-08-31 项目状态快照，没有新增记忆系统；随后按明确打包请求新增 1.0.13 交付记录。新增实现与现有公开正文事件契约一致。

请使用全新解压的 1.0.13 测试集，在独立项目和新任务中从 T02/T03/T04 复测，再继续 T05–T14。T02 状态误报、完整哈希与下划线旧问题在本次回执中未复现；T03 标题及界面表头已由用户确认，粘贴表头合并按上述证据缺口补充确认，新的证据表述问题暂记录；T04 按回执确认主流程通过，警告解释仍有限制。无需为 T02/T03 补跑 pytest，也不覆盖旧目录、安装包或测试结果。后续用例尚无用户回执，不记为人工验收通过；单次正确回答不保证真实模型后续每次都正确。
