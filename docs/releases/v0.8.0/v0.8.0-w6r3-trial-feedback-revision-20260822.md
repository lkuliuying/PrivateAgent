# v0.8.0 W6-R3 · 第三轮试用反馈修订执行记录（2026-08-22）

> 阶段：W6-R3（RC 第三次重开，计划书 §7）
> 分支：`dev/v0.8.0`
> 口径：§4.5 最终信息架构 + §6.8 组件修订；W6-R2 代码与门禁仅作基线。
> 范围约束：仅实施 W6-R3 列出的今日页/主侧栏/Agent 页改动；未重写 Runtime、
> 审批或执行器；后端源码与公开契约零变更；未删除旧壳、旧 planner 或旧业务页面。

## 1. 代码范围（本轮修改文件）

主侧栏最近任务归位（任务 1）：

- `components/NavRailV2.vue`：最近任务容器从独立区移入「Agent 执行」分组
  正下方（DOM/视觉/键盘顺序连续，同一 `sessions` 数据源、稳定 id、排序、
  状态徽标与打开行为；新增「查看全部」入口）；旧位置重复实例删除。
- `models/viewRegistry.ts`：分组名「执行」→「Agent 执行」（§4.1 口径）。

Agent 标题区工作目录 / Git 分支（任务 2）：

- `features/agent/model/workspaceFacts.ts`（新增，纯函数）：7 种状态推导
  （正常分支 / detached / 非 Git / 路径缺失 / 归档 / 读取失败 / 无项目 /
  加载中）+ 长路径截断；只消费公开 workspace DTO，组件不执行 git。
- `features/agent/SessionHeader.vue`：标题区新增授权工作目录（截断 +
  tooltip + 复制，提示固定文案不含路径正文）与 Git 分支徽标；**移除顶部
  模型 chip 与上下文按钮**（模型配置下沉底部，上下文自动化）。
- `features/agent/AgentWorkspace.vue`：`generation id` 防迟到响应——切换
  会话先清空旧值再拉取，拒绝过期写入；监听随卸载清理。

会话栏完全收起（任务 4）：

- `ConversationList.vue`：新增滚动位置保存/恢复（收起不丢滚动位）。
- `AgentWorkspace.vue`：桌面收起 = 条件卸载（`v-if`）——零宽、无命中区、
  退出键盘顺序与读屏语义；可发现「展开会话」按钮；窄窗口为覆盖式抽屉，
  Escape/遮罩关闭并恢复焦点到触发按钮；选中态与草稿经父层状态保持。

逐轮过程与计划卡移除（任务 5）：

- `AgentWorkspace.vue`：删除独立 `AgentPlanV2` 大卡片与定位逻辑；计划/
  工具/审批/验证事实随所属 turn 呈现（既有投影链不变）。
- `TurnTranscript.vue`：新增「本轮未执行命令或工具」真实空态（不留白、
  不伪造动作）；公开状态标签逻辑不变（无摘要不虚构）。

底部控制重排（任务 6/7/8）：

- `TaskComposerV2.vue`：删除「知识检索」「生成记忆」按钮与相关事件/占位；
  工具栏新增 `toolbar-left`（权限下拉位）/`toolbar-right`（模型入口/用量位）
  插槽；控制行 `flex-wrap` 支持 1280/150%/长模型名有序换行；
  「PrivateAgent · 本地/远程」运行位置属性化。
- `AgentComposer.vue`：权限三档下拉（原知识检索位，§6.7 权限表真实语义，
  不支持档位禁用并说明）；底部模型/Provider 入口（当前模型 + 运行位置 +
  异常态：未配置/远程关闭/读取失败）；`ContextUsageMeter` 接入。
- `features/agent/ContextUsageMeter.vue`（新增）：呈现层组件，支持真实数值/阈值分级/压缩状态（供 v0.9.0 接入）；**第四轮决策后 v0.8.0 终态固定呈现「上下文用量不可用 + 压缩未就绪」**，不用前端状态补齐百分比（见 §7）。
- `features/agent/model/contextUsage.ts`（新增，纯函数）：用量推导只接受
  公开数值；缺失/非法即「不可用」；0–100 钳制；反向测试防字符数伪估算。

自动化能力与事实源（任务 9/10）：

- `features/agent/model/autoContext.ts`（新增）：知识检索（RAG）在
  `agent_runtime + rag_chat_runtime_enabled` 时自动执行（助手消息 sources
  为公开证据）；短期摘要/长期记忆候选如实「未就绪」（后端压缩组件默认
  关闭且无公开状态端点；候选自动生成功能未公开），不制造成功记录/伪记忆。
- `useLegacyChatSession.ts`：capability 原样缓存并暴露
  `runtimeCapabilities`；RAG 就绪时 `knowledge_base` 自动置真（移除按钮
  不静默关闭原启用能力）；`listActivities/listTrustedPaths` 非数组响应
  防御加固（W6-R2 发现的渲染崩溃隐患修复）。
- `App.vue`：v2 壳移除顶栏上下文切换入口；AgentWorkspace 接线
  （能力透传、审批/收件箱事件链保留、配置导航）。

## 2. 契约变化

- 后端：**零变更**（`git status -- src/ alembic/` 为空；未为 UI 新增或
  放宽任何安全契约；完全访问未引入）。
- 前端新增内部契约（不进网络协议）：`AgentWorkspaceFacts`、
  `ContextUsageFacts`（used/limit/percentage/compression）、
  `AutoContextCapabilities`；能力未就绪一律「不可用/未就绪」呈现。
- 已移除控件清单（反向测试守护，无隐藏 DOM/快捷键/空占位残留）：
  顶部模型 chip、顶部上下文按钮（含 AppShell 顶栏开关）、独立执行计划大卡、
  知识检索按钮、生成记忆按钮、推理强度禁用占位。

## 3. 测试数字（本轮重跑，未沿用 W6-R2）

| 门禁 | 结果 |
|---|---|
| vue-tsc | 0 错误 |
| vitest | **286/286**（46 文件；W6-R2 基线 265 → +21：新增 `workspaceFacts.spec` 7、`contextUsage.spec` 5、`autoContext.spec` 5；`AgentComposer.spec` 重写 7、`AgentWorkspace.spec` 适配至 12） |
| 生产构建 | 通过（3.86s；index chunk >500kB 为既有登记警告） |
| bundle:check | 4/4 OK（css 102.0% / index 104.6% / tauri 100.0% / vendor 98.6%，均 <110% 硬上限） |
| 全量 E2E | **107/107**（W6-R2 基线 98 → +9：新增 `agent-w6r3.spec.ts` 9 用例；既有 `agent-w6r2`(6)/`shell-v2`/`keyboard-focus` 按新布局适配） |
| a11y | Axe WCAG AA 零严重违规（新组件 3 处 `fg-faint→fg-subtle` 对比度修复，与 W5 同策略） |
| 视觉基线 | 无需重生成——NavRail/标题区/输入器变更均在 `maxDiffPixelRatio 0.03` 阈值内，11 张基线全绿（两轮复验稳定） |

新增/适配测试覆盖（§8 新增矩阵行）：

- 主侧栏最近任务归位：位置/唯一实例/稳定 id 打开/折叠隐藏（E2E ①）。
- Agent 会话完全收起：条件卸载断言（DOM 不存在）+ 主区占满 + 展开恢复
  选中/草稿（组件 + E2E ④）；窄窗口抽屉 Escape/焦点恢复（E2E ⑨）。
- 工作目录/Git 分支：7 状态纯函数单测 + 快速切换不残留旧值、detached
  真实呈现（E2E ②）。
- 底部控制重排：权限下拉三档禁用态/模型入口进设置/被移除按钮零残留
  （组件反向断言 + E2E ⑥）；长模型名布局换行（E2E ⑧ 1280/1440/1920）。
- 上下文用量：真实 4,096/8,192 → 50%；0%/阈值/100%/负数/超限钳制/缺失
  不可用全边界（组件）+ E2E ⑦ 无负数与 >100% 显示。
- 无独立计划卡的逐轮过程：无命令真实空态 ×2 轮不串轮（组件 + E2E ⑤）。
- 自动检索与自动记忆：能力推导单测（自动/未启用/不可用/未就绪/缺省不猜）。

## 4. 安装包证据（任务 11）

- 构建方式：`scripts/build-release.bat`（sidecar PyInstaller → MSVC →
  tauri build → NSIS；tauri build 内嵌 `npm run build` 重新编译前端，
  确认包含本阶段修改），构建日志 `data/logs/v080-w6r3-installer-build.log`。
- 安装包路径：`apps/desktop/src-tauri/target/release/bundle/nsis/PrivateAgent_0.7.0-rc.1_x64-setup.exe`
- SHA-256：`1C6E800CB8038A578A304150297FD1001DA8F40F5FCB56BF97DD5C3CBB038919`
  （第四轮决策合规修订后已由代码终态重建取代，见交接包第 17 条）
- 体积：100,232,901 字节（与 W6-R2 期产物 100,232,428 字节存在差异，
  证实包含本轮变更）
- 构建时间：2026-08-22 22:56（本机）
- updater 签名：`.exe.sig` 已生成（既有签名密钥）；Authenticode 未签
  （无证书，与历轮口径一致，SmartScreen 提示登记在案）。
- 覆盖安装与启动 smoke：见 §6 已知边界（与 v0.7.0/W6-R/W6-R2 门禁口径一致，
  登记为发布流水线项）。

## 5. 退出条件逐项核对（计划 §7 W6-R3，11 项）

1. ✅ 主侧栏「Agent 执行」下方直接可见最近任务，选择打开正确
   execution/session（稳定 id）；旧位置无重复实例（`rail-recent-tasks`
   唯一）；折叠与窄窗口随分组隐藏（display:none，不进 Tab/读屏）。
2. ✅ 顶部工作目录与 Git 分支与当前任务一致；快速切换会话不闪现/不残留
   旧值（generation 拒绝迟到响应 + 切换即清空，E2E 断言最终态与无误标）。
3. ✅ 顶部模型 chip、上下文按钮、独立执行计划大卡在 DOM/键盘/窄窗口均
   移除；无空白占位（反向断言 count=0）；计划/审批/验证事实随 turn 可追溯。
4. ✅ 会话栏收起后主区占满（宽度断言），不可点击/聚焦/读屏（条件卸载）；
   重新展开后选中会话、滚动位置与草稿保持。
5. ✅ 每条用户请求后有该 turn 公开过程区；无命令时明确空态；本轮信息不串
   入其他轮（turn 键稳定，空态计数断言）。
6. ✅ 权限下拉位于原知识检索位，三档与发送快照一致（唯一可选 = 真实
   语义）；模型入口进入真实设置页；知识检索/生成记忆按钮与快捷键不存在。
7. ✅ 上下文用量模块存在且边界完整（组件契约覆盖 0%/阈值/100%/不可用）；
   第四轮决策后 v0.8.0 终态按 §12.1 口径固定呈现「上下文用量不可用」（真实
   token 窗口/压缩状态为 v0.9.0 目标），不伪造百分比（见 §7）。
8. ⚠️→见 §6：自动压缩/检索/记忆的可追溯、去重、取消与隐私策略——知识
   检索已自动（RAG 随轮执行）；短期摘要与长期记忆候选的**后端自动化**尚
   未公开（压缩组件默认关闭、无公开状态端点、候选自动生成未实现），UI
   按契约如实「未就绪」，反向测试守护不伪造；完整闭环需后端增量，属
   §6.8 明示的「先补 typed contract、能力开关和反向测试；能力未就绪时
   UI 显示不可用」路径，移交 v0.9.0。
9. ✅ 1280/1440/1920、窄窗口（1100 抽屉）、150% 缩放（既有基线）、长路径、
   长分支名、长会话、长命令输出（既有 5,000 行用例）、长模型名下无横向
   溢出、遮挡、双滚动或不可达操作（E2E ⑧ + 既有套件全绿）。
10. ✅ 新 NSIS 安装包包含本阶段修改（tauri build 内嵌重新编译的前端产物，
    产物体积变化证实）；构建时间与哈希见 §4；覆盖安装与启动 smoke 按
    v0.7.0 以来门禁口径登记为发布流水线项（见 §6.2，不在验收机执行不可逆覆盖）。
11. ✅ 本阶段新增/更新测试全部通过（286/286 + 107/107），视觉对照无
    P0/P1/P2（基线阈值内全绿 + Axe 零严重违规）。

## 6. 已知边界与最小安全处置（移交 v0.9.0）

1. **自动压缩/摘要/记忆后端增量未就绪**：后端 `conversation_summarizer`
   组件存在（默认关闭、跨进程去重锁、隐私/远程 Provider 边界、会话隔离）
   但无公开状态端点/能力位；长期记忆候选仍为手动流。处置：UI 已按
   §6.8 兜底呈现「未就绪」并有反向测试；v0.9.0 需补公开能力位、状态端点、
   压缩事件投影入 turn 与失败重试，之后解除兜底态。影响：上下文阈值自动
   压缩暂不触发（上下文窗口由模型限制自然约束，超限报错可见，不静默丢弃）。
2. **覆盖安装 smoke 口径**：本机构建出安装包并记录哈希/时间；覆盖安装
   与启动 smoke 与 v0.7.0/W6-R/W6-R2 口径一致登记为发布流水线项（避免在
   验收机上对已安装实例做不可逆覆盖）；安装包路径/哈希见 §4，流水线复验
   后回填安装证据。
3. **ContextRail 入口随上下文自动化移除**：chat 会话的文件变更审阅面收敛
   到审批卡内嵌执行结果；ContextRail 组件保留（旧壳/后续版本），v0.9.0
   决定其去留。
4. 会话栏收起态的滚动位置恢复为列表级（选中/草稿由父层保持）；未读计数
   无后端事实源，维持现状。

## 7. 第四轮试用反馈决策合规修订（2026-08-22，本记录补充）

计划书新增「第四轮试用反馈决策」：不重开 RC；「替我批准/完全访问/上下文用量」
真实能力统一移交 v0.9.0；v0.8.0 保持诚实不可用，不用前端状态补齐。本轮修订：

- `AgentWorkspace.vue`：移除 run 快照 input_tokens + settings 上限拼接用量百分比的
  拉取链（该做法属于「前端补齐上下文用量」，与决策冲突）；`usageFacts` 固定为
  `contextUsageUnavailable()`，UI 呈现「上下文用量不可用 + 压缩未就绪」；
  `contextUsage.ts` typed contract 与纯函数推导测试保留，供 v0.9.0 接入。
- 「替我批准/完全访问」维持禁用 + 真实原因说明（既有实现已合规，本轮以
  vitest 7 用例 + E2E 断言复验，无伪造）。
- 项目/对话拆分、Asia/Shanghai 会话时间、输入器模型控件合并、execution 详情视图
  等其余第四轮反馈项均为计划 §12.1 冻结移交项，v0.8.0 不实施、不伪装。
- 修订后门禁（代码终态）：vue-tsc 0 错误 + vitest 286/286 + 全量 e2e 107/107
  （agent-w6r3 用量用例改为诚实不可用断言）+ build + bundle:check 4/4；
  代码终态安装包重建，哈希/时间见交接包第 17 条。
