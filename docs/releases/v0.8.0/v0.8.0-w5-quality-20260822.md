# PrivateAgent v0.8.0 W5：可访问性、性能与视觉 QA

> 完成日期：2026-08-22
> 分支：`dev/v0.8.0`
> 依据：[v0.8.0 开发计划 §7 W5](./v0.8.0-development-plan-20260820.md)、[W0 冻结 §2.2 断点矩阵](./v0.8.0-w0-ui-freeze-20260822.md)
> 前置：[W4 执行记录](./v0.8.0-w4-engineering-convergence-20260822.md)

## 1. 交付

| 项 | 内容 |
|---|---|
| 分段渲染 | `RunTranscript` 长列表分段渲染（默认最近 200 条，「显示更早」每次 +1000，切换 run 重置）——计划 §6.4 窗口化要求；diff 800 行/输出 600 行渲染上限维持 |
| 压力夹具 | `?coding-run-preview=stress`：4,998 帧 tool 循环 + 终态（≈5,000 活动记录） |
| 质量门禁 | 新增 `e2e/coding-quality.spec.ts`（7 用例）：Axe WCAG AA（首页 + 任务页富内容态：diff/命令输出/抽屉同场）、键盘可达（Tab 入侧栏/树 Enter/浮层 Esc/焦点）、5,000 记录压力（分段+展开+可交互）、重复 thread 切换 ×6 监听器/定时器净值无增长、1280/1440/1920 无横向溢出与页面级双滚动 |
| 对比度修复 | coding 组件信息性文本 `--color-fg-faint`（#8ba0a6，对 panel 2.63:1）→ `--color-fg-subtle`（#5d7179，≈4.9:1）共 30 处；CommandOutput `.stat.skip` 在 success-soft 底色上升级 `--color-fg-muted`（subtle 在该底色 4.47 差临） |
| 视觉矩阵 | `visual-regression.spec.ts` 扩展 6 例：coding 首页 1280/1440/1920 + 任务页（审批+命令输出）1440 + **1280@125% / 1440@150%**（deviceScaleFactor 独立 describe）；冻结时钟 + reducedMotion + fonts.ready 确定性，maxDiffPixelRatio 0.03；基线已生成并复验 |
| 探针语义 | 资源探针对齐 performance-resource 套件净值语义（Map<target,fn> + 断连目标过滤；裸计数会把框架重复注册误报为泄漏——实测教训记录） |

## 2. W5 退出条件核对（计划 §7）

| 退出条件 | 状态 |
|---|---|
| 无严重 a11y 违规 | ✅ Axe wcag2a/2aa serious+critical = 0（首页与富内容任务页；两处对比度修复后达成） |
| 定时器、监听器、SSE 和 AbortController 无增长 | ✅ 6 轮 thread 切换监听器/定时器净值无增长（quality spec）；SSE/AbortController 清理由 useRunStream 单测（迟到拒绝/detach/scope dispose）与 useRunStream.spec 断言 |
| 关键页面无横向溢出和双滚动 | ✅ 1280/1440/1920 断言 scrollWidth 与 body 纵向滚动 |

Reduced Motion：coding 组件全部动效（spin/transition/caret）带 `prefers-reduced-motion` 分支；全局由既有 animation.spec「reduced motion keeps the interface stable」覆盖。

## 3. 测试证据（2026-08-22，本机）

| 门禁 | 结果 |
|---|---|
| `vue-tsc --noEmit` | ✅ 0 错误 |
| `vitest run` | ✅ 37 文件 / 213 用例全绿 |
| `playwright test`（全量） | ✅ **88/88**（W5 新增 13：quality 7 + visual 6；含 125%/150% 缩放） |
| `npm run build` + `bundle:check` | ✅ index 161.4 kB（基线 100.1%，对比度 token 变更所致，阈值内） |

## 4. `0.8.0-beta.1` 门禁核对（计划 §7）

- 全量页面 smoke、视觉、性能、a11e 与安装版主链：
  smoke/视觉/性能/a11y ✅（88/88 全量）；**安装版主链**：桌面打包安装验证依赖
  tauri build/安装介质，属发布流水线环节（与 v0.7.0 门禁同口径：installer 证据在
  完整门禁清单单独核对），内部 beta 阶段登记为待流水线项，不阻断 beta.1 代码门禁。
- 新 UI 仍通过 flag 内部启用，不切正式默认 ✅（`isCodingWorkbench()` 默认关闭）。

## 5. 移交 W6

1. 安装版主链（tauri 打包 + 升级 + 回退演练）在 RC 门禁复核中执行。
2. dsf 截图在 CI 环境的字体栅格化差异若超阈值，按 0.4.0 rc.3 政策维持 retries:1
   与 0.03 差异阈值，不放宽语义。
