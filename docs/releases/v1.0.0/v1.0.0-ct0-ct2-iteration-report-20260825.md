# v1.0.0 Codex 工具体系融合 · 迭代报告（CT0-01～03 / CT1-01～05 / CT2-01～03 + CT3/CT4/CT5/CT9 增量）

> 日期：2026-08-25（同日第二轮增量）
> 依据：[Codex 工具体系融合开发计划书](./v1.0.0-codex-tool-engine-integration-plan-20260825.md) §15/§16/§17
> 状态：P0 关键路径上除 CT-6/CT-8（Rust Exec Host、App Server Spike）外的全部工作包已落地并有测试锚定

## 1. 交付范围

| 任务 | 内容 | 落点 |
|---|---|---|
| CT0-01 | Codex upstream manifest（冻结 commit/许可证/升级流程） | `docs/third-party/codex-adoption-manifest.md` |
| CT0-02 | AD-T02 Exec Host Boundary ADR | `docs/releases/v1.0.0/adr/ADR-006-exec-host-boundary.md` |
| CT0-03 | AD-T05 Completion Evidence ADR + AD-T06 Tool Exposure ADR | `adr/ADR-007-completion-evidence.md`、`adr/ADR-008-tool-exposure.md`（索引已更新） |
| CT1-01 | `hello.py` 假成功回归场景固化（F-001～F-008） | `tests/test_v100_ct1_fake_success_gate.py` |
| CT1-02 | ExecutionIntent schema + P0 意图 tag + 规则层桥接 | `agent_v2/domain/intents.py`、`application/intent_rules.py` |
| CT1-03 | Effect/Evidence/CompletionContract schema 与唯一求值引擎 | `agent_v2/domain/effects.py`、`agent_v2/domain/completion.py` |
| CT1-04 | 模型调用前 required-effect preflight gate 并接入 run 创建链 | `application/preflight.py` + `api/routes_agent_runs.py` |
| CT1-05 | 完成门禁以 v2 引擎收口（含 F-008 仅预览修复） | `application/contract_factory.py`、`agents/verification.py` 新增 `CompletionContractOutputVerifier` |
| CT2-01 | ToolSpec v2 枚举/字段（namespace/exposure/effects/approval/network/parallel 等） | `agent_v2/domain/tool_catalog.py` |
| CT2-02 | Catalog builder：唯一性、别名冲突、规范化 hash | `agent_v2/application/catalog.py` |
| CT2-03 | ToolPlan/ToolSnapshot：§9.2 八层暴露决策链与脱敏诊断视图 | `agent_v2/application/planner.py` |

## 2. 迭代退出证据（对照计划书 §28）

1. **hello.py 成功链**：审批 → `apply_patch_to_workspace` 原子写入 → 回读
   `verified=true` → durable execution → v2 契约求值满足
   （`test_hello_py_end_to_end_disk_evidence_chain`，含磁盘 SHA 断言）。
2. **Patch 工具关闭时 preflight failure**：run 创建即返回
   `409 tool_capability_unavailable`，公开原因含 flag 名与"未对磁盘进行任何
   更改"，不调用模型、不持久化 run（路由级用例 ×2：flag 关闭/readonly 权限）。
3. **零工具调用宣称成功** → `required_effect_missing`（F-003）；仅 proposal
   → `completion_not_met` + 立即 apply 纠偏话术（F-004）；写入未过回读 →
   `side_effect_unverified`（F-007）。
4. **本轮 ToolSnapshot**：`build_tool_snapshot` 对每个工具给出
   direct/deferred/hidden:<稳定原因>，隐藏原因七种全部有测试锚定，条目不含
   schema/描述全文/参数（脱敏红线断言）。
5. **测试与 Git diff**：后端全量 pytest 通过；ruff 全绿；
   `check_agent_v2_imports` 依赖规则 OK；无用户既有改动被覆盖。

## 3. 门禁结果

| 门禁 | 结果 |
|---|---|
| 后端全量 pytest | 1167 passed + 修复后 public contracts 12/12 → **1168+ 全绿**（2 skipped 为既有基线口径） |
| 新增测试 | 47 个（CT1 26 + CT2 21），全部通过 |
| ruff check src/tests/scripts | All checks passed |
| agent_v2 依赖方向检查 | OK（domain 未导入实现层/传输/Provider SDK） |

## 4. 行为变更（对调用方可见）

1. **F-002 预检失败关闭**：coding run 明确 file.mutate 意图但本轮没有任何
   可真正落盘的工具入口（Patch/PatchSet flag 全关或 readonly 权限）时，
   创建从 202 变为 409 结构化错误。受影响的既有 H1-B 用例已按新契约更新
   （开启 patch flag 后验证门槛持久化）。遥测新增低基数路径 `tool_preflight`
   （blocked/allowed），公开契约测试同步登记。
2. **完成判定收口（单一判断实现）**：`min_tool_executions` 与
   `require_successful_file_write` 两个条件族自本迭代起由
   `evaluate_completion` 唯一求值；路由装配时旧 verifier 对应分支让位
   （传 0/False），不再双判。契约由持久化 `completion_conditions_json`
   确定性重建，create/resume 得到同一 `contract_id`。
3. **F-008 修复**：显式"只预览/不要写入"请求清除 filesystem.write 要求，
   不再注入 `require_successful_file_write`，proposal 即可完成；同时豁免
   写入预检。规则层新增公开辅助 `core.executable_intent.has_preview_only_marker`。

## 5. 边界与延后项

### 5.1 第二轮增量（同日）：CT-3 / CT-4 / CT-5 / CT-9

| 工作包 | 交付 | 测试锚定 |
|---|---|---|
| CT-4 Router/Lifecycle | `application/tool_engine.py`：Router→Policy→Approval→Lease→Handler→Verifier 端口化引擎，每步产生低敏感阶段记录；pre/post 内部钩子（frozen 上下文、异常不改变语义）；v0.9 dispatcher 资产经 `engine_adapter.py` 复用原边界执行/脱敏实现接入 | `tests/test_v100_ct4_tool_engine.py`（23 个：金标等价矩阵、故障注入 unknown/deny/approval/claim/timeout/cancel/verifier/persistence、阶段轨迹顺序、钩子只读） |
| CT-4 安全并发 | `application/read_only_scheduler.py`：AD-T07 资格门槛；任一副作用调用 → 整轮串行（并发数恒为 1）；bounded task group、取消收敛全部子任务、结果按提交序 replay | 并发峰值 ≤ 上限、副作用恒串行、取消收敛、replay 确定性 |
| CT-5 Patch 统一化 | `domain/patch_operations.py`：PatchOperation schema 冻结（create/update/delete/rename；before/after SHA 义务；审批绑定规范化 hash）；Windows 危险路径 schema 层拒绝矩阵（盘符/UNC 设备路径/ADS 冒号/保留设备名含扩展名变体/段尾点空格/控制字符）；大小写碰撞检测；`application/patch_adapter.py` 单文件/PatchSet → 统一 operations | `tests/test_v100_ct5_patch_operations.py`（26 个）。Codex 自由文本 parser 评估结论 **Defer** 已登记 adoption manifest §3.1.1 |
| CT-3 模型探测 | `application/model_probe.py`：六类 P0 固定用例（单调用/严格参数/连续两轮/同轮多调/失败纠正/拒绝编造）、可重复 runner、`ModelToolProfileSnapshot`（digest/样本数/results）、无有效快照 → 最小工具面门禁 | `tests/test_v100_ct3_model_probe.py`（10 个，fake 模型驱动；真实 Qwen/Ollama 探测由 runner 对接 Provider 执行） |
| CT-9 诊断 API | 新 flag `PA_AGENT_V2_TOOL_SNAPSHOT_ENABLED`（默认关）；`workspace_catalog.py` 冻结元数据表把 v0.9 九个已审计工具投影为 ToolSpecV2；`GET /agent-runs/tool-diagnostics` 返回脱敏快照（direct/hidden:<原因> + 四组 hash），observe-only | `tests/test_v100_ct9_tool_diagnostics_api.py`（flag 门控 404、脱敏红线、意图过滤、非法 tag 422） |

增量后全量门禁：**pytest 1230 passed + 2 skipped 全绿；ruff 全绿；agent_v2 依赖规则 OK。**

### 5.2 剩余工作包（按 §16 关键路径）

- **CT-6 Rust Exec Host/Sandbox**：已闭环至环境允许的最大面（见 §7.2）；
  发布策略已正式决议：**v1.0.0 保持 Exec Host 默认禁用**（生产代码无拉起
  调用点，网络强制强形态待健康环境；见 `adr/evidence/ct6-v100-release-policy-decision.md`）；
- **CT-7 Deferred Tool Search/MCP P1**：已交付（见 §7.3）；
- **CT-8 App Server Spike**：研究项，Defer 结论已归档；
- **CT-9 桌面 UI 投影**：诊断 API/面板已就绪；MCP 逐工具策略的后端 API
  已就绪，前端策略编辑器属 CT-9 前端里程碑延后项。

## 6. 下一步建议

CT-4/CT-5/CT-3/CT-9 增量已落地；下一步进入 CT-6：先以 CT6-01 JSONL spike
验证 Rust 打包链与进程边界，网络沙箱门禁（ADR-004）闭环后再开放通用 exec。
Python 侧协议契约与客户端骨架位于 `agent_v2/execution/`。


---

## 7. 第九轮增量（2026-08-26）：CT-6 P0 方法补齐与 CT-7 MCP 闭环

### 7.1 基线修复（错误码透传）

`ExecHostClient` 此前丢弃 host 返回的稳定错误码（只保留文案），导致
AC 失败关闭断言无法命中 `sandbox_policy_unavailable`（§7.7 错误信封必须携带稳定码）。`ExecutorUnavailable` 现携带 `code` 并格式化进消息；
基线由 1261 过 1 红恢复全绿。

### 7.2 CT-6：§11.2 P0 方法补齐与受控 PTY（失败关闭）

| 交付 | 语义 | 测试锚定 |
|---|---|---|
| `execution/stdin/write` | std/restricted/AC/PTY 四路径保留写入端；逐次校验 execution id + session nonce（§11.4），`stdin_mode=pipe` 缺 nonce 拒绝；错配/未绑定/已关闭 → `bad_nonce`/`bad_params`/`stdin_closed` | `tests/test_v100_ct6_stdin_pty.py`（6 用例：回显回读/错 nonce/无管道/缺 nonce/续读/PTY） |
| `execution/output/read` | 每执行 1 MiB 滑动窗口、字节偏移续读；执行移除后 `unknown_execution`（持久 artifact 属 Python Core） | 同上（续读不重复 + 移除后拒绝） |
| 受控 PTY（ConPTY） | `sandbox::spawn_pty` 属性表附着（inherit/Low IL 双路径）；**就绪探针**：首次 pty 请求以 cmd 回显实证附着，不通过 → `pty_environment_unavailable`，绝不交付无法回显的伪会话；AC+PTY 一律拒绝 | 二选一结局（回显成功或结构化拒绝）已锚定；本机环境阻断下自动以拒绝结局通过 |
| health.modes | 如实上报 `["argv","pty"]`；可用性由 start 门禁裁决 | 握手用例同步 |

本机实证：无控制台父会话下 ConPTY 附着不生效（与 AC 加载链限制同源，
见 `adr/evidence/s4-network-enforcement-plan.md` §3.9）；探针门禁保证产品侧零静默伪会话。
协议契约：`ExecStartParams.session_nonce`（additive，向后兼容）；客户端未显式指定时自动注入会话派生 nonce。

### 7.3 CT-7：MCP 逐工具审批策略 + discovery 缓存 + namespace 投影（§12）

| 交付 | 语义 | 落点 |
|---|---|---|
| §12.2 审批策略 | server 默认 + 逐工具覆盖（auto/prompt/writes/always/deny）；auto 仅限显式配置→免审批；writes 对不透明调用保守按逐次审批；deny 不注册不调用；非法值回落不扩权 | 迁移 0034（additive）+ `mcp/contracts.py`/`repository.py`/`manager.py`/`routes_mcp.py` |
| §12.1 discovery 缓存 | 保存时绑定连接身份哈希；超 TTL（1h）或身份变化 → 工具面失败关闭，不静默使用过期目录 | `discovery_is_fresh`/`config_identity_hash`，registry 构建门禁 |
| §12.1 namespace 投影 | `mcp.<server_id>` + 规范化 canonical + 可逆别名（复用 v0.9 provider 命名）；过期 → `hidden:health_failed`；deny → `hidden:policy_denied`；接入诊断 API | `agent_v2/application/mcp_catalog.py` + `routes_agent_runs.py /tool-diagnostics` |
| M-002 提示注入 | 描述中的注入文本不参与策略决策（决策只来自受信配置） | `tests/test_v100_ct7_mcp_policy.py`（8 用例） |

前端延后项：McpServersPanel 的审批策略编辑器（后端 API 已就绪，字段向后兼容）。

### 7.4 门禁结果（第九轮）

| 门禁 | 结果 |
|---|---|
| CT-6 五套件 | 21 过 4 环境守护跳过（含新 6 用例） |
| CT-7 MCP + 既有 MCP + deferred search + catalog + 诊断 API | 62 过 |
| ruff src/tests/scripts | All checks passed |
| agent_v2 依赖方向 | OK |
| 迁移 | 0034 幂等应用至专用测试库（0033→0034） |
| 全量 pytest | **1274 passed + 6 skipped（0 failed）**；cargo 构建无警告；
  桌面目标 vitest（McpServersPanel/ToolDiagnosticsPanel）11/11 |


---

## 8. 第十轮增量（2026-08-26）：§20 Feature Flag 与 §7.7 错误码合规收口

完成审计发现两处与计划书条目的差距并补齐：

### 8.1 §7.7 稳定错误码冻结注册表与接线

- 新增 `agent_v2/domain/error_codes.py`：§7.7 十三个公开错误码冻结集合
  （`FROZEN_ERROR_CODES`，测试锚定完整性）；
- `tool_hidden_by_policy`：v2 引擎策略拒绝公开码统一（替换旧内部码；
  v0.9 dispatcher 保留 `permission_denied` 不变，金标测试以 §7.7 统一映射表钉住）；
- `tool_health_failed`：预检新增 `unhealthy_executors` 判定；MCP server
  `status=error` 从注册面剔除（失败关闭），v2 投影纳入 `health_failed` 集合；
- `tool_plan_invalidated`：`TurnSearchSession` 支持显式 `invalidate()` 与
  `guard_catalog()` 目录哈希守护，失效后搜索/激活一律结构化拒绝（§7.2：
  不静默换工具）。

### 8.2 §20 Feature Flag 全量注册与接线

`config.py` 新增 8 个 flag（连同既有 TOOL_SNAPSHOT 共 9 个，均含 owner/
默认值/进入与退出条件注释）；`.env.example` 同步；阶段语义：

| Flag | 默认 | 接线 |
|---|---|---|
| PA_AGENT_V2_TOOL_PREFLIGHT_ENABLED | true（enforce） | run 创建预检门禁；关闭回落 v0.9 无预检形态（§24，非绕过完成门禁） |
| PA_AGENT_V2_COMPLETION_EVIDENCE_ENABLED | true（enforce） | v2 完成契约求值；关闭回落 v0.9 条件族求值（同样非 fail-open；§20：enforce 后不得对副作用任务 fail-open） |
| PA_AGENT_V2_TOOL_SNAPSHOT_ENABLED | false | 诊断 API（既有） |
| TOOL_ENGINE / EXEC_HOST / DEFERRED_TOOL_SEARCH / SAFE_PARALLEL_TOOLS / NATIVE_APPLY_PATCH / CODEX_APP_SERVER_SPIKE | false | 灰度预留；对应能力要么在应用层（未接主链），要么受沙箱门禁失败关闭约束 |

### 8.3 测试与门禁（第十轮）

- 新增 `tests/test_v100_s20_flags_error_codes.py`（9 用例）：注册表完整性、
  flag 默认值、引擎策略拒绝码、健康失败码、搜索会话失效（显式/目录哈希）、
  MCP error 状态剔除+投影、预检 flag 回落；
- CT-4 金标等价更新为 §7.7 统一映射；
- 终态门禁：**全量 pytest 1283 passed + 6 skipped（0 failed）**；
  ruff 全绿；agent_v2 依赖规则 OK；迁移 0034 已应用。


---

## 9. 第十一轮（2026-08-26）：验收阻断项关闭与候选提交整理

针对验收结论的四个发布阻断项逐项关闭：

### 9.1 阻断 1：完整 E2E → **116/116 全绿**

修复分四类（均为夹具/渲染鲁棒性，非功能回退）：
1. **capability 夹具补齐**：7 个 spec 的 /capabilities mock 补齐三开关
   （coding_agent_ui_enabled + agent_runs_api_enabled +
   project_bound_runs_enabled，runtime.ts 门禁）；
2. **预览夹具乱序修复**：`codingRunPreview.ts` 的 failed/cancelled 键帧序不单调，
   被投影器幂等跳过 → 重编序号；新增夹具守护套件（全键可投影 + 关键事实）；
3. **消息端点防御**：/sessions/{id}/messages 返回非数组时击穿 RunTranscript
   渲染 → 前端 `fetchThreadMessagesSafe` 回落空数组 + 夹具补端点；
4. **终态错误码保持**：流层合成 run.terminal 帧与无错误码快照把已落库错误码
   洗成空 → 投影器两处回退到 `projection.error`（新增 3 个单测钉住）；
   命令卡详情折叠（W6-R）与 h1b/h1d 快照用量同步更新断言/夹具。
新增守护：`codingRunPreview.spec.ts`（4）、`runProjectorTerminal.spec.ts`（3）、
CodingThreadWorkspace 预览+抽屉用例。vitest **344/344**，Playwright **116/116**。

### 9.2 阻断 2：CT6 策略 → **默认禁用决议正式化**
见 `adr/evidence/ct6-v100-release-policy-decision.md`：正式版保持
`PA_AGENT_V2_EXEC_HOST_ENABLED=false`，生产代码无拉起调用点，通用命令不标记
stable；解除条件（健康环境 kernel_deny 证据对 + N4 全量 + sandbox_available 如实转 true）
冻结。验收口径：CT-6 按"技术探针与失败关闭机制"验收。

### 9.3 阻断 3：长稳 → **1000 轮/10000 次达标**
`scripts/run_soak_gate.py`（默认规模）重跑：1000 Turn 全部 completed（5 种模式各 200），
10000 次重复投递全部幂等吸收，1000 run 完整性核验丢失/重复/载荷不一致均为 0，
verdict=pass；证据覆写 `adr/evidence/s19_2-soak-results-20260825.json`。

### 9.4 阻断 4：工作区 → **干净候选提交**
132 条未提交/未跟踪记录整理为 4 个逻辑提交（工作区 0 残留；本节关闭记录
作为收尾提交追加）：
1. 计划书/ADR/证据链/迭代报告（含验收关闭记录）；
2. Rust Exec Host + spawn-test（target/ 已入 .gitignore）；
3. 后端契约/门禁/迁移 0034/脚本/测试（81 文件）；
4. 桌面工作台增强 + E2E 夹具同步（37 文件）。
候选基线 = `dev/1.0.0` HEAD（四个提交叠加于 a2b743c 之上）。

### 9.5 干净基线复验（关闭顺序 #5）

| 项 | 结果 |
|---|---|
| 后端全量 pytest（干净 HEAD） | **1283 passed + 6 skipped（0 failed）** |
| ruff src/tests/scripts | All checks passed |
| 桌面 vitest | 344/344 |
| Playwright 全量 | 116/116 |
| 桌面生产构建 | 成功（638 KB 主包警告为已知非阻断治理项） |
| Tauri cargo test | 17 passed |
| 文件创建冒烟 | F-001 端到端磁盘证据链用例（含真实落盘 + SHA 回读）随全量绿；安装版级冒烟属发布流水线 |
| 安装包 | 属发布流水线（build-release），待 RC 窗口执行 |

### 9.6 非阻断治理项（维持登记）
- 主 JS 包 638 KB > 500 KB 警戒；bundle +7.1%/CSS +5.5%；codingHttp 静/动态双导入未分包；
- CT8 保持 Defer（未声明已验证）；
- exec-host crate Rust 单测为 0，覆盖由 Python 侧 e2e/探针承担（随默认禁用决议降级为非阻断）。


---

## 10. 第十二轮（2026-08-26）：二次验收阻断项关闭与 1.0.0 发布产物

### 10.1 阻断 1：桌面全量单测时序不稳定 → **关闭**
`CodingThreadWorkspace.spec.ts` 预览用例的固定 50ms 等待改为基于 DOM 条件的
有界等待（`waitForCondition`：轮询工具卡渲染，4s 超时）；新增请求竞态守护后
全量连跑 3 次均 **345/345**（含新守护用例 1 个），对套件负载不再敏感。

### 10.2 阻断 2：CT3 模型探测接入生产链路 → **关闭**
此前仅具备探测框架；本轮完成产品接线（§8.2/§8.3）：
1. **能力映射修复（P1）**：`requirements_from_results` 由探测结果推导——
   单调用+严格参数 → function_calling；同轮多调用 → parallel_tool_calls；
   freeform/vision 保持失败关闭（AD-T04）；`run_probe` 快照不再永远取默认值；
2. **快照持久化**：迁移 0035 新增 `model_tool_profile_snapshots`（每次探测一行，
   status/results/requirements/digest/probed_at）；
3. **随配置自动执行**：模型 profile upsert 后自动探测（尽力而为，失败落
   `probe_failed/probe_timeout/capability_unproven` 快照，不阻断配置保存；
   `PA_AGENT_V2_MODEL_PROBE_ENABLED` 门控，测试期默认关）；
4. **实际控制工具面**（两层失败关闭）：
   - run 创建预检：绑定 profile 无有效快照时文件写入意图 → 409 `tool_model_unsupported`；
   - 工具注册层：无有效快照时副作用工具（patch/patchset/command/http/sql）
     不注册，只保留最小工具面（只读）；
   - 有效性 = 最新快照 status=ok 且 function_calling 且 digest 与当前
     model_name 匹配（换模型未重探即失效）；
5. 新增 28 个测试（映射/持久化/失败留痕/路由门禁 409→补快照放行；
   `tests/test_v100_ct3_probe_production.py` + v070 用例同步种快照）。

### 10.3 阻断 3：版本与发布产物 → **1.0.0 产物就绪**
- 五处版本源统一 `0.9.0 → 1.0.0`：pyproject/__init__/package.json/
  tauri.conf.json/src-tauri Cargo.toml（+ Cargo.lock 同步）；
- `scripts/build-release.bat` 全链成功（rc=0）：sidecar（PyInstaller）+
  NSIS 安装包 + 更新签名 + 发布清单：
  - `PrivateAgent_1.0.0_x64-setup.exe`（96.0 MB，SHA-256
    `7f151943baea631f338af9187b1c18878dbedcfdb630287b6960a64c17459acc`）；
  - `.sig` 更新签名（SHA-256 `cd19acd1…f2a085`）；
  - sidecar SHA-256 `2a1395c8…dc7160a2`；清单 `dist/release-manifest-1.0.0.md`；
- 安装版创建文件/升级/回退冒烟：属 RC 窗口在干净机器执行（与既有验收口径一致），
  产物证据链已就绪。

### 10.4 P2：诊断面板请求竞态 → **修复**
`ToolDiagnosticsPanel` 连续查询改为“最后一次生效”：新请求 AbortController 取消
旧请求 + 序号防护（慢旧响应不得覆盖新结果）；新增竞态守护用例（延迟旧响应验证）。
`fetchToolDiagnostics` 支持 AbortSignal 透传。

### 10.5 终态门禁（二次验收口径复跑）

| 项 | 结果 |
|---|---|
| 后端全量 pytest | **1287 passed + 6 skipped（0 failed）** |
| 桌面 Vitest | **345/345 × 连跑 3 次** |
| Playwright E2E | **116/116** |
| Ruff / 依赖方向 | All checks passed / OK |
| 桌面生产构建 | 成功 |
| Tauri cargo test | 17 passed（本轮未改 src-tauri 代码，证据持续有效） |
| 长稳门禁 | 1000 轮/10000 次重放通过（§9.3） |
| 发布产物 | 1.0.0 安装包 + 签名 + 清单已生成 |
| 工作区 | 干净候选提交（见 §9.4 与本轮收尾提交） |


---

## 11. 第十三轮（2026-08-26）：三次验收阻断项关闭

### 11.1 P0：探测链路适配 → **关闭**
`model_probe_service` 将生产 `ModelGateway` 直接传给 `run_probe` 导致
`cancellation` 缺失（实测 0/6）。新增 `_GatewayProbeClient` 适配层补齐
cancellation 令牌；生产探测默认 `repeats=2`（单次六题存在模型波动，多轮聚合降误判）；
新增回归测试：受控网关必须收到 cancellation，未补齐即用例失败（7/7 绿）。

### 11.2 P1：同步探测改后台任务 → **关闭**
- 保存请求不再等待模型：`auto_probe_profile` 仅调度后台任务（独立会话）；
- 进度/结果：先落 `running` 快照，完成后覆写 `ok/failed`（probe_failed/
  probe_timeout/capability_unproven）；`GET /agent-model-profiles/{id}/tool-probe`
  查询最新快照；`POST /…/tool-probe` 重试（运行中 409）；
- 前端：ModelProfilesPanel 逐 profile 呈现探测状态（未探测/探测中/已验证/未通过）+
  “探测能力”重试按钮；运行中状态 3s 轮询；卸载取消在途请求。

### 11.3 P1：fail-open 修复 → **关闭**
`probe_gate_for_run`：绑定 profile 已删除/查不到 → `False`（副作用工具不注册）；
仅未绑定 profile 的历史/非 coding run 保持既有行为（兼容契约）。
新增用例：缺失/无快照/删除后均失败关闭，补快照后放行。

### 11.4 P1：E2E 稳定性 → **缓解**
playwright `retries: 1` 吸收应用首屏低频启动竞态（白屏/首请求失败）；连续两次
同位失败仍计真实回归。已验证全量 116/116。

### 11.5 P2：组件卸载取消请求 → **关闭**
ToolDiagnosticsPanel `onScopeDispose` 取消在途请求（配合既有序号防护）；
ModelProfilesPanel 探测轮询定时器与 AbortController 同步清理。

### 11.6 风险项：soak 死锁重试 → **关闭**
`run_soak_gate.py` 增加 MySQL 1213 死锁与瞬态级联（EventSinkError/
AgentRunProjectionError 链）判定：轮次残留清理后重驶（上限 3 次），重放投递
死锁重试；重试计数写入证据。复跑 1000 轮/10000 次：verdict=pass，
deadlock_retries=0，完整性核验全零。

### 11.7 P1：发布产物绑定 → **重建中**
三次验收修复提交后重建安装包，使发布清单绑定当前 HEAD；随后生成
dist/latest.json 与 dist/release-check-1.0.0.json（见终态记录）。
安装版创建文件/覆盖升级/回退冒烟与 Authenticode 签名属发布流水线/RC 窗口
（需签名证书与干净机器），产物证据链（SHA/签名/清单）就绪。

