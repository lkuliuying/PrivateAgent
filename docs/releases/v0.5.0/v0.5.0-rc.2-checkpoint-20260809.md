# PrivateAgent v0.5.0-rc.2 检查点（验收修复）

> 路线更新（2026-08-20）：本文件记录当时的 RC2 事实；其中 v0.5 自然日观察安排已被 [观察期顺延决策](../observation-policy-20260820.md) 取代，最终观察改在 `v1.0.0-rc.1` 全部开发完成后执行。

> 制定日期：2026-08-09
> 绑定提交：`1305a0d`（验收修复：凭据边界/可见性/E2E 真实化/门槛强化）
> 前置：rc.1 独立验收不通过 → 阻断项全部处置

## 1. 验收阻断项处置

| 阻断项 | 处置 | 证据 |
|---|---|---|
| HTTP 凭据不可用（明文进 Vue、引用不绑定、新建丢失、删除残留、单位混用） | 前端只声明 `secret_slots`（头名）；keyring 引用由后端生成；明文只经 CredUI（`prompt_http_profile_secret`）写入 OS keyring；删除 profile 返回引用供桌面壳清理 keyring；update 字段映射修复；响应上限统一 KB 输入/字节提交；`window.confirm` 移除 | `test_secret_slots_generate_keyring_references` / `test_secret_injected_via_keyring_channel_and_absent_from_output`；HttpProfilesPanel 重写 |
| SQL 密码违反契约（明文进 Vue） | 密码输入改为系统凭据对话框（`prompt_sql_profile_secret`）；后端生成 `password_secret_ref`；删除清理 keyring | SqlProfilesPanel 重写；后端路由测试 |
| 工具可见性与计划不一致（无 profile 也注册） | HTTP/SQL flag 开启但无已启用 profile 时工具不注册；禁用后立即不可见 | `test_http_flag_and_profile_control_tool_visibility` / `test_sql_flag_and_profile_control_tool_visibility` |
| B6 E2E 假阳性（条件跳过、自造 DOM） | 重写：真实 SSE 审批流渲染 + 真实 approvals API 触发 + executions 驱动 SQL 表格/Diff 弹窗 + 真实组件长输出 + 设置面板；禁止条件跳过（元素不存在即失败） | `e2e/keyboard-focus.spec.ts` 5 项，E2E 全量 52/52 |
| 安装升级证据未完成 | 0.3.0-alpha.2 → 0.5.0-rc.2 静默覆盖安装演练（版本/二进制替换 + sidecar SHA 与 manifest 一致 + 卸载器）；数据快照与 additive 迁移证据；0.2.1 安装包缺失（仅 tag）→ 完整实机演练列为观察环境首日任务 | `dist/upgrade-smoke/install-overlay-0.3.0a2-to-0.5.0rc2.json` / `dist/upgrade-report-0.2.1-to-0.5.0-rc.2.json` / `before.json` |
| RC 证据未绑定当前提交 | smoke 证据按版本命名且绑定 `git_commit`；manifest 重新生成绑定 `1305a0d`；release-check 报告含 gate issues | `qa-evidence-0.5.0-rc.2.json`（git_commit=1305a0d）/ `release-check-0.5.0-rc.2.md` |
| release-check 门槛不严格 | `ok` 现在包含：步骤全过 ∧ 工作区干净 ∧ 五处版本一致 ∧ 安装包存在 ∧ qa-evidence 绑定当前 commit；违规项记录 `release_gate_issues` | `test_assemble_report_strict_gates_fail_without_evidence`；报告 `release_gate_issues: none` |

## 2. rc.2 门禁

| 门禁 | 结果 |
|---|---|
| pytest | ✅ 753（+ 16：凭据绑定/可见性/gates） |
| Vitest | ✅ 104 |
| E2E（Playwright） | ✅ 52（含键盘真实渲染 5 项） |
| cargo check/test | ✅ 15 |
| release-check | ✅ 14/14，`release_gate_issues: none`，`worktree_dirty: False`，`installer_built: True` |
| 安装版 smoke | ✅ 5/5（qa-evidence 绑定 1305a0d） |
| NSIS 安装包 + updater 签名 | ✅ 构建完成（未签名状态如实报告） |

## 3. 证据清单（rc.2）

- `dist/qa-evidence-0.5.0-rc.2.json`（smoke 5/5，git_commit 绑定）
- `dist/release-check-0.5.0-rc.2.json/.md`（14/14，gate none）
- `dist/release-manifest-0.5.0-rc.2.md`（绑定 1305a0d，installer/sidecar/.sig SHA）
- `dist/alpha-0.5.0-rc.2-flags.json`（23 项）
- `dist/codesign-status-0.5.0-rc.2.json`（未签名说明）
- `dist/upgrade-smoke/`（before.json 数据快照、install-overlay 证据、upgrade-report）
- NSIS 安装包 + `.sig`（target/release/bundle/nsis/）

## 4. 观察前明确项

- **0.2.1 → rc.2 完整实机演练**（启动/迁移/数据保留/回退/updater）需观察环境执行：
  0.2.1 安装包需从 `v0.2.1` tag 构建；实机启动避免与开发主库/端口冲突。
- 安装包级证据（覆盖安装 + SHA 绑定）已在本次产出，演练流程与成对计数模板就绪。

## 5. 状态

rc.2 为当前候选冻结提交：代码缺陷阻断项全部关闭，门禁全绿，证据绑定当前
提交。按验收建议：从 rc.2 冻结提交开始唯一一次 14 天观察；观察期间完成
0.2.1 完整升级/回退演练；观察通过后发布正式 0.5.0。
