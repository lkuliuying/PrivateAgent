# PrivateAgent v0.5.0-rc.3 检查点（验收二次修复）

> 制定日期：2026-08-09
> 绑定提交：`b0613e3`（最终冻结提交；所有证据绑定此提交）
> 前置：rc.2 二次验收 → 阻断项全部处置

## 1. 二次验收阻断项处置

| 项 | 处置 | 证据 |
|---|---|---|
| RC 证据未绑定当前提交 | 最终冻结提交 `b0613e3` 上重新执行安装版 smoke（5/5）并重新生成 qa-evidence（`git_commit` 绑定）、manifest、flags、codesign、release-check | `qa-evidence-0.5.0-rc.3.json` / `release-check-0.5.0-rc.3.md` / `release-manifest-0.5.0-rc.3.md` |
| 升级/updater/回退未执行 | **完整实机演练完成**（隔离库 `upgrade_e2e` + 隔离 APPDATA/.env）：0.2.1（从 v0.2.1 tag 构建安装包）安装启动 → 0021/64 表 → 写入业务数据 → rc.3 覆盖安装启动 → **0026/67 表、数据保留一致** → 0.2.1 回退安装启动 → **schema 保持 0026 不降级不破坏、数据保留一致**（`data_preserved=True`）；updater 签名来源验证：同一 key 重签名密文与内置 `.sig` 一致 | `dist/upgrade-smoke/upgrade-rollback-0.2.1-to-0.5.0rc2.json` / `data-preservation-and-updater.json` / `latest-0.5.0-rc.3.json` / `install-overlay-0.3.0a2-to-0.5.0rc2.json` / `before.json` |
| 三项 E2E 假阳性 | Diff 弹窗 Esc 后**断言焦点回到触发按钮**；5000 行命令输出**经真实审批卡**（SSE command approval + executions + output 轮询）渲染；HTTP 配置表单**真实 Tab 焦点顺序**（名称→Scheme→Host→Port→Path）断言 | `e2e/keyboard-focus.spec.ts` 5 项全部真实断言 |
| OS keyring 生命周期 | 保存 profile 后才可设置密钥（取消/失败不留孤立凭据）；删除清理失败显示**重试入口**；Tauri invoke 统一收口 `api/credentials.ts`（组件不再散落动态导入） | HttpProfilesPanel/SqlProfilesPanel 重写 + credentials.ts |
| 检查点/manifest 矛盾 | rc.3 检查点按**机器生成报告**修正（pytest 739 / Vitest 104 / E2E 52 / release-check 14/14 `ok=True` / `release_gate_issues: none`）；manifest 由 `generate_release_manifest.py --write` 生成（含 release-check 摘要与勾选状态） | 本文件与 `dist/release-check-0.5.0-rc.3.md` 一致 |

## 2. rc.3 门禁（最终冻结提交 b0613e3）

| 门禁 | 结果 |
|---|---|
| release-check | ✅ 14/14，`ok=True`，`release_gate_issues: none`，`worktree_dirty: False`，`installer_built: True` |
| pytest | ✅ 739 passed（与机器报告一致） |
| Vitest | ✅ 104 |
| E2E（Playwright） | ✅ 52（键盘 5 项真实断言；视觉回归允许单次重试吸收负载抖动，真实回归仍 FAIL） |
| Cargo | ✅ 15 |
| 安装版 smoke | ✅ 5/5（绑定 b0613e3） |
| NSIS 安装包 + updater 签名 | ✅ 构建完成（SHA 与 manifest 一致；未签名状态如实报告） |

## 3. 升级/回退演练结果（隔离环境）

| 阶段 | schema | 表数 | 业务数据（sessions/messages/projects） |
|---|---|---|---|
| 0.2.1 安装启动 | 0021 | 64 | 播种 1/1/1 |
| rc.3 覆盖安装启动 | 0026 | 67 | 1/1/1（保留 ✓） |
| 0.2.1 回退安装启动 | 0026（不降级） | 67 | 1/1/1（保留 ✓） |

updater：latest.json（rc.3）+ `.sig` 生成；同一 key 重签名密文与内置一致。

## 4. 遗留说明（观察环境执行）

- **应用内更新按钮实机点击**：需要 UI 会话与本地更新端点，列观察环境首日；
  签名与 latest.json 机制已验证。
- 主开发库未受演练影响（隔离库 `upgrade_e2e`；主库 schema 0026/67 表核验不变）。

## 5. 结论

rc.3 为**最终冻结提交**（`b0613e3`）：全部证据绑定该提交，release-check
`ok=True`，升级/回退/数据保留/updater 签名演练完成。满足进入唯一一次
14 天整体观察的前置条件；观察通过后从该提交发布正式 `0.5.0`。
