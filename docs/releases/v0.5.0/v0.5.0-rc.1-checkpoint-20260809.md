# PrivateAgent v0.5.0-rc.1 检查点（RC 硬化与冻结）

> 制定日期：2026-08-09
> 上游计划：[`v0.5.0-development-plan-20260809.md`](./v0.5.0-development-plan-20260809.md) §B6
> 状态：B6 完成（RC 功能冻结），进入唯一一次 14 天观察

## 1. B6 交付内容

1. **版本提升**：`0.5.0-rc.1` 统一（`__init__.py`、pyproject、package.json、
   tauri.conf.json、Cargo.toml、Cargo.lock、package-lock.json，UTF-8 无 BOM）；
   telemetry scope 测试改为动态引用版本号。
2. **BOM 治理**：移除 13 个源文件 BOM（版本提升时 PowerShell 写入引入；
   曾导致 vite PostCSS 与 hatchling 解析失败），E2E 在 BOM 修复后 50/50 全绿。
3. **键盘深度焦点 E2E**（`e2e/keyboard-focus.spec.ts`，3 项）：审批卡按钮
   键盘可达、长文本渲染不卡死且页面可交互、Diff 弹窗 Esc 后焦点可恢复。
4. **长内容压力**：5000 字符级输出渲染后页面仍响应（SQL 表格/命令输出路径）。
5. **数据快照**：`dist/upgrade-smoke/before.json`（升级演练基线，1000 会话等）。
6. **RC 证据包**：manifest、flags 快照（23 项）、codesign 状态、release-check。

## 2. RC 门禁核对

| 门禁 | 结果 |
|---|---|
| release-check 全绿、工作区干净 | ✅ 14/14（首次 13/14 为已知视觉回归环境抖动，重跑通过）；提交后干净 |
| pytest / Vitest / Playwright / Cargo / 安装版 smoke | ✅ 737 / 104 / 50 / 15 / 5-5（beta.2 smoke） |
| P0/P1 = 0，P2 有处置 | ✅ 三项目 P2 已文档化（见 §3） |
| 迁移均 additive、旧版回退可读 | ✅ 0023–0026 全部 additive；备份清理测试覆盖 |
| 安装/升级/回退/数据保留证据可审计 | 📋 数据快照与 upgrade_smoke 工具就绪；真实安装包演练需构建环境（B6 已产出 rc.1 sidecar 与清单，安装演练随 RC 观察环境执行） |
| 观察 runner 不误计、不破坏业务数据 | 📋 快照只读（`--snapshot` 不改数据）；遥测持久化口径既有测试覆盖 |
| RC 期间默认开启功能已冻结 | ✅ 四类工作流默认关闭且 flag 快照冻结；无默认开启新功能 |

## 3. P2 处置清单（RC 冻结后不再新增功能）

| P2 | 处置 | 负责人 |
|---|---|---|
| LLM 需要显式 project_id/profile_id 上下文 | 工作流提示注入可用；ContextBuilder 增强留待观察后评估（不阻塞 RC） | 观察期后 A 轨道 |
| Windows 符号链接 E2E 需权限环境 | CI/开发者模式环境执行；拒绝逻辑由注入测试覆盖 | 观察期 QA |
| 前端 E2E 环境抖动 | 低负载环境全绿（本次 release-check 14/14）；观察期保持复跑 | 观察期 QA |

## 4. 冻结声明

- 功能冻结：四类工作流、多步骤恢复、人工处置、完成条件、profile 管理均
  不再新增功能；观察期只允许缺陷/文案/布局/测试修复。
- 公开 DTO 冻结：B0 契约表与 Schema 摘要（`test_workflow_contracts.py`）冻结；
  /capabilities additive 字段已入 0.3.0 契约文档。
- 迁移冻结：0026 为最后基线；观察期不允许非 additive 变更。
- flags 冻结：23 项快照（`dist/alpha-0.5.0-rc.1-flags.json`）。

## 5. 进入 14 天观察

- 观察 runner/聚合器/告警口径：复用既有 telemetry 持久化 + upgrade_smoke 快照；
  最低样本与故障场景按主计划 §RC 执行。
- 严重问题重计规则：仅数据损坏/秘密泄漏/审批绕过/重复副作用重启完整 14 天。

## 6. 交付物

- `docs/releases/v0.5.0/v0.5.0-rc.1-checkpoint-20260809.md`（本文档）
- `dist/release-manifest-0.5.0-rc.1.md`、`dist/alpha-0.5.0-rc.1-flags.json`、
  `dist/codesign-status-0.5.0-rc.1.json`、`dist/release-check-0.5.0-rc.1.md`、
  `dist/upgrade-smoke/before.json`
- `e2e/keyboard-focus.spec.ts`
