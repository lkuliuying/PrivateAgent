# PrivateAgent v0.5.0-rc.4 检查点（观察前最终验收 · 阻断项全部闭环 · 冻结）

> 路线更新（2026-08-20）：本检查点仍是有效工程基线，但原 14 天观察在 Day 10 后停止，最终观察顺延到 `v1.0.0-rc.1`。见 [观察期顺延决策](../observation-policy-20260820.md)。

> 制定日期：2026-08-11（终稿）
> 绑定提交：**本提交**（含 smoke 判定修复；见 `git rev-parse HEAD`）
> 前置：rc.4 第四次验收 → updater 端点阻断闭环（上轮）→ 标签/QA 证据治理（本轮）

## 1. 观察前阻断项闭环状态

### 1.1 updater（上轮已闭环，本节复述）

- 公开更新通道仓库 `lkuliuying/PrivateAgent-updates`（仅资产，无源码）；
  `tauri.conf.json` 端点 = `https://github.com/lkuliuying/PrivateAgent-updates/releases/latest/download/latest.json`。
- rc.4 Release（非 prerelease）挂 `latest.json` + 安装包 + `.sig`；匿名下载 SHA 与本地一致
  （latest.json `71544413…` / 安装包 `6189ba9b…` / sig `25b26358…`）。
- 源仓库 rc.4 Release 已标记 prerelease；稳定 latest 语义回到 v0.2.1。
- 实机 in-app 演练（rc.4 → rc.5 演练包）：检查→下载→验签→安装→重启→数据保留全部通过
  （证据：`dist/upgrade-smoke/drill-evidence/*.png`、`dist/upgrade-smoke/data-preservation-and-updater-drill.json`）。

### 1.2 本轮治理：QA smoke 真实性 + 冻结标签 + 证据绑定

| 项 | 处置 | 证据 |
|---|---|---|
| QA smoke 假通过（`limit_exceeded` 也判 pass） | `v050_workflow_smoke.py` 通过标准修复：run A 最终状态必须 `completed`、无 failed execution、patch 与 command 在该轮均成功；SQL/HTTP run 也必须 `completed`；取消场景必须 `cancelled` 且无残留进程；重试改为新 run 完整等待（≤5 轮），不再跨轮累计。smoke 另适配纯 CPU/慢模型环境（run limits 放宽 + 轮询超时提高 + 提示词要求最小外科式修改）。**重新运行后 6/6 通过** | `dist/qa-evidence-0.5.0-rc.4.json`（绑定本提交，`run_a.status=completed`） |
| 源仓库 `0.5.0-rc.4` 标签指向旧提交 `7dcf372` | 标签已移至冻结提交（见下） | GitHub tag |
| 证据绑定漂移（f2aed11 vs HEAD） | 唯一冻结对象 = **本提交**：qa-evidence、release-check、manifest 全部在本提交上重新生成 | `dist/release-check-0.5.0-rc.4.json` / `dist/release-manifest-0.5.0-rc.4.md` |
| 无 Day 1 观察记录 | `docs/releases/v0.5.0/observation-day1-20260811.md` | 同文件 |

> 说明：本提交之后不再对候选代码做任何修改；后续仅允许追加文档/报告提交（
> 若发生，须在文档中声明其不属于候选代码）。14 天观察基线 = 本提交 + 本提交
> 上生成的安装包与公开通道资产。

## 2. 门禁（机器报告口径，绑定本提交）

| 门禁 | 结果 |
|---|---|
| release-check-full | 14/14，`ok=True`，`release_gate_issues: none`（`dist/release-check-0.5.0-rc.4.json`） |
| pytest / Vitest / E2E / Cargo | 739 / 104 / 52 / 15 |
| 安装版运行时 smoke | 6/6（qa-evidence 绑定本提交；run A completed + 无 failed execution） |
| NSIS + updater 签名 | 安装包 `6189ba9b…` 与 `.sig` `25b26358…` 与公开仓库资产一致 |

> E2E 备注：`performance-resource.spec.ts` 长任务 p95≤50ms 用例在流水线负载下临界
> （51-57ms，独立跑 50ms 通过），按仓库既有政策（b0613e3 先例）允许单次重试，
> 重试仍失败则 FAIL，不掩盖真实回归。

## 3. 升级/回退（历史记录，rc.4 安装包）

| 阶段 | schema | 表数 | 业务数据 |
|---|---|---|---|
| 0.2.1 安装启动 | 0021 | 64 | 播种 1/1/1 |
| rc.4 覆盖启动 | 0026 | 67 | 保留 ✓ |
| 0.2.1 回退启动 | 0026（不降级） | 67 | 保留 ✓ |

## 4. 发布物状态

- 源仓库（私有）：rc.4 Release 标记 prerelease；稳定 latest = v0.2.1；
  **`0.5.0-rc.4` 标签指向冻结提交**。
- 公开更新通道：`PrivateAgent-updates` rc.4（非 prerelease，latest 语义生效）
  + rc.5 演练包（prerelease，仅审计保留）。
- 升级方向：rc.4 安装版检查更新 → 通道 latest.json（rc.4）→ 已是最新；
  正式 v0.5.0 发布时新 Release 成为 latest 即触发更新。

## 5. 结论（冻结声明）

观察前阻断项全部闭环：

1. ✅ 匿名 updater 端点 + 真实资产（公开通道仓库）
2. ✅ rc.4 标记 prerelease，稳定语义干净
3. ✅ 安装版实机 in-app 全链路演练通过（检查→下载→验签→安装→重启→数据保留）
4. ✅ QA smoke 严格判定修复并取得真实 6/6（run A completed、无 failed execution）
5. ✅ 唯一冻结对象确定：本提交；qa-evidence / release-check / manifest 全部绑定本提交
6. ✅ 源仓库 `0.5.0-rc.4` 标签指向冻结提交
7. ✅ Day 1 观察记录建立（`observation-day1-20260811.md`）

**自本提交起冻结 RC，开始唯一一次 14 天整体观察（Day 1 = 2026-08-11）。**
观察通过后升版 `0.5.0`：发布正式资产到公开通道（新 Release 成为 latest）、
源仓库稳定 Release 更新、切换稳定 updater 通道语义。
