# PrivateAgent v0.5.0-rc.4 检查点（进入观察前最终验收修复）

> 制定日期：2026-08-10
> 绑定提交：以实际 HEAD 为准（本轮证据链最终统一后记录）
> 前置：rc.3 第三次验收 → 阻断项处置（本轮：签名协调、升级证据重生成、manifest 统一）

## 1. 第四次验收阻断项处置

| 项 | 处置 | 证据 |
|---|---|---|
| updater 签名与最终安装包不匹配 | **最终签名协调**：sign rc.4 安装包 → 同一 `.sig` 字节写入 `latest-0.5.0-rc.4.json`（`signature strictly identical: True`）；`.sig` SHA 与 latest.json SHA 记录；发布 GitHub Release 资产并经**认证下载验证与本地严格一致** | `latest-0.5.0-rc.4.json` / `data-preservation-and-updater-rc4.json` |
| rc.4 升级证据混 rc.2 内容 | 重写 `data-preservation-and-updater-rc4.json`（rc.4 场景、`generated_at`、安装包 SHA、步骤日志、数据计数）；升级报告 rc.4 版 | 同前 + `upgrade-report-0.2.1-to-0.5.0-rc.4.json` |
| manifest 升级勾选不成立 | upgrade/clean 勾选依据重生成的一致证据 | `release-manifest-0.5.0-rc.4.md` |
| 观察与正式版 | 见 §5 | — |

**发布阻断记录**：GitHub 仓库为**私有**——匿名浏览器下载 release 资产 404，Tauri
updater（无认证）无法从该端点下载更新。资产经认证下载验证与本地一致；**应用内
updater 实机点击需公开端点**，列为观察环境首日任务（用同签名 key 的公开/本地
端点演练包验证下载+安装全链路）。正式发布前需公开资产或切换可公开端点（P2）。

## 2. rc.4 门禁（机器报告口径）

| 门禁 | 结果 |
|---|---|
| release-check | 14/14，`ok=True`，`release_gate_issues: none`（最终提交绑定后复验） |
| pytest / Vitest / E2E / Cargo | 739 / 104 / 52 / 15 |
| 安装版 smoke | 5/5（qa-evidence 绑定提交） |
| NSIS + updater 签名 | rc.4 构建完成，SHA 与 manifest 一致 |

## 3. 升级/回退/updater（rc.4 安装包，隔离环境）

| 阶段 | schema | 表数 | 业务数据 |
|---|---|---|---|
| 0.2.1 安装启动 | 0021 | 64 | 播种 1/1/1 |
| rc.4 覆盖启动 | 0026 | 67 | 保留 ✓ |
| 0.2.1 回退启动 | 0026（不降级） | 67 | 保留 ✓ |

updater：`latest-0.5.0-rc.4.json` 内嵌签名与 `.sig` 文件**严格一致**，GitHub
Release 已发布且经认证下载验证一致；应用内实机点击（需公开端点）列观察首日。

## 4. 遗留说明

- 应用内 updater 按钮实机点击验证：观察环境首日（私有仓库需公开/本地端点；
  签名与 latest.json 机制已验证）。
- 主开发库未受演练影响（隔离库；主库 schema 0026 核验不变）。

## 5. 结论

rc.4 为观察前最终候选。全部阻断项闭环后：从最终冻结提交开始唯一一次
14 天整体观察；观察通过后升版 `0.5.0` 并发布正式资产、切换稳定 updater 通道
（需公开资产或可公开访问端点）。
