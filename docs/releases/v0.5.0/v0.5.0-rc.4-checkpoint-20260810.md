# PrivateAgent v0.5.0-rc.4 检查点（进入观察前最终验收修复）

> 制定日期：2026-08-10
> 绑定提交：`a9b723c`（凭据清理重试闭环；checkpoint 提交后以实际 HEAD 为准）
> 前置：rc.3 第三次验收 → 阻断项全部处置

## 1. 第三次验收阻断项处置

| 项 | 处置 | 证据 |
|---|---|---|
| 凭据清理"重试"按钮无效 | 保存独立 `pendingCleanup` 对象（name+slots）；重试按钮直接调用凭据清理函数（不再调用已删除 profile 的 remove）；清理成功自动清除提示 | HttpProfilesPanel/SqlProfilesPanel（`retryCleanup`） |
| 升级/回退/updater 证据为 rc.2 | **用 rc.4 安装包完整重跑**：0.2.1 安装启动（0021/64 表）→ 播种数据 → rc.4 覆盖启动（0026/67 表，数据保留）→ 0.2.1 回退启动（schema 保持，数据保留）；updater `latest-0.5.0-rc.4.json` + 同一 key 重签名密文一致 | `upgrade-rollback-0.2.1-to-0.5.0rc4.json` / `data-preservation-and-updater-rc4.json` / `latest-0.5.0-rc.4.json` / `install-overlay-0.3.0a2-to-0.5.0rc4.json` |
| manifest 内部不一致 | manifest 顶部与嵌入 release-check 摘要均绑定同一提交；14 步骤全部 [x]；手工项（/health、clean-install、code_signed、upgrade）由机器 QA 记录勾选并附证据 | `release-manifest-0.5.0-rc.4.md` |
| 检查点冻结提交描述过期 | 本文档按实际提交编写；最终 release-check 绑定最终 HEAD 后重建 manifest | 本文件 + manifest |
| 14 天观察与正式版 | rc.4 为观察前最终候选；观察期唯一一次 14 天，通过后升版 0.5.0 并发布正式资产与稳定渠道 | 后续执行 |

## 2. rc.4 门禁（机器报告口径）

| 门禁 | 结果 |
|---|---|
| release-check | 14/14，`ok=True`，`release_gate_issues: none`（最终提交绑定后复验） |
| pytest / Vitest / E2E / Cargo | 739 / 104 / 52 / 15 |
| 安装版 smoke | 5/5（qa-evidence 绑定提交） |
| NSIS + updater 签名 | rc.4 构建完成，SHA 与 manifest 一致 |

## 3. 升级/回退/updater（rc.4 安装包，隔离环境）

| 阶段 | schema | 表数 | 业务数据 |
|---|---|---|---|
| 0.2.1 安装启动 | 0021 | 64 | 播种 1/1/1 |
| rc.4 覆盖启动 | 0026 | 67 | 保留 ✓ |
| 0.2.1 回退启动 | 0026（不降级） | 67 | 保留 ✓ |

updater：latest-0.5.0-rc.4.json + .sig 生成；重签名密文与内置一致
（应用内更新按钮实机点击需 UI 会话，列观察环境首日）。

## 4. 遗留说明

- 应用内 updater 按钮实机点击验证：观察环境首日（签名机制与 latest.json 已验证）。
- 主开发库未受演练影响（隔离库；主库 schema 0026 核验不变）。

## 5. 结论

rc.4 为观察前最终候选。全部阻断项闭环后：从最终冻结提交开始唯一一次
14 天整体观察；观察通过后升版 `0.5.0` 并发布正式资产、切换稳定 updater 通道。
