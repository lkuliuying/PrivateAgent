# PrivateAgent v0.5.0-rc.4 检查点（观察前最终验收 · 阻断项全部闭环）

> 制定日期：2026-08-10（终稿：2026-08-11）
> 绑定提交：`f2aed11`（updater 公开端点 + e2e 性能用例重试政策）
> 前置：rc.4 第四次验收 → 唯一剩余阻断项（updater 端点不可匿名访问）闭环

## 1. 观察前阻断项闭环（2026-08-11 终稿）

### 1.1 匿名 updater 端点

| 项 | 处置 | 证据 |
|---|---|---|
| 私有仓库无匿名访问 | 新建**公开更新通道仓库** `lkuliuying/PrivateAgent-updates`（仅发布资产，无源码）；`tauri.conf.json` 端点改为 `https://github.com/lkuliuying/PrivateAgent-updates/releases/latest/download/latest.json`，提交 `8d3f612` | [公开仓库](https://github.com/lkuliuying/PrivateAgent-updates) |
| 端点真实提供 latest.json + 安装包 | rc.4 Release（非 prerelease）挂 `latest.json`（指向 rc.4）+ 安装包 + `.sig`；**匿名下载 SHA 与本地构建严格一致**（latest.json `71544413…`、安装包 `6189ba9b…`、sig `25b26358…`） | `dist/upgrade-smoke/data-preservation-and-updater-drill.json` |
| rc.4 占用稳定 latest 语义 | 源仓库 rc.4 Release 标记 **prerelease**（`isPrerelease=true`）；稳定 `releases/latest` 语义回到 v0.2.1 | GitHub rc.4 Release 状态 |
| 应用内全链路未实机验证 | **实机 in-app 演练通过**（见 §1.2） | `dist/upgrade-smoke/drill-evidence/*.png` |

### 1.2 实机 in-app updater 全链路演练

- 安装版：重建后 rc.4（新端点）真实安装；演练目标：**0.5.0-rc.5 演练包**（同源码、仅版本号提升，`drill/0.5.0-rc.5` 分支 `75ce7fa` 构建，同一签名 key）。
- 驱动：Playwright CDP（`--remote-debugging-port=9222`）真实点击。

| 阶段 | 结果 |
|---|---|
| 检查更新 | UI 显示「新版本 v0.5.0-rc.5」+ 发布时间 + 发布说明（匿名取清单成功） |
| 下载 | 100,004,173 字节安装包下载完成（经本机代理，约 3.5 分钟） |
| 验签 | 内置公钥 minisign 验签通过（失败即拒绝） |
| 安装 | NSIS 静默安装；注册表 DisplayVersion `0.5.0-rc.4 → 0.5.0-rc.5` |
| 重启 | 应用自动重启；诊断页显示「版本 0.5.0-rc.5」 |
| 数据保留 | `--verify` 全部表 preserved=true（隔离库 upgrade_e2e；演练前后 `.env` 已恢复 `PA_DB_NAME=personal_assistant`） |

- 演练后：公开仓库 `0.5.0-rc.5` 标记 **prerelease**，`releases/latest` 回到 rc.4（稳定语义）；本机覆盖重装 rc.4 恢复观察基线（DisplayVersion=0.5.0-rc.4，启动验证通过）。
- **本机环境说明**：该开发机直连 github.com 会被重置（git/curl 复现），WinINET 代理 `127.0.0.1:10808` 可用；演练启动应用时按项目既有约定（`scripts/build-release.bat` 注释）设 `HTTPS_PROXY`。**这是本机网络环境特性，与发布物无关**；真实用户网络匿名可达（PowerShell 匿名下载验证已证）。

## 2. 最终门禁（机器报告口径，绑定 f2aed11）

| 门禁 | 结果 |
|---|---|
| release-check-full | **14/14，`ok=True`，`release_gate_issues: none`，worktree 干净**（`dist/release-check-0.5.0-rc.4.json`） |
| pytest / Vitest / E2E / Cargo | 739 / 104 / 52 / 15 |
| 安装版运行时 smoke | 5/5（qa-evidence 绑定 f2aed11） |
| NSIS + updater 签名 | rc.4 重建完成；安装包 `6189ba9b…` 与 `.sig` `25b26358…` 与公开仓库资产一致 |

> E2E 备注：`performance-resource.spec.ts` 长任务 p95≤50ms 用例在流水线负载下临界（51-57ms，独立跑 50ms 通过），按仓库既有政策（b0613e3 视觉回归先例）为性能用例加入单次重试（f2aed11）；重试仍失败则 FAIL，不掩盖真实回归。

## 3. 升级/回退（历史记录，rc.4 安装包）

| 阶段 | schema | 表数 | 业务数据 |
|---|---|---|---|
| 0.2.1 安装启动 | 0021 | 64 | 播种 1/1/1 |
| rc.4 覆盖启动 | 0026 | 67 | 保留 ✓ |
| 0.2.1 回退启动 | 0026（不降级） | 67 | 保留 ✓ |

## 4. 发布物状态

- 源仓库（私有）：rc.4 Release 标记 prerelease；稳定 latest = v0.2.1。
- 公开更新通道：`PrivateAgent-updates` rc.4（非 prerelease，latest 语义生效）+ rc.5 演练包（prerelease，仅审计保留）。
- 升级方向：rc.4 安装版检查更新 → 通道 latest.json（rc.4）→ 当前已最新；正式 v0.5.0 发布时新 Release 成为 latest 即触发更新。

## 5. 结论（冻结声明）

观察前阻断项已全部闭环：

1. ✅ 安装版可匿名访问 updater 端点（公开通道仓库）
2. ✅ 端点真实提供 `latest.json` + 对应安装包 + `.sig`
3. ✅ rc.4 Release 正确标记 prerelease，稳定语义干净
4. ✅ 安装版实机完成 检查→下载→验签→安装→重启 全链路（证据见 §1.2）
5. ✅ 证据链统一绑定最终提交 `f2aed11`，门禁 14/14 `ok=True`

**自 `f2aed11` 起冻结 RC，开始唯一一次 14 天整体观察。** 观察通过后升版 `0.5.0`：发布正式资产到公开通道（新 Release 成为 latest）、源仓库稳定 Release 更新、切换稳定 updater 通道语义。
