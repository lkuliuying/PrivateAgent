# PrivateAgent v0.9.0-rc.1 硬化与开发完成检查点

> 日期：2026-08-23（dev/0.9.0）
> 依据：[开发计划](./v0.9.0-development-plan-20260820.md) §8（H6）、§11、§12
> 检查点链：[H0 契约](./v0.9.0-h0-contracts-20260823.md) →
> [H0+H1 执行记录](./v0.9.0-h0h1-execution-20260823.md) →
> [alpha.1](./v0.9.0-alpha1-checkpoint-20260823.md) →
> [beta.1](./v0.9.0-beta1-checkpoint-20260823.md) →
> [beta.2](./v0.9.0-beta2-checkpoint-20260823.md)

## 1. RC 即时完整门禁（2026-08-23，全绿）

| 门禁 | 结果 |
|---|---|
| 后端全量 pytest | **1041 passed, 2 skipped**（v0.8.0 基线 976 → **+65**） |
| ruff（`src tests scripts`） | All checks passed |
| alembic | head=**0032**（0031 绑定审计+授予表；0032 worktree 生命周期；均 additive） |
| 前端 vue-tsc | 0 错误 |
| 前端 vitest | **295 passed**（47 files） |
| bundle:check | css 102.0% / index 104.3% / tauri 100% / vendor 98.6%（< +10% 硬线） |
| Playwright e2e | **109 passed**（2.2min；含视觉回归矩阵无漂移） |
| Rust sidecar `cargo check` | Finished（Tauri 侧编译通过） |
| P0/P1 | **0** |

**三类真实项目即时矩阵（计划 §9）**：
- **Python**：后端 pytest 真实执行（命令权限经真实 `pytest` 子进程；worktree 在真实 `git init` 仓库创建/清理）；
- **Vue/Node**：桌面前端（Vue 3 + Node 工具链）vitest 295 + e2e 109；
- **Rust**：Tauri sidecar `cargo check` 编译通过。

## 2. 完成定义核对（计划 §12）

| # | 定义 | 状态 |
|---|---|---|
| 1 | Coding Agent 成为新安装与升级后的默认主线 | ✅ `DEFAULT_CODING_WORKBENCH=true` + 能力位回退 |
| 2 | 首启、项目授权、任务执行和恢复体验完整 | ✅ 首启流程 + `authorize-scope` + reconcile/waiting_approval 恢复 |
| 3 | 可选 worktree 安全且不影响根 workspace | ✅ H3（固定 argv、HEAD 复核、dirty 不删） |
| 4 | 真实项目故障注入和安装版门禁通过 | ✅ 后端故障注入套件；三类真实项目矩阵（Python 真实执行 / Vue-Node vitest+e2e / Rust cargo check）；安装版构建主链登记发布流水线（v0.7.0 起口径） |
| 5 | 旧 planner/UI 调用达到隔离与观察门槛 | ✅ 遥测标签就位（§7 清单）；自然日观察顺延 `v1.0.0-rc.1` |
| 6 | 旧业务数据仍可访问和导出 | ✅ 无物理删除；`bind-project` 仅显式迁移 |
| 7 | `0.9.0-rc.1` 即时完整门禁通过且无 P0/P1 | ✅ 见 §1 |
| 8 | 新建项目/对话拆分，新对话绑定真实项目/工作区 | ✅ 侧栏双动作 + `NewProjectDialog` + 绑定校验 |
| 9 | 工作目录、Git facts、Asia/Shanghai 时间准确一致 | ✅ 固定 argv Git 服务 + RFC 3339 UTC + `timeDisplay.ts` |
| 10 | 三档权限独立能力、快照、审计、撤销、失败关闭 | ✅ `full_access` 授予链 + 降级遥测 + 反向测试 |
| 11 | 上下文圆环真实 budget、压缩、无障碍、响应式 | ✅ `ContextUsageRing` + budget 端点 + aria/文本替代 |
| 12 | 重复模型模块移除，PrivateAgent 唯一入口 | ✅ 矩形模块删除；模型属性收敛 PrivateAgent 入口 |
| 13 | 执行详情公开决策链可追溯、不暴露隐藏推理 | ✅ `decision.summary` + `execution-detail` 端点 |

## 3. 零容忍项复核（计划 §11）——全部未触碰

- 默认权限 `confirm`（未授予 workspace 自动写）✅
- 升级零数据丢失、零隐式绑定 ✅
- worktree 不破坏原仓库、不删 dirty ✅
- unknown 不自动重试（决策词汇反向断言）✅
- 旧/新执行链不并行（既有单链保障延续）✅
- 取消/退出无残留进程（既有进程树清理延续）✅
- 遥测仅低基数计数，无代码/路径/命令正文 ✅
- 新对话强制项目/工作区绑定，无静默改绑 ✅
- Git 事实来自固定 argv 服务，不由前端文本推断 ✅
- 时间统一 UTC 事实 + 上海显示，迁移不改先后 ✅
- 「替我批准」为真实能力位（非标签）✅
- `full_access` 独立确认/撤销/到期，不绕过凭据/远程边界 ✅
- 圆环无伪造百分比/负数/>100/旧值 ✅
- 模型模块删除后 Provider 配置仍可达（PrivateAgent 入口）✅
- 执行后进入详情视图，命令/审批/验证可追溯，无隐藏 CoT ✅

## 4. 版本号与交付

- 安装版版本号维持 `0.7.0-rc.1`（v0.8.0 先例：内部里程碑不变更公开
  updater `latest`，不 bump 版本字符串；`0.9.0-*` 为内部检查点标签）。
- 不切公开 updater；RC 回退键保留：`?coding=0`、`pa_coding_workbench=0`、
  `?ui=v1`、`PA_CODING_AGENT_UI_ENABLED=false`。
