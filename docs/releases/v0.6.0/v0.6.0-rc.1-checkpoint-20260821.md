# PrivateAgent v0.6.0-rc.1 检查点（RC 硬化完成 · 内部开发完成版冻结）

> 制定日期：2026-08-21
> 绑定提交：候选代码 **`578db27`**（C6-1 版本 bump + 回退加固）；文档/修复提交 **`6f1e87a`**（ruff 门禁修复 + v0.7.0 交接包，无候选行为变化）
> 前置：C6-1 契约冻结 → C6-2 sidecar 构建 + QA smoke → C6-3 NSIS/manifest/latest.json → C6-4 全量门禁 → C6-5 安装升级演练
> 版本主题：Coding 领域与运行事实底座（[开发计划书](./v0.6.0-development-plan-20260820.md)）

## 1. 里程碑完成矩阵（C0 → C6）

| 里程碑 | 交付 | 退出条件 | 状态 |
|---|---|---|---|
| C0 契约冻结与迁移设计 | 契约文档、迁移 0027/0028、模型/DTO/错误码/flag/telemetry、13 项失败契约测试 | 字段 additive、无隐式授权设计 | ✅ 完成（`c5632a5`） |
| C1 ProjectWorkspace 与会话绑定 | workspace 模型/repo/service/API、幂等 root 补建、session DTO、前端类型化 API、迁移 0027/0028 幂等化 | 旧数据不变、不重复建 root、路径丢失失败关闭 | ✅ 完成（`f1bcc22`） |
| `0.6.0-alpha.1` | schema upgrade/clone/回退通过；Workspace 契约与 API 测试通过；不更新稳定 updater | 同左 | ✅ 通过（`f1bcc22` 检查点验证） |
| C2 Project-bound Run 与 Coding Context | 创建 DTO/响应、归属校验、Git/权限快照、client_request_id 幂等、ContextBuilder 项目片段、迁移 0029、错误码 | 一请求一 run、不依赖前端选择推断路径、错配 100% 拒绝 | ✅ 完成（`db91ff6`） |
| C3 真实计划与事件续读 | update_run_plan safe 工具、RunPlan CAS/状态机、plan/artifact durable 事件、SSE 续读/heartbeat/快照纠偏 | UI 不推断计划、乱序/旧版本拒绝、断线不建替代 run | ✅ 完成（`ba20040`） |
| `0.6.0-alpha.2` | 主路径/兼容矩阵、fixture 冻结、契约/恢复/API 测试通过 | 同左 | ✅ 通过 |
| C4 前端 Run Projector 原型 | Coding contracts、runProjector（(run_id, sequence) 幂等）、dev fixture、15 项单测 | 覆盖重复/乱序/重连/切换、旧 UI 不回退 | ✅ 完成（`16b9fe1`） |
| C5 兼容、诊断与恢复 | 路由 telemetry、诊断脱敏、SSE 断线不取消、flag 精确回退、旧 session/task 兼容 | 兼容可解释、回退无 downgrade、恢复不重复副作用 | ✅ 完成（`d0d3818` + `eaed140`） |
| `0.6.0-beta.1` | 全量门禁、安装版 smoke 四场景、数据库升级与旧应用回退通过 | 同左 | ✅ 通过（`eaed140`，安装版场景 1-4 重跑） |
| C6 RC 硬化 | C6-1 契约冻结 + bump rc.1 + 回退加固（`578db27`）→ C6-2 sidecar 构建 + QA smoke 11/11 → C6-3 NSIS 安装包 + manifest + latest.json → C6-4 全量门禁 → C6-5 安装升级演练 → C6-6 本检查点 + v0.7.0 交接包 | 见第 2-5 节 | ✅ 完成 |

## 2. 门禁（机器报告口径）

### 2.1 release-check-full（`dist/release-check-0.6.0-rc.1.json` / `.md`）

| 门禁 | 结果 |
|---|---|
| pytest | **825 passed** |
| ruff check（src/tests/scripts） | ✅ 通过（C6-4 修复 6 项测试/脚本 import 问题） |
| compileall | ✅ 通过 |
| npm build（vue-tsc + vite build） | ✅ 通过 |
| npm test（Vitest） | ✅ **119 passed**（23 files，含 runProjector 15 项单测） |
| npm e2e（Playwright） | ⚠️ **51 passed / 1 failed**（性能用例，环境漂移，见 K2） |
| cargo check / cargo test | ✅ 通过 |
| sidecar smoke | ✅ PASS（/health 200） |
| alembic current | ✅ schema `0029` |
| git diff check | ✅ 通过 |
| docker compose config / diagnostic redaction / latest.json 校验 | ✅ 通过 |
| database schema | `0029`（迁移链 0027/0028/0029 就绪） |

### 2.2 e2e 唯一失败项（K2，非回归，已证伪）

`performance-resource.spec.ts:205`「页面切换与主要操作：无 50ms 级无必要长任务」p95=51-61ms（预算 50ms）。

**非回归证据链（完整）：**

1. `git diff 9250da6..578db27 -- apps/desktop`：生产运行时**零变化**——仅版本号 bump、`types.ts` 纯类型声明、`api.ts` 新增未被 UI 调用的函数、`runProjector.ts` 新模块（无生产引用）、dev fixture（不进生产产物）。
2. **基线对照实验**：对 rc.4 冻结提交 `9250da6` 建 git worktree，同一机器、同一时间窗、同一依赖运行同一用例 → **同样失败**（52ms / 53ms），与 v0.6.0 候选代码无关。
3. 该用例自 0.4.0 起即处于预算临界（rc.4 检查点记录：套件内 51-57ms 临界、单独跑稳定 50ms），当前机器存在后台负载（douyin 1.4GB、Ollama、IDE 等）导致基线漂移。

处置：记录为已知问题 K2（环境漂移，附对照证据），不修改性能预算或测试逻辑；不构成 P0/P1。

## 3. 安装版证据

| 证据 | 结果 |
|---|---|
| QA smoke（`dist/qa-evidence-0.6.0-rc.1.json`，绑定 `578db27`） | **11/11 ok**：flag 开启启动、workspace ensure + coding session、run 创建幂等（replay=True）、partial 422 拒绝、session_not_coding 409 拒绝、SSE 断线不取消、断线不建替代 run、sidecar 重启、重启 reconcile running run、flag 关闭启动、flag 关闭 legacy 保留 |
| NSIS 安装包（`PrivateAgent_0.6.0-rc.1_x64-setup.exe`） | 已构建，sha256 `3d16643e…c201b5`，签名 `.sig` `53b71a1d…3f9f`（`release-manifest-0.6.0-rc.1.md`） |
| updater manifest `latest.json` | 结构与签名校验通过 |
| 安装升级演练（`dist/upgrade-smoke/upgrade-rollback-0.5.0rc4-to-0.6.0rc1.json`） | **ALL PASS**：0.5.0-rc.4（0026）→ 0.6.0-rc.1（0029）覆盖安装、种子数据保留、新表建齐（agent_run_artifacts/project_workspaces/run_plan_items）→ 回退安装 0.5.0-rc.4（无破坏性 downgrade，schema 0029 保留、数据保留）→ 重装 0.6.0-rc.1 恢复启动。`data_preserved=true` |

## 4. 已知问题（冻结时点）

| 编号 | 问题 | 影响与处置 |
|---|---|---|
| K1 | 0.5.0-rc.4（head=0026）无法识别 0029 schema，回退安装后旧版拒绝启动 | schema/数据零破坏；v0.6.0 自身已加固（DB revision 不在迁移链内时跳过迁移继续启动，C6-1 `server_entry`）；升级/回退/恢复演练 3 步全部通过 |
| K2 | e2e 性能用例（50ms 长任务预算）在本机负载下 p95=51-61ms | 非 v0.6.0 回归：生产运行时零变化 + rc.4 基线同环境同样失败（对照实验）；列入 checkpoint 报告，不修改预算与测试 |

无未处置 P0/P1。

## 5. 完成定义逐条核对（计划书 §9）

| # | 完成定义 | 证据 | 状态 |
|---|---|---|---|
| 1 | ProjectWorkspace、CodingThread、AgentRun 和 RunPlan 契约稳定 | C0 契约文档 + 契约测试（13 项）+ C2/C3/C6 前置扩展；C6-1 冻结提交 | ✅ |
| 2 | project-bound run 可创建、续读、取消和恢复 | QA smoke：run_create_idempotent、sse_disconnect、restart_reconciles；C2/C3/C5 测试 | ✅ |
| 3 | 真实计划由后端持久化和发布 | `run_plan_items` 表 + `update_run_plan` + plan.created/updated/item_changed durable 事件 + SSE 续读 | ✅ |
| 4 | 前端 projector 可以完全消费公开事件 | `runProjector` + 15 项单测（重复/乱序/重连/切换/legacy）+ dev fixture | ✅ |
| 5 | 旧会话、旧任务和旧 UI 不回退 | flag 回退测试 + smoke `flag_off_preserves_legacy` + legacy 契约测试 | ✅ |
| 6 | 数据库升级与应用回退不丢数据 | 安装升级演练 ALL PASS（data_preserved=true，无破坏性 downgrade） | ✅ |
| 7 | `0.6.0-rc.1` 即时完整门禁通过且无未处置 P0/P1 | release-check-full：pytest 825 / vitest 119 / e2e 51+1（K2 环境漂移，已证伪回归）/ cargo / smoke 全过；K1/K2 均已处置 | ✅（K2 附基线对照证据） |

## 6. 冻结声明与交接

- **`0.6.0-rc.1` 冻结为 v0.6.0 内部开发完成版**：候选代码 = `578db27`；`6f1e87a` 起仅文档/报告/门禁修复提交。
- 不切换公开 updater 稳定通道（latest 语义保持 rc.4 状态不变；0.6.0 从未发布公开资产）。
- 安装包 / manifest / qa-evidence / release-check 全部绑定冻结提交生成。
- **已交接 v0.7.0**：[v0.7.0 交接包](./v0.6.0-handoff-to-v0.7.0-20260821.md)（DTO 示例、事件字典、重连 fixture、权限/路径/Git 快照约束、project/workspace 解析服务、flag 与 telemetry 报告），对接 [v0.7.0 开发计划书](../v0.7.0/v0.7.0-development-plan-20260820.md)。
