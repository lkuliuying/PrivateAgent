# PrivateAgent v0.7.0 外部验收修复：2×P0 + 2×P1 处置

> 处置日期：2026-08-21
> 验收结论：0.7 暂不通过（代码审查 2 个 P0、2 个 P1；自动化门禁 14/14 全绿）
> 处置范围：4 项阻断问题全部修复 + 12 个回归测试 + 全套/全量门禁复核
> 复核状态：验收修复后 v0.7.0 全套 **126 passed**（114 + 12 新增）

## 0. 第二轮验收修复（2026-08-21 复核：边界未封死）

第二轮外部验收：主路径修复有效但边界未封死，仍 2 个 P0 + 2 个 P1；
完整发布门禁 13 passed / 1 failed（性能 E2E p95=54ms > 50ms 临界）。处置：

| 编号 | 问题 | 修复 | 回归测试 | 状态 |
|---|---|---|---|---|
| P0-1 | 命令边界仍可绕过：`C:outside`（drive-relative，盘符后无斜杠）与 `--basetemp=C:outside` 被接受；`-p`/`--pyargs` 等工作区外模块加载参数不受路径特征约束 | ① 盘符正则收紧为 `^[A-Za-z]:`（drive-relative 一律拒绝）；② 新增命令感知 argv schema（`_COMMAND_ARG_POLICY`）：pytest 的 `-p`/`--pyargs`/`--basetemp`/`--rootdir`/`--confcutdir` 一律拒绝（模块/插件/临时目录/收集根加载能力）；cargo `--manifest-path`、npm `--prefix` 值必须 workspace 内（等号或下一 token）；③ `_command_key` 识别 pytest（含 python -m pytest / uv run pytest，忽略 Windows .exe/.cmd/.bat 后缀） | `test_command_drive_relative_and_module_load_args_rejected`（7 种越界 + 2 种合法） | ✅ |
| P0-2 | 本地 profile 可连接远程 Ollama：`_model_gateway_for_run` 用全局 `ollama_base_url`，适配器允许任意 HTTP(S) 主机（如 `https://ollama.example.com`），快照仍记录 no_send | 本地 profile 路由前校验 `ollama_base_url` 主机必须 loopback（localhost/127.0.0.1/::1/0.0.0.0）；非本地 → ModelProfileUnsupported 失败关闭（不静默连接，快照 no_send 保持真实） | `test_local_profile_rejects_remote_ollama_base_url`（远程拒绝 + loopback 正常） | ✅ |
| P1-1 | PatchSet DB 终态失败恢复不完整：只捕获 PatchSetStateConflict，普通 SQL/commit 异常冒泡，磁盘已改、状态未落盘 | `_handle_db_terminal_failure`：DB 终态任意异常 → 先逆序回滚磁盘（备份仍在）→ 回滚成功尽力标记 rolled_back、失败尽力标记 partial_unknown；DB 完全不可用时磁盘已恢复（或残留备份可人工处置），不再有「磁盘已改 + previewed」幽灵状态 | `test_apply_db_terminal_failure_rolls_back_disk`（模拟 SQL 异常：磁盘回原状 + 状态 rolled_back + 零 .pa 残留） | ✅ |
| P1-2 | Windows 路径规范化不足：仅 lower()，`Readme.md` 与 `README.MD.`（尾随点）仍同时接受，Windows 上可能同一文件 | `_windows_normalize`：逐组件去除尾随点/空格后 lower（与设备名规则同一规范化语义）；唯一性键改用规范化结果 | `test_path_uniqueness_trailing_dot_space_normalized`（4 组尾随点/空格冲突 + 无冲突正常） | ✅ |

第二轮复核：验收修复回归 **16 passed**（12 + 4 新增）；v0.7.0 全套 **130 passed**；
全量 pytest 见提交后门禁重跑；ruff All checks passed。

### 0.1 第三轮验收修复（2026-08-21 复核：等价绕过）

第三轮外部验收：直接样例已修复但存在等价绕过，仍 2×P0 + 2×P1；完整发布门禁
13 passed / 1 failed（性能 E2E p95=51ms 临界）。处置：

| 编号 | 问题 | 修复 | 回归测试 | 状态 |
|---|---|---|---|---|
| P0-1 | 命令 schema 可嵌套绕过：`--override-ini=pythonpath=C:outside`、`-o addopts=-p ...` 注入、`uv run --offline pytest -p ...`、`python -I -m pytest --pyargs ...` 等 wrapper 变体 | ① pytest 改**安全参数 allowlist**（`_PYTEST_ALLOWED_FLAGS`：未列入即拒绝——`-p`/`--pyargs`/`-o`/`--override-ini`/`--addopts`/`-c`/`--config`/`--basetemp`/`--rootdir`/`--confcutdir`/`--import-mode` 全部不在集合）；② `_command_key` 规范化解析 wrapper（`uv run [选项] pytest`、`python [选项] -m pytest`，`-m` 作为定位符不跳过）；③ wrapper 选项（--offline/-I）不在 profile 前缀也不在 allowlist → 拒绝（模型不能注入 wrapper 选项） | `test_command_nested_bypass_rejected`（9 种嵌套/变体拒绝 + 2 种合法） | ✅ |
| P0-2 | loopback 请求仍继承远程代理：httpx 客户端默认 trust_env=True，HTTP_PROXY 会把 127.0.0.1 请求送往远程代理（正文离开本机，快照仍 no_send） | `_HttpAdapter` 支持 `trust_env` 参数；`OllamaChatAdapter` 默认 `trust_env=False`（本地服务不走环境代理）且新增 `require_loopback` 构造校验（本地 profile 路由传 True，与 routes 层双保险） | `test_ollama_adapter_bypasses_proxy_and_requires_loopback` + `test_ollama_adapter_client_trust_env_false`（HTTP_PROXY 设置下 trust_env=False + 请求体捕获） | ✅ |
| P1-1 | post-commit 失败反向分叉：transition_status 先 commit 再查询，commit 成功而查询失败被当成持久化失败 → 回滚磁盘 → DB=applied、磁盘=旧内容 | `_handle_db_terminal_failure` 补偿前先经**新事务确认 durable 状态**（async_session_factory 查询）：已 applied → 按成功路径收敛（补发事件/投影，绝不回滚磁盘）；仍 previewed → 才回滚磁盘并尽力落 rolled_back/partial_unknown | `test_apply_post_commit_query_failure_keeps_disk`（commit 成功 + 查询失败：磁盘保持新内容 + DB applied + 零 .pa 残留） | ✅ |
| P1-2 | 规范化后为空的组件形成别名：`src/.../config.py`（`...` rstrip 后为空）与正常路径解析到同一文件，唯一性键不同 | `_validate_rel_path` 逐组件按 Windows 规范化后检查：为空 / 折叠为 `..` / 折叠为 `.` 一律拒绝（`...`、`.. `、`. ` 全部拒绝） | `test_path_component_collapses_to_empty_rejected`（4 种形态拒绝 + 合法路径正常） | ✅ |

第三轮复核：验收修复回归 **21 passed**（12 + 4 + 5 新增）；v0.7.0 全套 **135 passed**；
全量 pytest 与完整门禁以提交后复核为准；ruff All checks passed。

### 0.2 第四轮验收修复（2026-08-21 复核：@argsfile 与内联值绕过）

第四轮外部验收：Ollama 本地数据外发（trust_env=False + loopback）**已确认关闭**，
仍 2×P0 + 2×P1；完整发布门禁 13 passed / 1 failed（性能 E2E p95=66ms）。处置：

| 编号 | 问题 | 修复 | 回归测试 | 状态 |
|---|---|---|---|---|
| P0-1 | pytest `@argsfile` 绕过 allowlist：`pytest @args.txt` 在校验后展开文件，可重新注入 `-p`/`--override-ini` 等禁用能力 | 位置参数以 `@` 开头一律拒绝（response file 展开面关闭；`_reject_command_args` 与 `_reject_generic_args` 两路接入） | `test_command_argsfile_and_double_dash_rejected`（@args.txt / @C:outside 拒绝） | ✅ |
| P0-2 | npm/cargo 未知 flag 的内联值未校验：`npm run build -- --config=../outside/vite.config.ts`（-- 透传底层工具）、`npm test -- --config=C:outside`、`cargo test --target=C:outside` | ① `--` 分隔符一律拒绝（npm run 透传 vite/vitest 等底层工具参数的入口关闭）；② cargo/npm 补齐完整 allow/path/reject schema（cargo：`--target`/`--target-dir`/`--manifest-path` 值 workspace 内；npm：`--prefix`/`--cache`/`--config` 值 workspace 内；未知 flag 与 pytest 同规则拒绝）；③ path flag 不在 allow 检查范围（值校验语义） | 同上（--config/--target/--target-dir/--cache 越界值拒绝；cargo -p/--offline、npm --if-present 合法） | ✅ |
| P1-1 | DB 状态不可判定时仍错误回滚：durable 查询失败被置 None 后回滚磁盘，commit 实际成功 + DB 暂不可达 → DB=applied、磁盘=旧内容 | `_handle_db_terminal_failure` 三态化：durable 查询异常（不可判定）→ **保留磁盘与备份（绝不回滚）**、尽力落 partial_unknown（人工处置态）、返回 unknown → apply 抛 `patchset_partial_unknown`；确认 applied → 成功路径收敛；确认 previewed → 才回滚 | `test_apply_durable_unknown_keeps_disk_and_backup`（DB 不可达：磁盘保持新内容 + 备份保留 + partial_unknown） | ✅ |
| P1-2 | 重复斜杠路径别名：`a/b` 与 `a//b`（PurePosixPath 折叠空组件但唯一性键保留双斜杠）解析到同一文件 | `_windows_normalize` **跳过空组件**——`a//b` 与 `a/b` 唯一性键一致（判重复） | `test_path_uniqueness_double_slash_normalized`（create 冲突 + rename 目标冲突 + 合法路径正常） | ✅ |

第四轮复核：验收修复回归 **24 passed**（12 + 4 + 5 + 3 新增）；v0.7.0 全套 **138 passed**；
全量 pytest 与完整门禁以提交后复核为准；ruff All checks passed。

### 0.3 第五轮验收修复（2026-08-22 复核：SAFE 泄漏与执行层约束）

第五轮外部验收：Ollama loopback/proxy 与 PatchSet 模糊提交状态**已确认闭环**，
仍 3×P0 + 1×P1；完整发布门禁 14/14（E2E 性能用例 1 flaky）。处置：

| 编号 | 问题 | 修复 | 回归测试 | 状态 |
|---|---|---|---|---|
| P0-1 | workspace 的 SAFE 是工具级而非 profile 级：项目所有 profile 标 safe → 命令工具整体免确认，但执行阶段仍接受未匹配 profile 的全局白名单命令（扩大自动执行范围） | `permission_mode` 透传到 `_resolve_command`（dispatcher → registry → spec → executor）：workspace 模式且未匹配项目 profile → 拒绝（E0 §4.1 自动允许范围 = 匹配 profile 的命令；SAFE 不再泄漏到全局白名单兑底） | `test_workspace_mode_unmatched_profile_rejected`（匹配 profile 自动允许 / 未匹配拒绝 / 非 workspace 兑底保留） | ✅ |
| P0-2 | 命令参数仍可绕过：`pytest -W ignore::outside_plugin.CustomWarning`（警告类别触发外部模块导入）；safe profile 前缀为 python 时可追加 `-c "__import__(...)"`（未知工具只查路径） | ① pytest allowlist 移除 `-W`（未列入即拒绝）；② python/py 新增 schema（`-c`/`-m`/`-i`/`-W` 一律拒绝，`-V/-B/-E/-O/-q/-u/-v` 允许）；`_command_key` 纯 python 调用归 python 工具 | `test_pytest_w_flag_and_python_code_args_rejected`（-W/-c/-m/-i 拒绝；python -B script.py 合法） | ✅ |
| P0-3 | `allow_network=False` 没有执行层约束：字段只被保存，未参与命令解析或进程隔离 | `_reject_network_args`：profile 匹配且 `allow_network=False`（默认）时，剩余参数中 URL 形态（`scheme://`）一律拒绝（直连 URL 拦截；代理类 env 已由 env_allowlist 拒绝） | `test_allow_network_false_rejects_url_args`（URL 参数拒绝 / allow_network=True 放行 / 无 URL 不受影响） | ✅ |
| P1-1 | Windows 路径别名未完全收敛：`src/./x.py` 与 `src/x.py` 指向同一文件但唯一性键不同（PurePosixPath 折叠 `.` 而 split 保留） | `_windows_normalize` 跳过规范化后为空的组件（`.` rstrip 后为空，与第四轮空组件规则合并）——`src/./x.py` → `src/x.py` 判重复 | `test_path_uniqueness_dot_component_normalized`（src/./x.py 冲突 + ./a.py 冲突） | ✅ |

第五轮复核：验收修复回归 **28 passed**（12 + 4 + 5 + 3 + 4 新增）；v0.7.0 全套 **142 passed**；
全量 pytest 与完整门禁以提交后复核为准；ruff All checks passed（含 cargo/npm `-q` 简写补入
allowlist、durable 测试 coroutine 警告修复）。

### 0.4 第六轮验收修复（2026-08-22 复核：网络隔离本质与执行路径统一）

第六轮外部验收：pytest -W、python -c/-m、未匹配全局命令、`./` 路径碰撞与异步测试
警告**已确认关闭**；仍 3×P0 + 1×P1（网络隔离本质 / 未知工具代码执行 / 前缀策略遮蔽 /
手动运行路由绕过）。处置：

| 编号 | 问题 | 修复 | 回归测试 | 状态 |
|---|---|---|---|---|
| P0-1 | allow_network=False 不是真正的网络隔离：URL 正则无法覆盖 `example.com`、IP:port、profile 固定前缀 URL、脚本内 socket | workspace SAFE 仅当**全部 profile safe 且全部 allow_network=True**；存在 allow_network=False → CONFIRM（禁止此类 profile 自动执行，网络行为由人工审批把关；argv 正则保留作纵深防御） | `test_workspace_safe_requires_allow_network_all`（allow_network=False → CONFIRM / 全 True → SAFE） | ✅ |
| P0-2 | 未知 safe profile 可扩展成任意代码执行（node -e、PowerShell -EncodedCommand、uv 等） | **safe profile 的未知工具（无完整审计 schema）必须精确 argv**——剩余参数非空即拒绝；pytest/python/cargo/npm（有 schema）仍按 allowlist 校验；confirm profile 保持审批把关 | `test_safe_unknown_tool_requires_exact_argv`（node -e 拒绝 / node tool.js 精确 argv 允许 / confirm 追加参数允许） | ✅ |
| P0-3 | 前缀匹配策略遮蔽：created_at 倒序 + 首个匹配，后创建宽泛 safe 可遮蔽具体 restricted | **最长前缀优先，同长度取最严格 risk**（restricted > confirm > safe），遍历全部 enabled profile 后择优 | `test_profile_prefix_longest_match_strictest`（宽泛 safe 不遮蔽 restricted / 未命中 restricted 落到 safe） | ✅ |
| P1-1 | 手动运行 profile 的 API 走旧执行器：忽略 project_id、绕过 argv/网络校验、Job Object 进程树清理与流式有界输出 | ① run 端点校验 `profile.project_id == project_id`（跨项目 404）；② `CommandProfileService.run` 统一走 `run_whitelisted_command_trusted`（受审计路径：_resolve_command 校验 + Job Object + 流式有界输出 + parsed/profile_version） | `test_command_run_route_rejects_cross_project`（跨项目 404 / 同项目执行成功） | ✅ |

第六轮复核：验收修复回归 **32 passed**（12 + 4 + 5 + 3 + 4 + 4 新增）；v0.7.0 全套 **146 passed**；
全量 pytest 与完整门禁以提交后复核为准；ruff All checks passed（含 workspace safe 既有测试
按新语义补 allow_network=True）。

## 1. P0-1：模型 profile 没有驱动实际 Provider

**问题**：Run 选择的 `model_profile_id` 只被校验和保存，执行仍使用全局
Provider；`reasoning_effort` 未生效；权限快照固定声明
`remote_provider_data_policy=no_send`——若全局启用远程 Provider，即使选择
本地 profile，也可能把工作区上下文发送到远程。

**修复**：

| 位置 | 变更 |
|---|---|
| `api/routes_agent_runs.py` | 新增 `_model_gateway_for_run(db, run)`：按 run 绑定的 `model_profile_id` 解析实际 ModelClient——本地 profile（provider=ollama / is_local）强制 Ollama 本地路由（绝不因全局远程设置发送远程）；远程 profile（openai/claude）要求全局 `remote_provider_enabled`（否则 422 fail-fast），secret 仍从全局原生凭据读取；profile 删除/禁用/能力变化 → 失败关闭，不静默回退全局 |
| 同上（create 端点） | 创建校验加强：远程 provider 需全局启用（422 `model_profile_unsupported`）；provider 白名单（ollama/openai/claude）；`reasoning_effort` 必须落在 profile 声明集合（422）；coordinator.start 传入按 run 解析的 model 与 reasoning_effort |
| 同上（approve 端点） | resume 改用 `_model_gateway_for_run`（不再用全局注入）；传 `run.reasoning_effort` |
| 同上（快照） | `remote_provider_data_policy` 按实际路由：本地 → `no_send`（真实）；远程启用 → `send`（如实声明） |
| `agents/contracts.py` | `ModelRequest` 增加 `reasoning_effort`（additive，≤32 字符） |
| `agents/runtime.py` | `AgentRuntime` 构造接受 `reasoning_effort` 并透传到每个 `ModelRequest` |
| `agents/coordinator.py` | `start`/`resume`/`_execute` 传递 `reasoning_effort` 到两处 runtime 构造 |
| `llm/adapters.py` | OpenAI 系请求体透传 `reasoning_effort`（o 系列参数；Ollama/Claude 不透传，本地模型无此概念） |

**回归测试**（`tests/test_v070_acceptance_fixes.py`）：
`test_local_profile_forces_local_route_even_with_remote_enabled`（本地 profile +
全局远程启用 → provider_name=ollama）、`test_remote_profile_run_creation_rejected_without_remote`
（422 fail-fast）、`test_remote_profile_drives_remote_route_and_snapshot_send`
（coordinator 收到远程 gateway + 快照 send）、
`test_reasoning_effort_validated_and_passed_to_coordinator`（集合校验 + 透传）、
`test_openai_adapter_payload_carries_reasoning_effort`（请求体字段）、
`test_profile_deleted_after_create_fails_closed`（resume 失败关闭）。

## 2. P0-2：项目命令可通过后缀参数越出 workspace

**问题**：白名单和 profile 都只比较 argv 前缀；`cargo test --manifest-path
C:/outside/Cargo.toml`、`npm run build --prefix C:/outside`、`python -m pytest
C:/outside/test_payload.py` 均被判定为白名单；项目 profile 全 safe 时命令
工具免确认执行。

**修复**（`core/command_workflow.py` + `core/code_tools.py`）：

- 新增 `_reject_out_of_workspace_args(root, remaining)`：前缀之后剩余参数中
  路径特征 token 必须解析在 workspace 内——盘符/绝对路径/UNC 直接拒绝；
  `..` 上级引用与相对子路径经 `resolve_within` 校验（工作区内引用允许）；
  `--flag=path` 等号形式取 value 检查。
- `_resolve_command` 匹配 profile 或全局白名单后，对剩余参数执行校验；
- `code_tools.py` 新增 `whitelisted_prefix_length`（`is_whitelisted_command`
  改为其包装，行为不变）。

**回归测试**：`test_command_profile_rejects_out_of_workspace_args`（验收三案例
+ 等号形式 + `..` 越界全部拒绝；`./Cargo.toml` 工作区内允许）、
`test_global_whitelist_remaining_args_guarded`（全局白名单剩余参数同样受控）。

## 3. P1-1：PatchSet 在持久化终态前删除回滚备份

**问题**：文件已提交并验证后，代码先清理 `.pa-bak` 再写 DB `applied`；若 DB
提交失败，留下「磁盘已修改、PatchSet 仍 previewed/pending」的幽灵状态且无法
自动回滚。

**修复**（`core/patch_set_service.py`）：成功路径改为**先持久化终态**
（`set_all_files_status(applied)` + `transition_status(previewed→applied)`，
内部 commit），DB 提交成功后再清理临时文件/备份；清理失败仅记日志（残留
`.pa-bak` 无害，状态已 applied 可解释）。失败/回滚路径原本就是先 DB 后清理，
未改动。

**回归测试**：`test_apply_persists_terminal_state_before_backup_cleanup`
（monkeypatch `transition_status` 断言执行时备份尚在，证明顺序）、
`test_apply_cleanup_failure_does_not_block_terminal_state`（清理失败不阻断
applied 终态）。

## 4. P1-2：Windows 设备路径校验不完整

**问题**：`nul.txt`/`CON.md`/`com1.json` 被当前校验接受（只比较完整 part）；
路径唯一性未按 Windows 大小写规则归一化。

**修复**（`core/patch_set_service.py`）：

- 新增 `_windows_device_name(part)`：按 MSDN 命名规则提取——去除扩展名
  （第一个 `.` 之前）、去除尾随点与空格后 lower，再与设备名集合比较；
  `nul.txt`/`CON.md`/`com1.json`/`con.`/`con ` 全部拒绝，`console.txt`/
  `nulll.txt` 允许。
- `_check_path_uniqueness`：唯一性键改为 `path.lower()`（Windows 大小写
  不敏感），`Readme.md` 与 `readme.md`、rename 目标大小写变体均判重复。

**回归测试**：`test_windows_device_names_with_extension_rejected`、
`test_path_uniqueness_case_insensitive`。

## 5. 门禁复核

| 门禁 | 结果 |
|---|---|
| 验收修复回归（`tests/test_v070_acceptance_fixes.py`） | **12 passed** |
| v0.7.0 全套（`tests -k v070`） | **126 passed**（114 + 12 新增，零回归） |
| 全量 pytest | 见验收复核记录（提交后 release-check-full 重跑） |
| ruff / compileall | 修复后复核通过 |

## 6. 修复记录（实现要点）

- `coordinator._execute` 补 `reasoning_effort` 参数（首次接线遗漏，回归测试
  捕获 NameError）。
- `ModelProfile` 属性名为 `reasoning_efforts_json`（routes 校验引用修正）。
- 测试库全局 settings 残留（`remote_provider_enabled` 等）污染后续用例：
  新增 `_reset_provider_settings` 前后清理；跨 session 断言沿用独立
  session 查询（MySQL REPEATABLE READ）。
- `SettingsService.update` 的 provider secret 只接受引用值（原生凭据边界），
  测试写 `secret://os-keyring/provider/openai` 引用而非明文。
