# PrivateAgent v0.7.0-rc.1 检查点（RC 与开发完成检查点 · 内部开发完成版冻结）

> 制定日期：2026-08-21
> 绑定提交：本文档提交（rc.1 检查点，版本 bump 0.7.0-rc.1 8 处一致）
> 前置：E0 契约冻结（5729364 内）→ E1 PatchSet（5729364）→ E2 命令（5729364）→
> E3 Artifact（5729364）→ E4 权限与模型 profile（137530f）→ E5 Coding E2E 与恢复（67cb9bf）
> → E6 RC 与开发完成检查点（本文档）
> 版本主题：可信编码执行 MVP（[开发计划书](./v0.7.0-development-plan-20260820.md)）
> 交付链路：`0.7.0-alpha.1 → 0.7.0-alpha.2 → 0.7.0-beta.1 → 0.7.0-rc.1 → 0.7.0`（内部）

## 1. 里程碑完成矩阵（E0 → E6）

| 里程碑 | 交付 | 退出条件 | 状态 |
|---|---|---|---|
| E0 契约与威胁模型 | 契约冻结（patch_set.* 事件 additive、权限三模式替换、模型 profile、命令 profile 版本化、完成条件扩展、flags 全关）、威胁清单 T1–T12、失败契约测试 xfail 先行 | 高风险能力默认关闭；不存在绕过 ToolSpec/approval/execution/verifier 入口 | ✅ 完成（5729364 内） |
| E1 PatchSet 预览与原子应用 | `coding_patch_sets`/`files` 表（迁移 0030）、propose/apply 工具、统一 Diff、T3–T7/T11 防护、回滚 manifest + partial_unknown、PatchSetResultVerifier 磁盘复核 | 未审批零写入；成功写入全部 verified；冲突失败无未解释部分状态；unknown 不自动重放 | ✅ 完成（5729364，22 用例） |
| `0.7.0-alpha.1` | PatchSet 契约/单元/数据库/文件系统故障测试通过；不更新正式 updater | 同左 | ✅ 通过（5729364） |
| E2 项目命令与结果解析 | 11 种冻结解析器、profile 版本化 + 字段校验（cwd_rel/env_allowlist/result_parser/risk_level/max_output_bytes）、执行落地、run 快照命令 profile 版本 | 非白名单 argv/cwd/env 全部拒绝；取消/超时/退出后残留进程为 0；大输出不阻塞不无限增长 | ✅ 完成（5729364，22 用例） |
| E3 Artifact 与完成条件 | kind 扩展 6 种（共 11）、分页 API、durable 事件投影（即时 + 终态重建幂等）、完成条件三扩展（must_pass_command_profiles/no_pending_patchsets/final_git_diff） | Artifact 可恢复、可分页、可脱敏；未满足完成条件 run 不得 completed | ✅ 完成（5729364，15 用例） |
| `0.7.0-alpha.2` | 「修改→测试→验证→报告」后端主链 E2E（真实 git+pytest）；审批/取消/失败/重启恢复 fixture 冻结 | 同左 | ✅ 通过（5729364） |
| E4 权限与模型 profile | 三模式（readonly/confirm/workspace）+ run 权限快照、模型 profile 能力 API（flag 门控、secret 原生边界）、restricted 执行时拦截、dispatcher 按模式注册、解除 2 个 xfail | 权限模式可解释且默认最小；profile 变化不修改历史 run；模型能力不通过名称猜测 | ✅ 完成（137530f，24 用例） |
| E5 Coding E2E 与恢复 | 真实项目七步闭环（read→plan→modify→test→report）、故障注入（冲突/HEAD 漂移）、审批过期、重启恢复、flag 独立回退矩阵 | 一个真实任务可以可信完成；失败路径无重复副作用和幽灵状态 | ✅ 完成（67cb9bf，7 用例） |
| `0.7.0-beta.1` | v0.7.0 全套门禁通过；beta evidence/manifest/flags/已知问题生成（E4–E5 交付文档） | 同左 | ✅ 通过（67cb9bf） |
| E6 RC 与开发完成检查点 | 契约冻结核对（test_v070_coding_contracts.py 全绿）、版本 bump `0.7.0-rc.1`（8 处一致）、release-check-full 14/14、rc.1 检查点（本文档）、v0.8.0 交接包 | 见第 2–4 节 | ✅ 完成（本文档） |

## 2. 门禁（机器报告口径）

### 2.1 release-check-full（`dist/release-check-0.7.0-rc.1.json` / `.md`，三轮）

| 门禁 | 第一轮 | 第二轮 | 第三轮（验收修复后，绑定 84cdd09） |
|---|---|---|---|
| pytest 全量 | **941 passed** | **941 passed** | **953 passed**（+12 验收修复用例） |
| ruff check（src/tests/scripts） | ❌ 3 处 F401 未使用导入 | ✅ All checks passed（已修复） | ✅ All checks passed |
| compileall | ✅ | ✅ | ✅ |
| npm build（vue-tsc + vite build） | ✅ | ✅ | ✅ |
| npm test（Vitest） | ✅ | ✅ | ✅ |
| npm e2e（Playwright） | ❌ 51/52：性能预算用例 p95=73ms（预算 50ms） | ✅ **51 passed / 1 flaky**（首跑 63ms → 重试通过，K1） | ✅ **51 passed**（性能用例首跑通过） |
| cargo check / cargo test | ✅ / ✅ | ✅ / ✅ | ✅ / ✅ |
| sidecar smoke | ✅ PASS（/health 200） | ✅ PASS | ✅ PASS |
| alembic current | ✅ schema `0030` | ✅ schema `0030` | ✅ schema `0030` |
| git diff check / docker compose / diagnostic redaction / latest.json | ✅ | ✅ | ✅（latest.json 保持 0.6.0-rc.1，预发布检查点不更新正式渠道） |
| **机器口径** | ok=False（ruff + e2e） | passed=14 failed=0 skipped=0；ok=False 仅因预期项 | **passed=14 failed=0 skipped=0；worktree_dirty=False；ok=False 仅剩 2 个设计内预期项**（installer_missing / evidence_not_bound） |

### 2.2 ok=False 的 3 个 release_gate_issues（全部为预期项，无 P0/P1）

| issue | 性质 | 处置 |
|---|---|---|
| `worktree_dirty` | 门禁运行时版本 bump/修复/文档未提交 | 验收修复提交 84cdd09 后消除（第三轮门禁 worktree_dirty=False） |
| `installer_missing` | 0.7.0 为内部开发完成版，**不切公开 updater、不构建安装包**（计划 E6 节） | 设计内预期；升级回退演练证据沿用 v0.6.0 rc.1（0026→0029 链 ALL PASS），v0.7.0 迁移 head=0030 additive |
| `evidence_not_bound` | 无安装包故无签名/QA evidence 绑定 | 同 installer_missing，设计内预期 |

### 2.3 ruff 修复记录（第一轮门禁捕获）

| 文件 | 修复 |
|---|---|
| `src/personal_assistant/core/patch_set_service.py` | 删除未使用 `import uuid`（F401） |
| `tests/test_v070_alpha2_gate.py` | 删除未使用 `CancellationToken` 导入（F401） |
| `tests/test_v070_e5_gate.py` | 删除未使用 `routes_agent_runs` 导入（F401） |

全部为纯删除未使用导入，无行为变化（两处门禁测试 12 passed 复核）。

## 3. 已知问题（冻结时点）

| 编号 | 问题 | 影响与处置 |
|---|---|---|
| K1 | e2e 性能预算用例（`performance-resource.spec.ts:205`「页面切换：无 50ms 级长任务」）在门禁首跑 p95=63–73ms（预算 50ms） | **延续 v0.6.0 K2（环境漂移）**：① v0.7.0 对 apps/desktop 生产运行时零变化（`git diff 67cb9bf -- apps/desktop/src src-tauri/src e2e` 为空）；② 该用例自 0.4.0 起预算临界，v0.6.0 已做基线对照实验（rc.4 冻结提交同机同窗同样失败）；③ 本机负载持续偏重（wallpaper64 动态壁纸 CPU 12719s、QoderCN 1.4GB、双 msedge/ChatGPT、mysqld 798MB），单独重跑 p95=67–72ms 同样超预算；④ 第二轮门禁重试通过（flaky 吸收），51 passed。不修改性能预算与测试逻辑，不构成 P0/P1 |
| K2 | 迁移链 head 由 0029 → 0030（coding_patch_sets/coding_patch_set_files/model_profiles 新表 + kind ENUM 扩展）；0.6.0 版应用无法识别 0030 | 与 v0.6.0 K1 同型已知限制：schema/数据零破坏（additive）；0.7.0 不切公开 updater，回退演练按 v0.6.0 rc.1 先例（重装 0.7.0 恢复启动） |

无未处置 P0/P1。

## 4. 完成定义逐条核对（计划书 §12）

| # | 完成定义 | 证据 | 状态 |
|---|---|---|---|
| 1 | 真实项目可以完成读取、计划、修改、测试和报告闭环 | `test_e5_full_loop_read_plan_modify_test_report`：真实 git 仓库 + 真实 pytest，read_code_file（confirm 审批）→ update_run_plan（plan.created）→ propose/apply 多文件 PatchSet（双审批）→ 真实 pytest（py-tests profile）→ 五类终态 Artifact（patch_preview/patch_applied/command_result/test_report/final_report，`completion_satisfied=True`）；run completed、apply verified=true、processes_remaining=0、磁盘回读存在、零 .pa 残留 | ✅ |
| 2 | PatchSet 多文件操作原子、可审查、可验证 | 四类操作（create/update/delete/rename）统一 Diff 可审查（preview 零写入 + truncated 硬拒绝 + parameters_hash 强校验）；原子应用（全量准备→提交 rename→create→update→delete→回读验证）；回滚 manifest 逆序回滚；成功全部 verified（PatchSetResultVerifier 磁盘复核不信任模型文本）；冲突/HEAD 漂移/部分状态全部失败关闭或 partial_unknown | ✅ |
| 3 | 项目命令受 profile、权限、超时和进程树边界约束 | 白名单 profile（argv 数组不经 shell、cwd_rel resolve_within、env_allowlist 拒绝凭据类、result_parser 冻结 11 种）；restricted 执行时拦截（不因模式切换自动获批）；Job Object 进程树清理（processes_remaining==0）；超时与 max_output_bytes 截断（truncated 标记）；workspace 模式 risk 动态化 | ✅ |
| 4 | Artifact 和完成条件均有 durable 事实 | artifact.created + 5 个 patch_set.* durable 事件（(run_id, sequence) 严格递增，payload 有界脱敏）；投影从 executions/patch_sets durable 事实重建且幂等；完成条件（must_succeed_tools/require_verified/must_pass_command_profiles/no_pending_patchsets/final_git_diff）由 WorkflowCompletionVerifier 基于事实求值，未满足 run 不得 completed | ✅ |
| 5 | 权限和模型 profile 契约稳定 | `PERMISSION_MODES={readonly, confirm, workspace}`（read_only/full_access 422 拒绝）；每次 run 权限快照（只存非秘密摘要、历史不随 profile 变化）；模型 profile 能力显式声明不通过名称猜测；native_tool_calls=False → 422 不进入 Coding 循环；契约测试全绿（test_v070_coding_contracts.py） | ✅ |
| 6 | 取消、冲突、拒绝、重启和 unknown 状态可恢复 | 冲突 → patchset_conflict 失败关闭（磁盘保持外部编辑）；HEAD 漂移 → git_snapshot_failed 零写入（patch_set 保持 previewed 人工重预览）；拒绝/取消 → rejected/cancelled 可解释终态；审批过期 → ToolApprovalExpiredError 零消费、人工取消唯一收敛、不自动重放；重启 → reconcile 不碰 waiting_approval、批准续跑全链恢复、崩溃后终态 Artifact 重建幂等；partial_unknown 永不自动重放 | ✅ |
| 7 | `0.7.0-rc.1` 即时完整门禁通过且无 P0/P1 | release-check-full **14/14 全绿**（pytest 941 / ruff / compileall / npm build+test / e2e 51 passed 1 flaky / cargo / sidecar / alembic 0030 / git diff / compose / redaction / latest.json）；ok=False 仅 3 个预发布检查点预期项（§2.2）；K1/K2 均已处置 | ✅ |

## 5. 冻结声明与交接

- **`0.7.0-rc.1` 冻结为 v0.7.0 内部开发完成版**：候选代码 = 本文档提交（版本 bump 8 处一致
  0.7.0-rc.1 + ruff 3 处修复 + 交接包 + 检查点）；此前里程碑链：`5729364`（E0–E3 + alpha.1/alpha.2）
  → `137530f`（E4）→ `67cb9bf`（E5/beta.1）。
- **不切换公开 updater 稳定通道**（latest.json 保持 0.6.0-rc.1 不变；0.7.0 从未发布公开资产），
  与 v0.6.0 rc.1 冻结口径一致。
- **已交接 v0.8.0**：[v0.8.0 交接包](./v0.7.0-handoff-to-v0.8.0-20260821.md)（Coding 主链 DTO/
  事件样本、PatchSet/Diff/命令/Artifact fixture、权限和模型 profile 列表、空态/运行/审批/失败/
  取消/重连/完成状态矩阵、不得展示的敏感字段与隐藏推理清单、错误码全集）。
- 回退性（计划 §10）：四类编码 flag 独立默认关闭（关闭 PatchSet 保留单文件可信工作流与只读
  工具；关闭 Artifact UI 不删除 execution 事实）；应用回退保留新表与 profile，无破坏性
  downgrade；partial/unknown 只能人工处置，不能通过回退自动重跑。
- 范围外（计划 §3.2）：未做 Coding Workbench 全面上线、自动 Git worktree 创建、任意目录递归
  删除、二进制 Patch、交互式长期终端、自动 PR/commit/push/部署/发布、HTTP/SQL 工作流 UI 重做。

## 6. 测试与证据索引

- 契约：`tests/test_v070_coding_contracts.py`（E0 冻结 + 2 个已解除 xfail）。
- v0.7.0 全套门禁：`tests -k v070` = **126 collected**（contracts/patchset/commands/artifacts/
  permissions/alpha2/e5/acceptance-fixes），验收修复后全绿；rc.1 全量以本检查点为准。
- 全量回归：pytest 全量 **953 passed / 2 skipped**（941 + 12 验收修复用例，零失败）。
- 门禁报告：`dist/release-check-0.7.0-rc.1.json` / `.md`（第二轮 14/14）。
- 里程碑文档：`docs/releases/v0.7.0/` 下 v0.7.0-e0-contracts / e1-patchset / e2-commands /
  e3-artifacts / e4-permissions / e5-e2e-recovery / alpha2-gate / acceptance-fixes /
  development-plan / handoff-to-v0.8.0 / 本文档。

## 7. 外部验收复核（2×P0 + 2×P1 处置）

外部验收结论（2026-08-21）：**0.7 暂不通过，不建议冻结或发布**（自动化门禁 14/14 全绿，
代码审查发现 2 个 P0、2 个 P1）。处置见
[验收修复记录](./v0.7.0-acceptance-fixes-20260821.md)，摘要如下：

| 编号 | 问题 | 修复 | 回归测试 | 状态 |
|---|---|---|---|---|
| P0-1 | 模型 profile 没有驱动实际 Provider（执行仍用全局 Provider；reasoning_effort 未生效；快照固定 no_send） | `_model_gateway_for_run` 按 run 的 model_profile_id 解析（本地 profile 强制 Ollama 本地路由、远程 profile 需全局启用否则 422 fail-fast、profile 删除/禁用 → 失败关闭不静默回退）；reasoning_effort 集合校验 + 透传（ModelRequest/runtime/coordinator/OpenAI payload）；快照按实际路由声明 no_send/send | 6 用例 | ✅ |
| P0-2 | 项目命令可通过后缀参数越出 workspace（cargo --manifest-path / npm --prefix / pytest 绝对路径被前缀白名单放行） | `_reject_out_of_workspace_args`：前缀之后剩余参数路径特征 token 必须解析在 workspace 内（盘符/绝对路径拒绝、`..`/相对子路径 resolve_within 校验、`--flag=path` 取 value）；profile 与全局白名单两路都接入 | 2 用例 | ✅ |
| P1-1 | PatchSet 在持久化终态前删除回滚备份（DB 失败 → 幽灵状态） | 成功路径改为先 DB 终态（transition_status 内部 commit）后清理备份；清理失败仅记日志不阻断 | 2 用例 | ✅ |
| P1-2 | Windows 设备路径校验不完整（nul.txt/CON.md/com1.json 被接受；唯一性未按大小写归一化） | `_windows_device_name` 按 MSDN 规则（去扩展名/尾随点空格后比较）；唯一性键 lower 归一化（Readme.md vs readme.md 判重复） | 2 用例 | ✅ |

修复后复核：验收修复回归 **12 passed**；v0.7.0 全套 **126 passed**；全量 **953 passed / 2
skipped**；ruff All checks passed；2 个既有审批回归（approval resume / chat compat）随 approve
端点兼容修复恢复全绿。

**验收修复后最终门禁（第三轮，绑定 84cdd09）**：passed=14 failed=0 skipped=0，
worktree_dirty=False，pytest 953 passed，e2e 51 passed（性能用例首跑通过），alembic 0030，
sidecar PASS；ok=False 仅剩 2 个设计内预期项（installer_missing / evidence_not_bound——
0.7.0 内部开发完成版不切公开 updater、不构建安装包，安装版 QA smoke 与签名 evidence 不在
本版本范围）。**至此 2×P0 + 2×P1 全部修复并经完整门禁复核；可提请复审冻结。**

### 7.1 第二轮验收复核（2026-08-21：边界未封死）

第二轮外部验收：**仍不通过**——主路径修复有效但边界未封死（2 个 P0 + 2 个 P1），完整发布
门禁 13 passed / 1 failed（性能 E2E p95=54ms > 50ms 临界，单独复跑 50ms 通过）。处置详见
[验收修复记录第 0 节](./v0.7.0-acceptance-fixes-20260821.md)：

| 编号 | 问题 | 修复要点 | 状态 |
|---|---|---|---|
| P0-1 | 命令边界仍可绕过（`C:outside` drive-relative；`-p`/`--pyargs` 模块加载） | 盘符正则收紧 `^[A-Za-z]:`；命令感知 argv schema（pytest 拒绝 `-p`/`--pyargs`/`--basetemp`/`--rootdir`/`--confcutdir`；cargo `--manifest-path`/npm `--prefix` 值 workspace 内） | ✅ |
| P0-2 | 本地 profile 可连接远程 Ollama（全局 ollama_base_url 指向任意 HTTP(S) 主机，快照仍 no_send） | 本地 profile 路由前强制 loopback 主机（localhost/127.0.0.1/::1/0.0.0.0），非本地失败关闭 | ✅ |
| P1-1 | PatchSet DB 终态失败只捕获状态竞争，普通 SQL/commit 异常冒泡 | `_handle_db_terminal_failure`：任意 DB 异常 → 先逆序回滚磁盘（备份仍在）→ 尽力标记 rolled_back / partial_unknown | ✅ |
| P1-2 | Windows 路径仅 lower 去重，`README.MD.` 与 `Readme.md` 仍碰撞 | `_windows_normalize` 逐组件去尾随点/空格后 lower | ✅ |

第二轮复核：验收修复回归 **16 passed**；v0.7.0 全套 **130 passed**；全量 pytest 与完整门禁以
提交后复核为准。**e2e 性能用例（K1 环境漂移）处于预算临界（54ms vs 50ms）**：维持 v0.6.0 先例
（不修改预算/测试；生产运行时零变化证据链完整），门禁重跑以实际结果记录。

### 7.2 第二轮修复后完整门禁（绑定 ceb0114）

| 门禁 | 结果 |
|---|---|
| pytest 全量 | ✅ **957 passed**（953 + 4 第二轮修复用例） |
| ruff / compileall / npm build / npm test / cargo / sidecar / alembic 0030 / git diff / compose / redaction / latest.json | ✅ 全绿 |
| npm e2e | ❌ **50/51**：性能用例 p95=58ms（预算 50ms，重试仍失败）；单独复跑 p95=69–70ms（长任务 1 个）——K1 环境漂移临界 |
| 机器口径 | passed=13 failed=1；worktree_dirty=False；issues 仅 installer_missing / evidence_not_bound（设计内预期） |

K1 判定维持：该用例自 0.4.0 预算临界（50ms），v0.7.0 对 apps/desktop 生产运行时零变化，
多轮门禁 p95 实测 73/63/通过/58/69–70ms 围绕预算随机抖动（本机 wallpaper64 动态壁纸 CPU
12719s+、IDE、双 msedge/ChatGPT、mysqld 798MB 等后台负载）；单独复跑在验收时点 50ms 通过、
本轮 69–70ms——纯环境负载漂移，非回归。不修改预算/测试（v0.6.0 K2 先例）；v0.8.0 交接项：
在真实桌面负载基线重新校准该性能预算。

### 7.3 第三轮验收复核（2026-08-21：等价绕过）

第三轮外部验收：**仍不通过**——直接样例已修复但存在等价绕过（2×P0 + 2×P1），完整发布门禁
13 passed / 1 failed（性能 E2E p95=51ms 临界）。处置详见
[验收修复记录第 0.1 节](./v0.7.0-acceptance-fixes-20260821.md)：

| 编号 | 问题 | 修复要点 | 状态 |
|---|---|---|---|
| P0-1 | 命令 schema 嵌套绕过（--override-ini pythonpath / addopts 注入 -p / uv run --offline / python -I 变体） | pytest 改**安全参数 allowlist**（未列入即拒绝）；`_command_key` 规范化解析 wrapper（-m 定位符不跳过）；wrapper 选项不在前缀也不在 allowlist → 拒绝 | ✅ |
| P0-2 | loopback 请求继承 HTTP_PROXY（httpx trust_env 默认 True，正文可能离开本机） | `_HttpAdapter` trust_env 参数；`OllamaChatAdapter` 默认 trust_env=False + require_loopback 构造校验（与 routes 层双保险） | ✅ |
| P1-1 | post-commit 查询失败被当成持久化失败 → 回滚磁盘 → DB=applied、磁盘=旧内容（反向分叉） | 补偿前先**新事务确认 durable 状态**：已 applied → 按成功路径收敛（绝不回滚磁盘）；仍 previewed → 才回滚 + 尽力落 rolled_back/partial_unknown | ✅ |
| P1-2 | `src/.../config.py` 等规范化后为空组件与正常路径别名 | `_validate_rel_path` 拒绝规范化后为空 / 折叠为 `..` / 折叠为 `.` 的组件 | ✅ |

第三轮复核：验收修复回归 **21 passed**；v0.7.0 全套 **135 passed**；全量 pytest 与完整门禁以
提交后复核为准。**K1（e2e 性能用例预算临界）持续**：门禁 p95=51ms 仍超 50ms 预算，单独复跑
50ms 通过——维持 v0.6.0 先例（不修改预算/测试），列为 v0.8.0 交接项（真实桌面负载基线
重新校准）。

### 7.4 第三轮修复后完整门禁（绑定 67241aa）

| 门禁 | 结果 |
|---|---|
| pytest 全量 | ✅ **962 passed**（957 + 5 第三轮修复用例） |
| ruff / compileall / npm build / npm test / cargo / sidecar / alembic 0030 / git diff / compose / redaction / latest.json | ✅ 全绿 |
| npm e2e | ❌ **50/51**：性能用例（K1 环境漂移临界，多轮门禁 p95 实测 73/58/51ms 均超 50ms 预算，重试后仍失败；v0.7.0 生产运行时零变化，非回归） |
| 机器口径 | passed=13 failed=1；worktree_dirty=False；issues 仅 installer_missing / evidence_not_bound（设计内预期） |

后端全量回归与全部构建/迁移/脱敏门禁保持全绿；唯一失败项为 K1 环境漂移性能用例
（预算临界 + 本机重负载），维持 v0.6.0 K2 先例不修改预算/测试，v0.8.0 交接项已记录。

### 7.5 第四轮验收复核（2026-08-21：@argsfile 与内联值绕过）

第四轮外部验收：**仍不通过**——Ollama 本地数据外发（trust_env=False + loopback）已确认
关闭，仍 2×P0 + 2×P1（@argsfile 绕过 / npm/cargo 内联值 / durable 不可判定回滚 /
重复斜杠别名）。处置详见 [验收修复记录第 0.2 节](./v0.7.0-acceptance-fixes-20260821.md)：

| 编号 | 问题 | 修复要点 | 状态 |
|---|---|---|---|
| P0-1 | pytest @argsfile 绕过 allowlist（展开后重新注入 -p/--override-ini） | 位置参数 @ 开头一律拒绝（两路接入） | ✅ |
| P0-2 | npm/cargo 未知 flag 内联值未校验（-- 透传、--target=C:outside 等） | `--` 分隔符一律拒绝；cargo/npm 补齐完整 allow/path/reject schema（--target/--target-dir/--prefix/--cache/--config 值 workspace 内） | ✅ |
| P1-1 | durable 不可判定时仍回滚磁盘（commit 成功 + DB 暂不可达 → 分叉） | 三态化：不可判定 → 保留磁盘与备份 + partial_unknown（人工处置）；确认 applied → 成功收敛；确认 previewed → 才回滚 | ✅ |
| P1-2 | 重复斜杠别名（a//b 与 a/b 唯一性键不同） | `_windows_normalize` 跳过空组件，键一致判重复 | ✅ |

第四轮复核：验收修复回归 **24 passed**；v0.7.0 全套 **138 passed**；全量 pytest 与完整门禁以
提交后复核为准。**K1（e2e 性能用例预算临界）持续**（门禁 p95=66ms）：维持先例不修改预算/
测试，v0.8.0 交接项已记录。

### 7.6 第四轮修复后完整门禁（绑定 b2047a1）——验收修复以来首次 14/14 全绿

| 门禁 | 结果 |
|---|---|
| pytest 全量 | ✅ **965 passed**（962 + 3 第四轮修复用例） |
| ruff / compileall / npm build / npm test / cargo / sidecar / alembic 0030 / git diff / compose / redaction / latest.json | ✅ 全绿 |
| npm e2e | ✅ **51 passed**（性能用例本次通过——K1 环境漂移抖动被吸收，印证非回归判定） |
| 机器口径 | **passed=14 failed=0 skipped=0**；worktree_dirty=False；ok=False 仅剩 2 个设计内预期项（installer_missing / evidence_not_bound，内部开发完成版不切公开 updater） |

四轮验收修复（12 + 4 + 5 + 3 = 24 个回归测试）全部落地，后端全量 965 passed 零失败，
完整发布门禁首次 14/14 全绿（e2e 性能用例通过）。**可提请复审冻结。**

### 7.7 第五轮验收复核（2026-08-22：SAFE 泄漏与执行层约束）

第五轮外部验收：**仍不通过**——Ollama loopback/proxy 与 PatchSet 模糊提交状态已确认闭环，
仍 3×P0 + 1×P1（workspace SAFE 工具级泄漏 / pytest -W 与 python -c / allow_network 无执行层
约束 / . 组件路径别名）。处置详见
[验收修复记录第 0.3 节](./v0.7.0-acceptance-fixes-20260821.md)：

| 编号 | 问题 | 修复要点 | 状态 |
|---|---|---|---|
| P0-1 | workspace SAFE 是工具级而非 profile 级：未匹配 profile 的全局白名单命令也免确认 | `permission_mode` 透传 `_resolve_command`：workspace 模式未匹配 profile → 拒绝（SAFE 不泄漏到全局白名单兜底） | ✅ |
| P0-2 | `pytest -W` 可导入外部模块；python 前缀可追加 `-c` 代码执行 | pytest allowlist 移除 -W；python/py 新增 schema（-c/-m/-i/-W 拒绝） | ✅ |
| P0-3 | allow_network=False 未参与执行 | `_reject_network_args`：URL 形态参数（scheme://）在 allow_network=false 时拒绝 | ✅ |
| P1-1 | `src/./x.py` 与 `src/x.py` 唯一性键不同 | `_windows_normalize` 跳过规范化后为空组件（`.` 折叠） | ✅ |

第五轮复核：验收修复回归 **28 passed**；v0.7.0 全套 **142 passed**；全量 pytest 与完整门禁以
提交后复核为准。

### 7.8 第五轮修复后完整门禁（绑定 887158c）

| 门禁 | 结果 |
|---|---|
| pytest 全量 | ✅ **969 passed**（965 + 4 第五轮修复用例） |
| ruff / compileall / npm build / npm test / cargo / sidecar / alembic 0030 / git diff / compose / redaction / latest.json | ✅ 全绿 |
| npm e2e | ❌ **50/51**：性能用例（K1 环境漂移临界，多轮门禁 p95 实测 73/58/51/66/53ms 均超 50ms 预算或 flaky；v0.7.0 生产运行时零变化，非回归） |
| 机器口径 | passed=13 failed=1；worktree_dirty=False；issues 仅 installer_missing / evidence_not_bound（设计内预期） |

五轮验收修复（12 + 4 + 5 + 3 + 4 = 28 个回归测试）全部落地，后端全量 969 passed 零失败；
唯一门禁失败项为 K1 环境漂移性能用例（预算临界 + 本机重负载），维持 v0.6.0 K2 先例
不修改预算/测试，v0.8.0 交接项已记录（真实桌面负载基线重新校准）。

### 7.9 第六轮验收复核（2026-08-22：网络隔离本质与执行路径统一）

第六轮外部验收：**仍不通过**——pytest -W、python -c/-m、未匹配全局命令、`./` 路径碰撞
与异步测试警告已确认关闭；仍 3×P0 + 1×P1（allow_network 非真实隔离 / 未知工具代码执行 /
前缀策略遮蔽 / 手动运行路由绕过）。处置详见
[验收修复记录第 0.4 节](./v0.7.0-acceptance-fixes-20260821.md)：

| 编号 | 问题 | 修复要点 | 状态 |
|---|---|---|---|
| P0-1 | allow_network=False 非真实网络隔离（URL 正则无法覆盖 IP/socket/脚本内） | workspace SAFE 仅当全部 profile safe 且全部 allow_network=True；存在 allow_network=False → CONFIRM（禁止自动执行，审批把关） | ✅ |
| P0-2 | 未知 safe profile 可追加任意代码参数（node -e / pwsh 等） | safe profile 的未知工具必须精确 argv（剩余参数非空即拒绝）；有 schema 工具按 allowlist | ✅ |
| P0-3 | 前缀匹配策略遮蔽（后创建宽泛 safe 遮蔽 restricted） | 最长前缀优先，同长度取最严格 risk（restricted > confirm > safe） | ✅ |
| P1-1 | 手动运行路由走旧执行器（忽略 project_id、无进程树清理） | run 端点校验 profile.project_id；`CommandProfileService.run` 统一走受审计执行器 | ✅ |

第六轮复核：验收修复回归 **32 passed**；v0.7.0 全套 **146 passed**；全量 pytest 与完整门禁以
提交后复核为准。

### 7.10 第六轮修复后完整门禁（绑定 29914c8）——14/14 全绿

| 门禁 | 结果 |
|---|---|
| pytest 全量 | ✅ **973 passed**（969 + 4 第六轮修复用例） |
| ruff / compileall / npm build / npm test / cargo / sidecar / alembic 0030 / git diff / compose / redaction / latest.json | ✅ 全绿 |
| npm e2e | ✅ **52 passed**（含性能用例——K1 环境漂移抖动被吸收，非回归判定持续印证） |
| 机器口径 | **passed=14 failed=0 skipped=0**；worktree_dirty=False；ok=False 仅剩 2 个设计内预期项（installer_missing / evidence_not_bound，内部开发完成版不切公开 updater） |

六轮验收修复（12 + 4 + 5 + 3 + 4 + 4 = 32 个回归测试）全部落地，后端全量 973 passed 零失败，
完整发布门禁第二次 14/14 全绿（e2e 性能用例通过）。**可提请复审冻结。**

### 7.11 第七轮验收复核（2026-08-22：验收通过 + 2×P2 观察项处置）

第七轮外部验收：**通过**——前六轮 16×P0 + 8×P1 全部确认闭环（逐文件复核修复代码与
回归测试：第六轮 allow_network/未知工具精确 argv/前缀遮蔽/手动路由四项实现与文档一致；
argv schema 分层、PatchSet 路径校验与三态补偿、模型 gateway loopback/trust_env 抽检在位；
写面 apply_patch_to_workspace / apply_patch_set 均为 CONFIRM，workspace 模式下仍逐次审批）。
完整门禁第三次 14/14 全绿（绑定 4844ac3：pytest 973、e2e 含性能用例通过、alembic 0030、
worktree_dirty=False；ok=False 仅 installer_missing / evidence_not_bound 设计内预期项）。
无新 P0/P1；2 个 P2 观察项当场处置（详见
[验收修复记录第 0.5 节](./v0.7.0-acceptance-fixes-20260821.md)）：

| 编号 | 问题 | 修复要点 | 状态 |
|---|---|---|---|
| O-1 | 手动运行 restricted profile 返回 500（PermissionError_ 落入 RuntimeError 分支；零执行语义正确，仅 HTTP 状态粗糙） | run 端点新增 `except PermissionError_` → 403（与 routes_projects/routes_integrations 惯例一致） | ✅ |
| O-2 | workspace SAFE 判定的单回合 TOCTOU：工具 risk 在 dispatcher 构建时求值，run 进行中收紧 profile 后当回合仍按 SAFE 免确认放行 | `command_risk` 透传执行链：SAFE 自动允许在执行时按**当前**匹配 profile 复核（仍 safe 且 allow_network=True 才放行，否则失败关闭，下一回合按 CONFIRM 审批收敛；CONFIRM 工具不受影响） | ✅ |

第七轮复核：验收修复回归 **34 passed**（32 + 2 新增）；v0.7.0 全套 **148 passed**；ruff 全绿。

### 7.12 第七轮修复后完整门禁（绑定 2d438b0）——14/14 全绿

| 门禁 | 结果 |
|---|---|
| pytest 全量 | ✅ **975 passed**（973 + 2 第七轮观察项处置用例） |
| ruff / compileall / npm build / npm test / cargo / sidecar / alembic 0030 / git diff / compose / redaction / latest.json | ✅ 全绿 |
| npm e2e | ✅ 全部通过（性能用例通过——K1 环境漂移非回归判定持续印证） |
| 机器口径 | **passed=14 failed=0 skipped=0**；worktree_dirty=False；ok=False 仅剩 2 个设计内预期项（installer_missing / evidence_not_bound，内部开发完成版不切公开 updater） |

七轮验收累计 36 个回归测试（12 + 4 + 5 + 3 + 4 + 4 + 2 + 2 观察项）全部落地，后端全量
975 passed 零失败，完整发布门禁 14/14 全绿。**第七轮验收通过；v0.7.0（内部开发完成版）
达到冻结标准，复审冻结。**
