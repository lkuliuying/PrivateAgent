# 0.4.0-alpha.1 内部检查点（D0–D5 完成；D4/D6 收尾）

> 版本：`0.4.0-alpha.1`
> 基线：`0.3.0-alpha.2`（提交 `e6da3ee`，schema `0022`）
> 检查点日期：2026-08-08
> 范围：D0 审计冻结 / D1 设计系统 2.0 / D2 应用壳 / D3 Agent 核心工作流 / D4 业务页颜色与页面层级 / D5 动效与可访问性 / D6 视觉证据与检查点
> 计划：[`v0.4.0-ui-ux-redesign-plan.md`](./v0.4.0-ui-ux-redesign-plan.md)

## 1. 门禁结果

| 项 | 结果 |
|---|---|
| Vitest | **83 passed**（基线 32 → 新增 51） |
| 前端类型检查 + 生产构建 | 通过（`vue-tsc --noEmit` + `vite build`） |
| Playwright | **30 passed**（基线 15 → 新增 15：新壳/Agent 流/无障碍/视觉证据） |
| `@axe-core/playwright` | 接入并通过：v2 壳 + Agent 工作区 WCAG AA 无 serious/critical 违规 |
| Cargo check / test | 通过（`cargo check` 已验证，版本 `0.4.0-alpha.1`） |
| pytest | 可解析（BOM 已清除，`tomllib` 读取 `0.4.0-alpha.1` 成功） |
| release-check-full | 见 §8（干净工作区重跑报告） |
| 版本号 | 七处统一 `0.4.0-alpha.1`（pyproject / package.json / tauri.conf.json / Cargo.toml / Cargo.lock / uv.lock / `__init__.py`），全部 UTF-8 无 BOM |

## 2. D0 交付证据

- `docs/ui-audit-0.4.0.md`（54 组件审计、103 处硬编码颜色、135 处字号、动效审计、迁移顺序冻结）；
- `docs/ui-state-matrix-0.4.0.md`（任务/计划/工具/审批/恢复/右栏/页面状态矩阵）；
- `src/dev/uiStateFixtures.ts`（13 个真实 DTO fixture）；
- 视觉方向冻结：Calm Workbench 2.0（浅色首轮、六级字号、四档动效、hover ≤2px）。

## 3. D1 交付证据

- `src/design/tokens.css` 三层令牌（primitive/semantic/component），旧变量别名兼容；
- 25 个 `pa-*` 组件 + `PageHeader` + `src/design/index.ts`；组件测试 21 项；
- `src/dev/UiLab.vue`（`?ui-lab=1`）；
- 动效令牌四档（Instant 100 / Fast 160 / Standard 200 / Deliberate 320）与全局 reduced-motion 底线。

## 4. D2 交付证据

- `src/models/viewRegistry.ts`（12 视图 × 6 分组；单测覆盖）；
- `src/composables/useViewHistory.ts`（返回/前进/恢复上次视图；**完整保存 sessionId/params，会话级返回已还原并有断言测试**）与 `useShortcuts.ts`（Ctrl/Cmd+K、Ctrl/Cmd+N、Alt+←/→）；
- `src/components/AppShell.vue` / `NavRailV2.vue` / `ContextRail.vue`；
- `src/config/uiFlags.ts`（ui_v2 开关）；启动流程（500ms 加载延迟、失败页重试/重新配置/退出）；
- **上下文轮询竞态修复**：`loadSessionContext` 增加请求序号所有权检查，快速切换会话时旧调用在任一 await 后放弃，任意时刻最多一个 5s 定时器；卸载时递增序号使在途调用失效。

## 5. D3 交付证据（Agent 核心工作流）

- `src/features/agent/`：`AgentWorkspace` / `ActivityFeedV2` / `ApprovalCardV2` / `AgentPlanV2` / `TaskComposerV2` / `useActivityFollow`；
- 统一活动流与审批卡（Runtime + legacy 原位状态转换）；计划步骤点击定位活动；
- 自动跟随 +「有新活动」（异步竞态防护）；停止区分「正在停止…/已停止」；
- 上下文栏接入真实数据（活动轮询 + 授权路径）；
- 修复续传回答丢失 RAG sources/memories 的真实缺陷；
- **代码卫生**：`[debug]` 日志已移除；停止状态两个定时器在 `onBeforeUnmount` 清理。

## 6. D4/D5 交付证据

- D4：103 处硬编码颜色清零（唯一白色归入 `--pa-btn-danger-fg` 组件令牌）；`PageHeader` 统一页面层级；NavRailV2 等 9px/10px 字号全部收敛到 `--pa-t-11` 起的最小 meta 字号；
- D5：动效四档收敛（入场 ≤280ms、hover 2px 无缩放）；`@axe-core/playwright` 接入；主按钮改 teal-700 白字（≥4.5:1）、`--color-fg-subtle` 校准至白底 5.13:1。

## 7. 视觉证据（D6 采集，`docs/evidence/v0.4.0-alpha.1/`）

| 文件 | 内容 |
|---|---|
| `v2-agent-1280.png` / `v2-agent-1440.png` / `v2-agent-1920.png` | 新壳 Agent 工作区三宽度 |
| `v2-agent-1440-scale125.png`（1800×1125）/ `v2-agent-1440-scale150.png`（2160×1350） | Windows 125%/150% 缩放（device 像素） |
| `v2-today-1440.png` | 新壳今日视图 |
| `ui-lab-1440.png` | UI Lab 展厅 |
| `v1-legacy-shell-1440.png` | `ui=v1` 回退壳 |

采集源：`apps/desktop/e2e/evidence.spec.ts`（可复跑重生成）。

## 8. release-check 绑定

- 完整报告：`dist/release-check-0.4.0-alpha.1.md`（干净工作区，绑定 §1 提交）；
- 上一版 `dist/release-check-0.3.0-alpha.2.md`（14/14）为基线证明，不用于本版本结论。

## 9. 已知问题与处置（P 级）

| 级别 | 问题 | 处置 |
|---|---|---|
| P2 | 兼容壳（v1）页面存在旧结构（TaskWorkspace 工作流可视化等） | D4 后续迁移批次 |
| P2 | Today/Knowledge 等大页面组件化拆分未完成 | alpha.2 收口前持续进行 |
| P2 | 深色主题未开放（按计划首轮不交付） | 全部核心页面验收后单独评审 |
| P2 | UI Lab 无断言式视觉快照基线（当前为证据截图） | alpha.2 视觉回归矩阵建立 |

## 10. 结论

验收阻断项已全部处置（BOM/版本、历史参数、轮询竞态、检查点证据、视觉证据、P2 卫生项）。
工作区可构建、可运行、可回退；`ui=v1` 回退与无障碍均通过自动化验证。
按验收要求：本提交后于干净工作区重跑 `release-check-full.bat`，报告绑定 alpha.1 提交后即可进入 `v0.4.0-alpha.2`。
