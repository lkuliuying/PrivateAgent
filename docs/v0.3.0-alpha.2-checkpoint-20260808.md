# PrivateAgent 0.3.0-alpha.2 内部检查点记录

> - 检查点日期：2026-08-08
> - 类型：内部预发布检查点（不进入稳定 GitHub Release 与正式 updater 渠道）
> - 版本性质：Runtime 能力冻结，供 `0.4.0-alpha` UI 全面改造使用
> - 冻结提交：`77b0c88`（工作区干净，所有改动已纳入提交）
> - 关联计划：[`v0.3.0-development-plan.md`](./v0.3.0-development-plan.md)、[`integrated-0.5.0-development-plan-20260808.md`](./integrated-0.5.0-development-plan-20260808.md)
> - 公开契约：[`v0.3.0-public-contracts.md`](./v0.3.0-public-contracts.md)

## 1. 本次新增

- **A5 契约冻结**：`docs/v0.3.0-public-contracts.md` 冻结 capabilities/SSE/run 状态/事件/审批/RAG 引用/摘要元数据/telemetry 标签/错误码词汇；`tests/test_public_contracts.py`（12 项）以测试固化，破坏性变更必须先更新文档与相关 E2E。
- **A3 真实模型审批缺陷修复（重要）**：
  - 根因：`get_agent_tool_bundle` 在 RAG 开启时无条件注入 RAG 引用验证器（带 strict JSON `output_schema`），普通聊天也被强制 JSON 输出；qwen2.5:14b 在带 `format` 的请求下抑制工具调用，CONFIRM 工具审批在真实模型中永远无法触发（此前 M0 演练 3 次均 `approval_not_triggered`）。
  - 修复：RAG 引用验证器只对 `knowledge_base=true` 注入（chat 与 `/agent-runs` 两路径一致）；新增 additive 迁移 `0022` 持久化 `agent_runs.knowledge_base`，审批恢复路径按同一持久化事实判断。
  - 验证：真实模型端到端审批（`waiting_approval → approve → completed`）事件链完整；新增回归测试 `test_plain_chat_does_not_inject_rag_verifier_but_rag_chat_does`。
- **A0 工具链可信度修复**：
  - `m0_agent_run_driver.py`：取消分支接受 `created` 状态（create 响应即为 created），修复取消样本从未触发的问题；
  - `m0_gate_runner.py`：审批只在审批提示词所在 run 上尝试一次（此前首轮即吞掉普通 run 记录）；审批未触发时按普通 run 记录并附 `approval_note`；新增 `POST /files/authorize` 预授权（read_file 为 CONFIRM 工具需要授权路径）；
  - **崩溃 reconcile 验证修复**：演练改用短宽限期（`RECONCILE_GRACE_SECONDS=60`，与 sidecar 环境一致），崩溃窗口超过宽限期后执行 reconcile 并验证关闭，消除「UPDATE 0 行必然失败」的工具缺陷；窗口审计 engine 指向 `--db-url`（此前查错库）；
  - **owner lock 竞争验证修复**：lock contention 移到首个 sidecar 仍持有锁时执行（此前在 cycle 结束后运行，锁已释放导致样本无效）；
  - **安装版演练支持**：新增 `--data-dir`（注入 `PA_DATA_DIR/.env` 完整配置，修复 frozen 模式只读 `_default_data_dir()/.env` 的偏差）与 `--migrate`（不跳过首次启动迁移）。
- **A4 安装版 smoke**：新增 `scripts/summary_worker_smoke.py`，对构建版 sidecar 做摘要 worker 端到端验收（本地模型生成、幂等、原始消息保留、telemetry 无正文、优雅停机、无残留）。
- **版本统一**：Python（`pyproject.toml`/`__init__.py`）、Vue（`package.json`/`package-lock.json`）、Tauri（`tauri.conf.json`）、Cargo（`Cargo.toml`/`Cargo.lock`）统一为 `0.3.0-alpha.2`；`latest.json` 校验支持预发布检查点不更新正式渠道。

## 2. 门禁证据（绑定冻结提交 `77b0c88`，工作区干净）

| 门禁 | 结果 |
|---|---:|
| release-check-full（pytest/ruff/compileall/npm build/vitest/playwright/cargo/alembic/latest.json 等 14 项） | 14 passed / 0 failed / 0 skipped |
| pytest 全量 | 637 passed |
| 数据库 schema | `0022`（additive：`agent_runs.knowledge_base`） |
| M0 开发期基线演练（`dist/m0-alpha-0.3.0-alpha.2.json`） | 有效 completed run 19、RAG 21、取消 ✓、审批 ✓、Ollama 中断 ✓、3 次正常退出 ✓、崩溃 reconcile ✓、无双 coordinator ✓、无陈旧/负时长窗口、无 P0/P1；`expected_sha256` 已绑定 |
| M0 安装版全新目录演练（`dist/m0-alpha-0.3.0-alpha.2-installed.json`） | 首次启动迁移 ✓、隔离数据目录 ✓、取消/审批/Ollama 中断 ✓、3 次正常退出 ✓、崩溃 reconcile ✓、无双 coordinator ✓ |
| 摘要 worker 安装版 smoke | passed（幂等 ✓、消息保留 ✓、telemetry 无正文 ✓、无残留 ✓） |
| 全新安装实机 | `C:\ProgramSoftware\PrivateAgent`，sidecar SHA 与 manifest 一致 |
| 0.2.1 → 0.3.0-alpha.2 覆盖安装实机 | SHA 由 `c4aecec8` 变为 `c3489d82`；数据保留校验通过（升级前后 10 表计数一致）；upgrade smoke run #29 passed |

## 3. Feature Flag 快照（开发环境）

```text
PA_AGENT_RUNS_API_ENABLED=true
PA_AGENT_RUN_READ_ONLY_TOOLS_ENABLED=true
PA_AGENT_CONTEXT_BUILDER_ENABLED=true
PA_AGENT_OUTPUT_VERIFICATION_ENABLED=true
PA_AGENT_RAG_TOOLS_ENABLED=true
PA_COMPATIBILITY_TELEMETRY_PERSIST_ENABLED=true
PA_CHAT_AGENT_RUNTIME_ENABLED=false        # 稳定配置保持关闭；Alpha QA 配置开启
PA_CONVERSATION_SUMMARY_WORKER_ENABLED=false  # 分阶段开启；smoke 单独开启
PA_CONVERSATION_SUMMARY_ALLOW_REMOTE_PROVIDER=false
PA_MCP_ENABLED=false
```

完整快照见 `dist/alpha-0.3.0-alpha.2-flags.json`。

## 4. 已知问题与回退

- 旧 M0 runner 的 `observation_days>=7`、`valid_completed_runs>=100` 门槛按新计划不再作为阻塞标准（开发期基线 20 run 即达标），`gate_pass=false` 仅由这两项引起。
- 回退：关闭 `PA_CHAT_AGENT_RUNTIME_ENABLED` 回到 legacy 普通聊天；RAG 聊天关闭 `PA_AGENT_RAG_TOOLS_ENABLED` 走兼容路径；关闭摘要 worker 不影响聊天主链。全部不需要数据库 downgrade。
- 迁移 `0022` 为 additive，旧应用可忽略新列。
- 安装包未正式代码签名（`codesign-status-0.3.0-alpha.2.json`），与 0.2.1 一致；正式证书交付延后到 `0.6.0+`。

## 5. Alpha 产物清单

- `docs/v0.3.0-public-contracts.md`（公开契约冻结清单）
- `docs/v0.3.0-alpha.2-checkpoint-20260808.md`（本文）
- `tests/test_public_contracts.py`（契约冻结测试）
- `scripts/m0_gate_runner.py`、`scripts/m0_agent_run_driver.py`（工具链修复）
- `scripts/summary_worker_smoke.py`（摘要安装版 smoke）
- `scripts/run_release_checks.py`（latest.json 预发布校验）
- `alembic/versions/0022_agent_run_knowledge_base.py`（additive 迁移）
- `dist/release-check-0.3.0-alpha.2.json/.md`（完整门禁）
- `dist/alpha-0.3.0-alpha.2-manifest.json`（commit/SHA-256）
- `dist/alpha-0.3.0-alpha.2-flags.json`（flag 快照）
- `dist/alpha-0.3.0-alpha.2-report.json`（门禁汇总）
- `dist/m0-alpha-0.3.0-alpha.2.json`（开发期基线演练）
- `dist/m0-alpha-0.3.0-alpha.2-installed.json`（安装版全新目录演练）
- `dist/summary-smoke-report.json`（摘要 smoke 报告）
- `dist/upgrade-before-alpha.json` / `dist/upgrade-after-alpha.json`（覆盖安装数据保留）

## 6. 后续

- `0.4.0-alpha.1`：UI 设计系统与新应用壳（T2），可与 `0.4.0-alpha.2` 页面迁移并行推进；
- 正式使用观察唯一一次在 `0.5.0-rc.1` 统一进行，本检查点不再附加等待。
