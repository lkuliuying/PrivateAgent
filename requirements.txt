# pip 本机开发依赖；运行时约束与 pyproject.toml 对齐。
# 可重复构建以 uv.lock 为准，不包含旧服务器、MySQL 或 RAG 依赖。
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.6
tzdata>=2024.1
httpx[socks]>=0.27
jsonschema>=4.26,<5
mcp==2.0.0
httpx2==2.9.1
httpcore2==2.9.1

# 开发检查与本机执行器打包
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.16
pyinstaller>=6.21.0
