# PrivateAgent 项目会话入口

适用范围：本仓库及其子目录。本文提供共享上下文，不替代用户当前请求，也不新增服务器操作、发布或凭据访问授权。

## 接手时先读

1. 先完整阅读 [项目状态记忆](docs/project-state.md)，确认记录日期、证据来源和待确认事项；不要默认其他会话的历史已传入当前上下文。
2. 只读核对当前工作区：

   ```powershell
   git status --short --branch
   git log -5 --oneline
   git diff --stat
   git diff --staged --stat
   ```

3. 再查看当前任务涉及的源码与差异。涉及联网版 1.0.3 模型/日志故障时，补读 [修复总结](docs/solutions/2026-08-31-privateagent-1-0-3.md) 和 [服务器操作说明](docs/connected-runtime-1.0.3-repair.md)。无需无差别读取全部历史文档。
4. 如记忆与当前源码、运行证据或用户新回执冲突，说明差异与时间，以对应环境的最新证据为准；未核实的状态继续写“待确认”。

## 必须区分的状态

- 普通版完整本机后端与联网版 `PrivateAgentRemote` 的本机轻量执行器是两种交付方式，不能混用依赖、版本号或更新通道。
- 代码存在、测试通过、构建成功、部署成功、真实账号验收通过是不同结论，必须分别记录。
- 本地 Git 引用不等于远端实时状态；源码目录不等于运行进程实际加载的安装副本。
- 不得把新检查进程的结果说成直接检查了原服务进程内存。
- 仓库内可能有其他会话的未提交改动。先核对归属，保留它们，不清理、强制覆盖或回退历史。

## 工程与安全约定

- 按现有 Tauri/Vue、FastAPI/SQLAlchemy 架构实现，先查源码依据；需求缺口影响实现时明确提出，不编造隐藏业务逻辑。
- 编写或修改代码注释时使用中文。检查异常、空值、权限、并发和资源释放；测试针对真实边界，不用错误测试替身掩盖模型契约问题。
- 测试与生产隔离；不把生产数据库用于测试，不为验证可用性擅自发起付费模型调用。
- 不读取、复制或写入共享文档中的密码、令牌、私钥、完整环境配置或生产日志正文；不提取 FinalShell 凭据，不绕过审批或安全检查。
- 文档中的服务器命令是操作参考，先核对当前环境与会话已有授权；本文不要求对已经授权的步骤重复确认。

## 交接要求

最终说明改了什么、实际验证了什么、未验证什么。仅在用户明确要求新增或更新项目记忆时，维护 `docs/project-state.md` 的日期、证据与状态；普通修复、测试、交付或生成交接文档不自动触发记忆改写。保留历史记录并注明被后续证据修正的结论，不把推测改写为已完成。
